---
phase: 84-toast
plan: "02"
type: execute
wave: 2
depends_on:
  - "84-01"
files_modified:
  - apps/docs/src/pages/components/ToastPage.tsx
autonomous: true
requirements:
  - TST-02

must_haves:
  truths:
    - "Toast CSS vars table in docs shows correct double-fallback var() form for color tokens matching :root"
    - "Shadow default in docs shows the two-layer value from tailwind.css :root"
    - "z-index default in docs shows 55 (matching tailwind.css :root, not stale 50)"
    - "toastCSSVars array includes --ui-toast-padding (currently missing)"
    - "cssVarsCode example uses semantic token references (not hardcoded hex values)"
  artifacts:
    - path: "apps/docs/src/pages/components/ToastPage.tsx"
      provides: "Accurate toastCSSVars array (22 entries with padding added) and corrected cssVarsCode example"
      contains: "var(--color-card, var(--ui-color-card))"
  key_links:
    - from: "apps/docs/src/pages/components/ToastPage.tsx (toastCSSVars)"
      to: "packages/core/src/styles/tailwind.css :root toast block"
      via: "CSS token default values must match exactly"
      pattern: "toastCSSVars default strings = tailwind.css :root values verbatim"
---

<objective>
Update the `toastCSSVars` array and `cssVarsCode` example string in `ToastPage.tsx` so the docs accurately reflect the actual CSS token defaults from `tailwind.css` :root.

Purpose: Developers reading the docs get accurate defaults for overriding toast tokens. Stale/wrong defaults (single-fallback var(), wrong shadow opacity, wrong z-index, missing padding entry, hex literals in example) mislead developers.
Output: `ToastPage.tsx` with corrected color token defaults (double-fallback form), corrected shadow (two-layer), corrected z-index (55 not 50), added padding entry, and updated cssVarsCode example using semantic token references.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key context the executor needs. -->

Actual :root values from packages/core/src/styles/tailwind.css (ground truth):
```
--ui-toast-bg:            var(--color-card, var(--ui-color-card))
--ui-toast-text:          var(--color-card-foreground, var(--ui-color-card-foreground))
--ui-toast-border:        var(--color-border, var(--ui-color-border))
--ui-toast-radius:        0.5rem
--ui-toast-padding:       1rem
--ui-toast-shadow:        0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)
--ui-toast-max-width:     24rem
--ui-toast-gap:           0.75rem
--ui-toast-z-index:       55
--ui-toast-success-bg:    oklch(0.95 0.05 150)
--ui-toast-success-border: oklch(0.70 0.15 150)
--ui-toast-success-icon:  oklch(0.55 0.20 150)
--ui-toast-error-bg:      oklch(0.95 0.05 25)
--ui-toast-error-border:  oklch(0.70 0.15 25)
--ui-toast-error-icon:    oklch(0.55 0.20 25)
--ui-toast-warning-bg:    oklch(0.95 0.05 85)
--ui-toast-warning-border: oklch(0.70 0.15 85)
--ui-toast-warning-icon:  oklch(0.55 0.20 85)
--ui-toast-info-bg:       oklch(0.95 0.05 250)
--ui-toast-info-border:   oklch(0.70 0.15 250)
--ui-toast-info-icon:     oklch(0.55 0.20 250)
```

Current (stale) values in ToastPage.tsx toastCSSVars that need correction:

1. --ui-toast-bg default: `'var(--color-card)'` → `'var(--color-card, var(--ui-color-card))'`
2. --ui-toast-text default: `'var(--color-card-foreground)'` → `'var(--color-card-foreground, var(--ui-color-card-foreground))'`
3. --ui-toast-border default: `'var(--color-border)'` → `'var(--color-border, var(--ui-color-border))'`
4. --ui-toast-shadow default: `'0 10px 15px -3px rgb(0 0 0 / 0.1)'` → `'0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)'`
5. --ui-toast-z-index default: `'50'` → `'55'`
6. MISSING entry for --ui-toast-padding — must be added after --ui-toast-radius:
   `{ name: '--ui-toast-padding', default: '1rem', description: 'Inner padding of the toast.' }`

The 12 variant entries (success/error/warning/info bg/border/icon) are correct in docs — do NOT change those.

Current cssVarsCode (stale — uses hex literals in the variant override section):
```css
/* Global override - affects all toasts */
:root {
  --ui-toast-radius: 1rem;
  --ui-toast-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.4);
  --ui-toast-padding: 1.25rem;
  --ui-toast-max-width: 28rem;
}

/* Customize variant colors */
:root {
  --ui-toast-success-bg: #f0fdf4;
  --ui-toast-success-border: #86efac;
  --ui-toast-success-icon: #16a34a;

  --ui-toast-error-bg: #fef2f2;
  --ui-toast-error-border: #fca5a5;
  --ui-toast-error-icon: #dc2626;
}
```

Replace cssVarsCode with semantic token references (same pattern as Phase 82-02 tooltip, Phase 83-02 popover):
```css
/* Global override - affects all toasts */
:root {
  --ui-toast-bg: var(--color-card, var(--ui-color-card));
  --ui-toast-text: var(--color-card-foreground, var(--ui-color-card-foreground));
  --ui-toast-border: var(--color-border, var(--ui-color-border));
  --ui-toast-shadow: 0 25px 50px -12px rgb(0 0 0 / 0.25);
  --ui-toast-max-width: 28rem;
}

/* Customize variant colors */
:root {
  --ui-toast-success-bg: oklch(0.95 0.05 150);
  --ui-toast-success-border: oklch(0.70 0.15 150);
  --ui-toast-success-icon: oklch(0.55 0.20 150);
}
```

Established pattern: Same fix applied in Phase 82-02 (tooltip) and Phase 83-02 (popover).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct toastCSSVars defaults, add padding entry, and update cssVarsCode in ToastPage.tsx</name>
  <files>apps/docs/src/pages/components/ToastPage.tsx</files>
  <action>
    In `apps/docs/src/pages/components/ToastPage.tsx`, make these targeted changes:

    **1. Update the `toastCSSVars` array — 5 corrections + 1 addition:**

    Change `--ui-toast-bg` default from:
    `'var(--color-card)'`
    to:
    `'var(--color-card, var(--ui-color-card))'`

    Change `--ui-toast-text` default from:
    `'var(--color-card-foreground)'`
    to:
    `'var(--color-card-foreground, var(--ui-color-card-foreground))'`

    Change `--ui-toast-border` default from:
    `'var(--color-border)'`
    to:
    `'var(--color-border, var(--ui-color-border))'`

    Change `--ui-toast-shadow` default from:
    `'0 10px 15px -3px rgb(0 0 0 / 0.1)'`
    to:
    `'0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)'`

    Change `--ui-toast-z-index` default from:
    `'50'`
    to:
    `'55'`

    Add a new entry for `--ui-toast-padding` immediately after the `--ui-toast-radius` entry:
    `{ name: '--ui-toast-padding', default: '1rem', description: 'Inner padding of the toast.' },`

    Do NOT change the 12 variant entries (success/error/warning/info -bg/-border/-icon) — they are correct.
    Do NOT change any other content (props, examples, demos, API tables, navigation).

    **2. Replace the `cssVarsCode` string:**

    Find the current cssVarsCode constant and replace it with:
    ```
    const cssVarsCode = `/* Global override - affects all toasts */
:root {
  --ui-toast-bg: var(--color-card, var(--ui-color-card));
  --ui-toast-text: var(--color-card-foreground, var(--ui-color-card-foreground));
  --ui-toast-border: var(--color-border, var(--ui-color-border));
  --ui-toast-shadow: 0 25px 50px -12px rgb(0 0 0 / 0.25);
  --ui-toast-max-width: 28rem;
}

/* Customize variant colors */
:root {
  --ui-toast-success-bg: oklch(0.95 0.05 150);
  --ui-toast-success-border: oklch(0.70 0.15 150);
  --ui-toast-success-icon: oklch(0.55 0.20 150);
}`;
    ```
  </action>
  <verify>
    <automated>grep -n "ui-toast-bg\|ui-toast-text\|ui-toast-border\|ui-toast-shadow\|ui-toast-z-index\|ui-toast-padding" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/ToastPage.tsx</automated>
    Expect:
    - --ui-toast-bg default shows double-fallback var() form
    - --ui-toast-text default shows double-fallback var() form
    - --ui-toast-border default shows double-fallback var() form
    - --ui-toast-shadow shows two-layer value with both 0.1 terms
    - --ui-toast-z-index default shows 55 (not 50)
    - --ui-toast-padding entry exists with default '1rem'
    - No hex literals (#f0fdf4, #86efac, #fef2f2, etc.) remain in cssVarsCode
  </verify>
  <done>
    - `--ui-toast-bg` default: `var(--color-card, var(--ui-color-card))`
    - `--ui-toast-text` default: `var(--color-card-foreground, var(--ui-color-card-foreground))`
    - `--ui-toast-border` default: `var(--color-border, var(--ui-color-border))`
    - `--ui-toast-shadow` default: two-layer value matching tailwind.css :root
    - `--ui-toast-z-index` default: `55`
    - `--ui-toast-padding` entry added with default `1rem`
    - `cssVarsCode` uses semantic token references, no hex literals
    - toastCSSVars array has 22 entries (was 21, now includes padding)
  </done>
</task>

</tasks>

<verification>
grep -n "ui-toast-z-index" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/ToastPage.tsx
Expected: shows '55' (not '50')

grep -c "ui-toast-padding" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/ToastPage.tsx
Expected: at least 1 (the new entry)

grep "hex\|#f0fdf4\|#86efac\|#fef2f2" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/ToastPage.tsx
Expected: 0 matches (hex literals removed from cssVarsCode)
</verification>

<success_criteria>
- All CSS token defaults in `toastCSSVars` match `tailwind.css` :root values exactly
- Color tokens (bg, text, border) use double-fallback var() form
- Shadow uses two-layer value matching :root
- z-index is 55 (corrected from stale 50)
- --ui-toast-padding entry added (was missing)
- `cssVarsCode` example uses semantic token references (not hardcoded hex values)
</success_criteria>

<output>
After completion, create `.planning/phases/84-toast/84-02-SUMMARY.md`
</output>
