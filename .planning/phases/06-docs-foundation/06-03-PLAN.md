---
phase: 06-docs-foundation
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - docs/src/App.tsx
  - docs/src/pages/Placeholder.tsx
autonomous: false

must_haves:
  truths:
    - "Docs app runs at /docs with navigation visible"
    - "Clicking nav links changes content area"
    - "Active nav link is visually highlighted"
    - "Mobile hamburger opens navigation sheet"
    - "Layout adapts correctly from mobile to desktop"
  artifacts:
    - path: "docs/src/App.tsx"
      provides: "React Router configuration with layout route"
      contains: "DocsLayout"
    - path: "docs/src/pages/Placeholder.tsx"
      provides: "Placeholder page component for all routes"
      min_lines: 10
  key_links:
    - from: "docs/src/App.tsx"
      to: "docs/src/layouts/DocsLayout.tsx"
      via: "Route element"
      pattern: "element=.*DocsLayout"
    - from: "docs/src/layouts/DocsLayout.tsx"
      to: "docs/src/pages/Placeholder.tsx"
      via: "Outlet renders child routes"
      pattern: "Outlet"
---

<objective>
Wire React Router with layout routes and verify the complete docs shell works.

Purpose: Connect all components with routing so navigation works, then verify the responsive layout behaves correctly on mobile and desktop.
Output: Working docs shell with navigation that users can click through, ready for content pages in later phases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-docs-foundation/06-RESEARCH.md
@.planning/phases/06-docs-foundation/06-02-SUMMARY.md

# Components to wire
@docs/src/layouts/DocsLayout.tsx
@docs/src/nav.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder page and wire routes</name>
  <files>
    docs/src/pages/Placeholder.tsx
    docs/src/App.tsx
  </files>
  <action>
1. **docs/src/pages/Placeholder.tsx**: Generic placeholder for all doc pages

```typescript
import { useLocation } from 'react-router'

export function Placeholder() {
  const location = useLocation()

  // Convert path to title: /components/button -> "Components / Button"
  const pathParts = location.pathname
    .split('/')
    .filter(Boolean)
    .map(part => part.charAt(0).toUpperCase() + part.slice(1).replace(/-/g, ' '))

  const title = pathParts.length > 0 ? pathParts.join(' / ') : 'Introduction'

  return (
    <div className="prose prose-gray max-w-none">
      <h1 className="text-3xl font-bold text-gray-900 mb-4">{title}</h1>
      <p className="text-gray-600 text-lg">
        This page is coming soon. Content will be added in later phases.
      </p>
      <div className="mt-8 p-4 bg-gray-50 rounded-lg border border-gray-200">
        <p className="text-sm text-gray-500 font-mono">
          Route: {location.pathname}
        </p>
      </div>
    </div>
  )
}
```

2. **docs/src/App.tsx**: Complete router configuration

```typescript
import { BrowserRouter, Routes, Route } from 'react-router'
import { DocsLayout } from './layouts/DocsLayout'
import { Placeholder } from './pages/Placeholder'

export default function App() {
  return (
    <BrowserRouter basename="/docs">
      <Routes>
        <Route element={<DocsLayout />}>
          {/* Index route */}
          <Route index element={<Placeholder />} />

          {/* Getting Started */}
          <Route path="installation" element={<Placeholder />} />
          <Route path="quick-start" element={<Placeholder />} />

          {/* Components */}
          <Route path="components/button" element={<Placeholder />} />
          <Route path="components/dialog" element={<Placeholder />} />

          {/* Guides */}
          <Route path="guides/react" element={<Placeholder />} />
          <Route path="guides/vue" element={<Placeholder />} />
          <Route path="guides/svelte" element={<Placeholder />} />
          <Route path="guides/theming" element={<Placeholder />} />
          <Route path="guides/accessibility" element={<Placeholder />} />

          {/* Catch-all for undefined routes */}
          <Route path="*" element={<Placeholder />} />
        </Route>
      </Routes>
    </BrowserRouter>
  )
}
```

Note: All routes use the same Placeholder component for now. Later phases will replace with actual content pages.
  </action>
  <verify>
Start dev server and verify routing:
```bash
cd docs && npm run dev
```
- Navigate to http://localhost:5173/docs - shows "Introduction"
- Click "Button" in sidebar - URL changes to /docs/components/button, shows "Components / Button"
- Active link is highlighted
  </verify>
  <done>
React Router wired with layout route, all navigation paths respond, placeholder shows current route
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete docs foundation with:
- Responsive layout (header + sidebar + content)
- Collapsible navigation sections
- Mobile hamburger menu with slide-in sheet
- React Router navigation with active state
  </what-built>
  <how-to-verify>
1. Start the docs dev server: `cd docs && npm run dev`
2. Open http://localhost:5173/docs in browser

**Desktop verification (window > 768px):**
- [ ] Sidebar visible on left with navigation sections
- [ ] Click "Getting Started" section header - collapses/expands with chevron rotation
- [ ] Click "Button" link - content area shows "Components / Button", link highlighted
- [ ] Header has "lit-ui" title/logo

**Mobile verification (window < 768px or DevTools mobile mode):**
- [ ] Sidebar hidden
- [ ] Hamburger icon visible in header
- [ ] Click hamburger - sheet slides in from left with navigation
- [ ] Click "Dialog" link - sheet closes, content shows "Components / Dialog"
- [ ] Click outside sheet or press ESC - sheet closes

**Style verification:**
- [ ] Fonts match landing page (Inter for text, JetBrains Mono for code)
- [ ] Colors match landing page theme (grayscale palette)
- [ ] No horizontal scrollbar at any breakpoint
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all checkpoints pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 6 success criteria from ROADMAP:
1. Docs app runs at /docs with same visual style as landing page - verified by theme match
2. Sidebar shows collapsible section navigation - verified by collapse/expand behavior
3. Mobile users see hamburger menu that reveals navigation - verified by mobile test
4. Layout adapts correctly from mobile to desktop breakpoints - verified by responsive behavior
</verification>

<success_criteria>
- Navigation between all routes works without full page reload
- Active NavLink styling shows current page
- Collapsible sections animate smoothly
- Mobile sheet opens/closes correctly with animation
- Sheet closes on route change (clicking nav link)
- No layout shift when switching breakpoints
- Visual theme consistent with landing page
</success_criteria>

<output>
After completion, create `.planning/phases/06-docs-foundation/06-03-SUMMARY.md`
</output>
