---
phase: 06-docs-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/package.json
  - docs/vite.config.ts
  - docs/tsconfig.json
  - docs/index.html
  - docs/src/main.tsx
  - docs/src/App.tsx
  - docs/src/index.css
  - docs/src/vite-env.d.ts
autonomous: true

must_haves:
  truths:
    - "Docs app starts with npm run dev without errors"
    - "Docs app displays React content in browser"
    - "Visual theme matches landing page (fonts, colors)"
  artifacts:
    - path: "docs/package.json"
      provides: "Project configuration with React Router and Radix dependencies"
      contains: "react-router"
    - path: "docs/vite.config.ts"
      provides: "Vite + React + Tailwind v4 configuration"
      contains: "@tailwindcss/vite"
    - path: "docs/src/index.css"
      provides: "Theme variables matching landing page"
      contains: "@theme"
    - path: "docs/src/App.tsx"
      provides: "React entry point with BrowserRouter"
      contains: "BrowserRouter"
  key_links:
    - from: "docs/index.html"
      to: "docs/src/main.tsx"
      via: "script module import"
      pattern: "src/main.tsx"
    - from: "docs/src/main.tsx"
      to: "docs/src/App.tsx"
      via: "React render"
      pattern: "import.*App"
---

<objective>
Scaffold the docs application with React, Vite, Tailwind v4, and React Router.

Purpose: Create the foundation for the documentation site as a separate app in /docs folder, matching the landing page stack and visual theme.
Output: A running React app at /docs with theme variables, ready for layout and navigation components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-docs-foundation/06-RESEARCH.md

# Reference for matching patterns
@landing/package.json
@landing/vite.config.ts
@landing/src/index.css
@landing/index.html
@landing/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs package and configuration</name>
  <files>
    docs/package.json
    docs/vite.config.ts
    docs/tsconfig.json
    docs/index.html
  </files>
  <action>
Create the docs folder and configuration files:

1. **docs/package.json**: Copy structure from landing/package.json with these changes:
   - name: "lit-ui-docs"
   - Add dependencies: react-router@^7.12.0, @radix-ui/react-collapsible@^1.1.12, @radix-ui/react-dialog@^1.1.15, lucide-react@^0.469.0
   - Keep all existing deps from landing (react, react-dom, framer-motion)
   - Keep all devDeps from landing (tailwindcss, @tailwindcss/vite, vite, typescript, @vitejs/plugin-react, @types/react, @types/react-dom)

2. **docs/vite.config.ts**: Copy exactly from landing/vite.config.ts (React + Tailwind plugins)

3. **docs/tsconfig.json**: Copy from landing/tsconfig.json

4. **docs/index.html**: Copy from landing/index.html with these changes:
   - title: "lit-ui Docs - Documentation"
   - meta description: "Documentation for lit-ui - beautiful, accessible web components"
   - Keep Google Fonts link (Inter + JetBrains Mono)
  </action>
  <verify>
Files exist with correct content:
- `cat docs/package.json | grep react-router` shows react-router dependency
- `cat docs/vite.config.ts | grep tailwindcss` shows tailwind plugin
  </verify>
  <done>
Configuration files created matching landing page patterns with React Router and Radix UI added
  </done>
</task>

<task type="auto">
  <name>Task 2: Create React app entry point with router</name>
  <files>
    docs/src/main.tsx
    docs/src/App.tsx
    docs/src/index.css
    docs/src/vite-env.d.ts
    docs/public/favicon.svg
  </files>
  <action>
Create the React application entry files:

1. **docs/src/main.tsx**: Standard React 18 entry point
   - Import React, ReactDOM
   - Import index.css
   - Import App
   - Render to #root with StrictMode

2. **docs/src/App.tsx**: Router shell with placeholder
   - Import BrowserRouter from 'react-router' (NOT react-router-dom - v7 uses single package)
   - Set basename="/docs" on BrowserRouter
   - For now, render a simple placeholder div with "lit-ui Documentation" text
   - Apply Tailwind classes: min-h-screen, font-sans

3. **docs/src/index.css**: Copy ENTIRE file from landing/src/index.css
   - This includes @import "tailwindcss", @theme block with all color/font variables
   - Includes all utility classes (grid-pattern, hero-gradient, code-block, etc.)

4. **docs/src/vite-env.d.ts**: Copy from landing/src/vite-env.d.ts
   - Contains: /// <reference types="vite/client" />

5. **docs/public/favicon.svg**: Copy from landing/public/favicon.svg (or symlink)
  </action>
  <verify>
Run from docs folder:
```bash
cd docs && npm install && npm run dev
```
App starts without errors, shows "lit-ui Documentation" at localhost:5173/docs
  </verify>
  <done>
Docs app runs with Vite dev server, displays placeholder content with correct fonts and background color
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd docs && npm run dev` starts without errors
2. Browser at http://localhost:5173/docs shows content
3. Font is Inter (matches landing page)
4. Background is white with correct color variables applied
5. No console errors about missing dependencies
</verification>

<success_criteria>
- Docs app scaffold complete in /docs folder
- Package.json includes react-router, radix-ui/react-collapsible, radix-ui/react-dialog
- Vite dev server starts successfully
- Theme variables match landing page (fonts, colors)
- BrowserRouter configured with /docs basename
</success_criteria>

<output>
After completion, create `.planning/phases/06-docs-foundation/06-01-SUMMARY.md`
</output>
