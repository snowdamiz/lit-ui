---
phase: 31-select-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/styles/tailwind.css
  - packages/core/src/tokens/index.ts
autonomous: true

must_haves:
  truths:
    - "Developer can use --ui-select-* CSS variables in their stylesheets"
    - "Developer can import tokens.select from @lit-ui/core/tokens"
    - "Select tokens follow same naming pattern as input/textarea tokens"
  artifacts:
    - path: "packages/core/src/styles/tailwind.css"
      provides: "Select CSS custom properties"
      contains: "--ui-select-radius"
    - path: "packages/core/src/tokens/index.ts"
      provides: "Select token TypeScript exports"
      contains: "select:"
  key_links:
    - from: "packages/core/src/tokens/index.ts"
      to: "packages/core/src/styles/tailwind.css"
      via: "var() references"
      pattern: "var\\(--ui-select-"
---

<objective>
Add CSS design tokens for the Select component to @lit-ui/core.

Purpose: Establish the foundational styling variables that will be used by the Select component and can be customized by users. This follows the same pattern as Input/Textarea tokens from v4.0.

Output: CSS custom properties (--ui-select-*) in tailwind.css and TypeScript token exports in tokens/index.ts.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-select-infrastructure/31-RESEARCH.md

# Reference files for patterns
@packages/core/src/styles/tailwind.css
@packages/core/src/tokens/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Select CSS tokens to tailwind.css</name>
  <files>packages/core/src/styles/tailwind.css</files>
  <action>
Add Select component CSS custom properties to the :root block in tailwind.css, following the Input/Textarea pattern.

Add after the Textarea Component section (around line 403):

```css
/* -------------------------------------------------------------------------
 * Select Component
 * ------------------------------------------------------------------------- */

/* Layout */
--ui-select-radius: 0.375rem;
--ui-select-border-width: 1px;
--ui-select-transition: 150ms;
--ui-select-dropdown-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
--ui-select-dropdown-max-height: 15rem;
--ui-select-option-height: 2.25rem;
--ui-select-z-index: 50;

/* Typography */
--ui-select-font-size-sm: 0.875rem;
--ui-select-font-size-md: 1rem;
--ui-select-font-size-lg: 1.125rem;

/* Spacing - Small */
--ui-select-padding-x-sm: 0.75rem;
--ui-select-padding-y-sm: 0.375rem;

/* Spacing - Medium */
--ui-select-padding-x-md: 1rem;
--ui-select-padding-y-md: 0.5rem;

/* Spacing - Large */
--ui-select-padding-x-lg: 1.25rem;
--ui-select-padding-y-lg: 0.75rem;

/* Trigger state colors */
--ui-select-bg: var(--color-background, white);
--ui-select-text: var(--color-foreground, var(--ui-color-foreground));
--ui-select-border: var(--color-border, var(--ui-color-border));
--ui-select-placeholder: var(--color-muted-foreground, var(--ui-color-muted-foreground));

/* Focus state */
--ui-select-border-focus: var(--color-ring, var(--ui-color-ring));
--ui-select-ring: var(--color-ring, var(--ui-color-ring));

/* Error state */
--ui-select-border-error: var(--color-destructive, var(--ui-color-destructive));
--ui-select-text-error: var(--color-destructive, var(--ui-color-destructive));

/* Disabled state */
--ui-select-bg-disabled: var(--color-muted, var(--ui-color-muted));
--ui-select-text-disabled: var(--color-muted-foreground, var(--ui-color-muted-foreground));
--ui-select-border-disabled: var(--color-border, var(--ui-color-border));

/* Dropdown colors */
--ui-select-dropdown-bg: var(--color-card, var(--ui-color-card));
--ui-select-dropdown-border: var(--color-border, var(--ui-color-border));

/* Option colors */
--ui-select-option-bg: transparent;
--ui-select-option-bg-hover: var(--color-accent, var(--ui-color-accent));
--ui-select-option-bg-active: var(--color-accent, var(--ui-color-accent));
--ui-select-option-text: var(--color-foreground, var(--ui-color-foreground));
--ui-select-option-text-disabled: var(--color-muted-foreground, var(--ui-color-muted-foreground));
--ui-select-option-check: var(--color-primary, var(--ui-color-primary));
```

Key points:
- Follow exact naming convention: --ui-select-{property}
- Use same fallback pattern as input: var(--color-*, var(--ui-color-*))
- Include all states: default, focus, error, disabled
- Include dropdown-specific tokens for positioning and styling
  </action>
  <verify>
Run `grep -c "ui-select" packages/core/src/styles/tailwind.css` - should show 30+ matches.
  </verify>
  <done>
tailwind.css contains all --ui-select-* CSS custom properties with proper fallback values matching the input/textarea pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Select tokens to tokens/index.ts</name>
  <files>packages/core/src/tokens/index.ts</files>
  <action>
Add a `select` object to the `tokens` constant in packages/core/src/tokens/index.ts, following the input/textarea pattern.

Add after the `textarea` object (before the closing `} as const;`):

```typescript
select: {
  // Layout
  radius: 'var(--ui-select-radius)',
  borderWidth: 'var(--ui-select-border-width)',
  transition: 'var(--ui-select-transition)',
  dropdownShadow: 'var(--ui-select-dropdown-shadow)',
  dropdownMaxHeight: 'var(--ui-select-dropdown-max-height)',
  optionHeight: 'var(--ui-select-option-height)',
  zIndex: 'var(--ui-select-z-index)',

  // Typography
  fontSizeSm: 'var(--ui-select-font-size-sm)',
  fontSizeMd: 'var(--ui-select-font-size-md)',
  fontSizeLg: 'var(--ui-select-font-size-lg)',

  // Spacing
  paddingXSm: 'var(--ui-select-padding-x-sm)',
  paddingYSm: 'var(--ui-select-padding-y-sm)',
  paddingXMd: 'var(--ui-select-padding-x-md)',
  paddingYMd: 'var(--ui-select-padding-y-md)',
  paddingXLg: 'var(--ui-select-padding-x-lg)',
  paddingYLg: 'var(--ui-select-padding-y-lg)',

  // Trigger state
  bg: 'var(--ui-select-bg)',
  text: 'var(--ui-select-text)',
  border: 'var(--ui-select-border)',
  placeholder: 'var(--ui-select-placeholder)',

  // Focus state
  borderFocus: 'var(--ui-select-border-focus)',
  ring: 'var(--ui-select-ring)',

  // Error state
  borderError: 'var(--ui-select-border-error)',
  textError: 'var(--ui-select-text-error)',

  // Disabled state
  bgDisabled: 'var(--ui-select-bg-disabled)',
  textDisabled: 'var(--ui-select-text-disabled)',
  borderDisabled: 'var(--ui-select-border-disabled)',

  // Dropdown
  dropdownBg: 'var(--ui-select-dropdown-bg)',
  dropdownBorder: 'var(--ui-select-dropdown-border)',

  // Option
  optionBg: 'var(--ui-select-option-bg)',
  optionBgHover: 'var(--ui-select-option-bg-hover)',
  optionBgActive: 'var(--ui-select-option-bg-active)',
  optionText: 'var(--ui-select-option-text)',
  optionTextDisabled: 'var(--ui-select-option-text-disabled)',
  optionCheck: 'var(--ui-select-option-check)',
},
```

Also add the type export at the bottom of the file, after `TextareaToken`:

```typescript
export type SelectToken = keyof typeof tokens.select;
```
  </action>
  <verify>
Run `pnpm --filter @lit-ui/core build` - build should succeed with no TypeScript errors.
  </verify>
  <done>
tokens/index.ts exports `tokens.select` object with all token references and `SelectToken` type is exported for TypeScript consumers.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "ui-select" packages/core/src/styles/tailwind.css` returns 30+
2. `grep "select:" packages/core/src/tokens/index.ts` shows select object exists
3. `grep "SelectToken" packages/core/src/tokens/index.ts` shows type export exists
4. `pnpm --filter @lit-ui/core build` completes without errors
</verification>

<success_criteria>
- [ ] 30+ --ui-select-* CSS custom properties defined in tailwind.css
- [ ] tokens.select object exists with all token references
- [ ] SelectToken type is exported
- [ ] @lit-ui/core builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/31-select-infrastructure/31-01-SUMMARY.md`
</output>
