---
phase: 70-button
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/src/pages/components/ButtonPage.tsx
autonomous: true
requirements: [BTN-02]

must_haves:
  truths:
    - "CSS Custom Properties table lists all user-facing --ui-button-* tokens with correct names (no --lui-* prefix)"
    - "CSS example code uses correct --ui-button-* property names"
    - "CSS vars count badge reflects the full set of documented properties"
  artifacts:
    - path: "apps/docs/src/pages/components/ButtonPage.tsx"
      provides: "Accurate Button docs page with correct CSS token names"
      contains: "--ui-button-radius"
  key_links:
    - from: "apps/docs/src/pages/components/ButtonPage.tsx (buttonCSSVars array)"
      to: "packages/core/src/styles/tailwind.css (:root --ui-button-* tokens)"
      via: "CSS property names matching tailwind.css token names"
      pattern: "ui-button-radius"
---

<objective>
Update the Button docs page (ButtonPage.tsx) to reflect the accurate --ui-button-* CSS custom property names.

Purpose: The docs page currently documents --lui-button-radius, --lui-button-shadow, --lui-button-font-weight (old --lui- prefix). The actual CSS tokens are --ui-button-radius, --ui-button-shadow, --ui-button-font-weight. The CSS example code also uses the old --lui-button-radius. This is stale and incorrect.

Output: ButtonPage.tsx CSS Custom Properties table and example code updated to use correct --ui-button-* names and a comprehensive set of documented tokens matching what's in tailwind.css.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-theme-foundation/THEME-SPEC.md

<interfaces>
<!-- Current state of ButtonPage.tsx CSS vars data — these need updating -->

Current buttonCSSVars array (WRONG — old --lui- prefix, only 3 tokens):
```tsx
const buttonCSSVars: CSSVarDef[] = [
  { name: '--lui-button-radius', default: 'var(--radius-md)', description: 'Border radius of the button.' },
  { name: '--lui-button-shadow', default: 'none', description: 'Box shadow of the button.' },
  { name: '--lui-button-font-weight', default: '500', description: 'Font weight of the button text.' },
];
```

Current cssVarsCode (WRONG — old --lui- prefix):
```tsx
const cssVarsCode = `/* Global override - all buttons become pill-shaped */
:root {
  --lui-button-radius: 9999px;
  --lui-button-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

/* Scoped override - only affects buttons in this container */
.card-actions {
  --lui-button-radius: 0.25rem;
  --lui-button-shadow: none;
}`;
```

Current inline demo (WRONG — old --lui- prefix):
```tsx
<style>{`
  .demo-pill-buttons { --lui-button-radius: 9999px; --lui-button-shadow: 0 4px 6px rgba(0,0,0,0.1); }
`}</style>
```

Actual tokens in tailwind.css :root (from THEME-SPEC.md — use these):
Layout: --ui-button-radius (0.375rem), --ui-button-shadow (none), --ui-button-border-width (1px)
Typography: --ui-button-font-weight (500), --ui-button-font-size-sm (0.875rem), --ui-button-font-size-md (1rem), --ui-button-font-size-lg (1.125rem)
Spacing: --ui-button-padding-x-sm/md/lg, --ui-button-padding-y-sm/md/lg, --ui-button-gap-sm/md/lg
Colors: --ui-button-primary-bg, --ui-button-primary-text, --ui-button-secondary-bg, --ui-button-secondary-text, --ui-button-secondary-hover-bg, --ui-button-outline-bg/text/border/hover-bg, --ui-button-ghost-bg/text/hover-bg, --ui-button-destructive-bg/text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS vars data, demo, and example code in ButtonPage.tsx</name>
  <files>apps/docs/src/pages/components/ButtonPage.tsx</files>
  <action>
In apps/docs/src/pages/components/ButtonPage.tsx, make three targeted changes:

**Change 1 — buttonCSSVars array:** Replace the 3-entry array using old --lui-* prefix with a comprehensive set of the user-facing --ui-button-* tokens. Document the layout/structural tokens (the ones users are most likely to override). Keep it readable — document the key structural and layout tokens rather than exhaustively listing all 20+ tokens:

```tsx
const buttonCSSVars: CSSVarDef[] = [
  { name: '--ui-button-radius', default: '0.375rem', description: 'Border radius of the button.' },
  { name: '--ui-button-shadow', default: 'none', description: 'Box shadow of the button.' },
  { name: '--ui-button-border-width', default: '1px', description: 'Border width (used by outline variant).' },
  { name: '--ui-button-font-weight', default: '500', description: 'Font weight of the button text.' },
  { name: '--ui-button-font-size-sm', default: '0.875rem', description: 'Font size for size="sm".' },
  { name: '--ui-button-font-size-md', default: '1rem', description: 'Font size for size="md".' },
  { name: '--ui-button-font-size-lg', default: '1.125rem', description: 'Font size for size="lg".' },
  { name: '--ui-button-padding-x-md', default: '1rem', description: 'Horizontal padding for size="md".' },
  { name: '--ui-button-padding-y-md', default: '0.5rem', description: 'Vertical padding for size="md".' },
  { name: '--ui-button-primary-bg', default: 'var(--color-primary, var(--ui-color-primary))', description: 'Primary variant background color.' },
  { name: '--ui-button-secondary-bg', default: 'var(--color-secondary, var(--ui-color-secondary))', description: 'Secondary variant background color.' },
  { name: '--ui-button-destructive-bg', default: 'var(--color-destructive, var(--ui-color-destructive))', description: 'Destructive variant background color.' },
];
```

**Change 2 — cssVarsCode string:** Update the --lui-button-radius and --lui-button-shadow references to --ui-button-radius and --ui-button-shadow:

```tsx
const cssVarsCode = `/* Global override - all buttons become pill-shaped */
:root {
  --ui-button-radius: 9999px;
  --ui-button-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

/* Scoped override - only affects buttons in this container */
.card-actions {
  --ui-button-radius: 0.25rem;
  --ui-button-shadow: none;
}`;
```

**Change 3 — inline demo style tag:** Update --lui-button-radius and --lui-button-shadow to --ui-button-radius and --ui-button-shadow in the inline style for the .demo-pill-buttons class:

```tsx
<style>{`
  .demo-pill-buttons { --ui-button-radius: 9999px; --ui-button-shadow: 0 4px 6px rgba(0,0,0,0.1); }
`}</style>
```

Do NOT change any other part of ButtonPage.tsx — props, slots, examples, or layout sections are accurate and should remain unchanged.
  </action>
  <verify>
    <automated>grep -n "lui-button" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/ButtonPage.tsx | wc -l</automated>
  </verify>
  <done>Running `grep "lui-button" ButtonPage.tsx` returns 0 results. The buttonCSSVars array has 12 entries using --ui-button-* names. The cssVarsCode string and inline style both use --ui-button-radius and --ui-button-shadow.</done>
</task>

</tasks>

<verification>
1. Run: `grep "lui-button" apps/docs/src/pages/components/ButtonPage.tsx` — should return no matches
2. Run: `grep "ui-button-radius" apps/docs/src/pages/components/ButtonPage.tsx` — should show correct usage
3. Run: `grep -c "ui-button" apps/docs/src/pages/components/ButtonPage.tsx` — should be > 5 (multiple token references)
</verification>

<success_criteria>
- No --lui-button-* references remain in ButtonPage.tsx
- CSS Custom Properties table documents the full set of layout/structural/color tokens using --ui-button-* names
- The CSS example code and inline demo use correct --ui-button-radius and --ui-button-shadow names
- BTN-02 satisfied: Button docs page is accurate and up-to-date
</success_criteria>

<output>
After completion, create `.planning/phases/70-button/70-02-SUMMARY.md` with what changed and why (old --lui- prefix → correct --ui- prefix, expanded token list).
</output>
