---
phase: 82-tooltip
plan: 02
type: execute
wave: 2
depends_on:
  - 82-01
files_modified:
  - apps/docs/src/pages/components/TooltipPage.tsx
autonomous: true
requirements:
  - TTP-02

must_haves:
  truths:
    - "tooltipCSSVars array contains exactly the 10 --ui-tooltip-* tokens declared in tailwind.css :root with correct default values"
    - "The shadow default matches tailwind.css :root exactly: '0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)' (not the stale '0 4px 6px -1px rgb(0 0 0 / 0.1)')"
    - "Color token defaults use double-fallback var() form matching tailwind.css :root exactly"
  artifacts:
    - path: "apps/docs/src/pages/components/TooltipPage.tsx"
      provides: "tooltipCSSVars array with 10 entries matching tailwind.css :root defaults exactly"
      contains: "ui-tooltip-bg"
  key_links:
    - from: "apps/docs/src/pages/components/TooltipPage.tsx"
      to: "packages/core/src/styles/tailwind.css :root block"
      via: "tooltipCSSVars defaults matching tailwind.css :root values"
      pattern: "ui-tooltip-bg.*color-foreground"
---

<objective>
Update tooltipCSSVars in TooltipPage.tsx so every entry's default value matches tailwind.css :root exactly. The array already has the correct 10 token names but two defaults are wrong.

Purpose: The current docs table lists stale default values: `--ui-tooltip-shadow` shows `0 4px 6px -1px rgb(0 0 0 / 0.1)` but tailwind.css :root has a two-layer shadow `0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)`. Also `--ui-tooltip-bg` and `--ui-tooltip-text` show single-fallback `var(--color-foreground)` / `var(--color-background)` instead of the double-fallback form in :root. Developers rely on docs to correctly override the component.

Output: TooltipPage.tsx with an accurate 10-entry tooltipCSSVars array and an updated cssVarsCode example using the actual token name forms.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

The established pattern (Phases 72-02 through 81-02): Replace stale CSS var defaults with exactly the values declared in tailwind.css :root. Color tokens use double-fallback var() form. Structural tokens (shadows, z-index, sizes) use exact literal string values.

**Current tooltipCSSVars in TooltipPage.tsx (10 entries, 2 have wrong defaults):**
- --ui-tooltip-bg: default shown as `var(--color-foreground)` — WRONG (should be double-fallback)
- --ui-tooltip-text: default shown as `var(--color-background)` — WRONG (should be double-fallback)
- --ui-tooltip-shadow: default shown as `0 4px 6px -1px rgb(0 0 0 / 0.1)` — WRONG (stale value)
- All other 7 entries are correct (radius, padding-x, padding-y, font-size, arrow-size, max-width, z-index)

**Correct 10 tokens from tailwind.css :root (lines 765-778):**
1.  --ui-tooltip-bg: var(--color-foreground, var(--ui-color-foreground))
2.  --ui-tooltip-text: var(--color-background, white)
3.  --ui-tooltip-radius: 0.375rem
4.  --ui-tooltip-padding-x: 0.75rem
5.  --ui-tooltip-padding-y: 0.375rem
6.  --ui-tooltip-font-size: 0.875rem
7.  --ui-tooltip-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)
8.  --ui-tooltip-arrow-size: 8px
9.  --ui-tooltip-max-width: 20rem
10. --ui-tooltip-z-index: 50
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tooltipCSSVars defaults to match tailwind.css :root</name>
  <files>apps/docs/src/pages/components/TooltipPage.tsx</files>
  <action>
In TooltipPage.tsx, find the `tooltipCSSVars: CSSVarDef[]` array (10 entries, lines ~87-98) and replace the entire array contents with these corrected 10 entries:

```typescript
const tooltipCSSVars: CSSVarDef[] = [
  { name: '--ui-tooltip-bg', default: 'var(--color-foreground, var(--ui-color-foreground))', description: 'Background color of the tooltip.' },
  { name: '--ui-tooltip-text', default: 'var(--color-background, white)', description: 'Text color of the tooltip.' },
  { name: '--ui-tooltip-radius', default: '0.375rem', description: 'Border radius of the tooltip.' },
  { name: '--ui-tooltip-padding-x', default: '0.75rem', description: 'Horizontal padding of the tooltip.' },
  { name: '--ui-tooltip-padding-y', default: '0.375rem', description: 'Vertical padding of the tooltip.' },
  { name: '--ui-tooltip-font-size', default: '0.875rem', description: 'Font size of the tooltip text.' },
  { name: '--ui-tooltip-shadow', default: '0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)', description: 'Box shadow of the tooltip.' },
  { name: '--ui-tooltip-arrow-size', default: '8px', description: 'Size of the arrow indicator.' },
  { name: '--ui-tooltip-max-width', default: '20rem', description: 'Maximum width of the tooltip.' },
  { name: '--ui-tooltip-z-index', default: '50', description: 'Z-index of the tooltip panel.' },
];
```

Also update the cssVarsCode example string to reference the correct double-fallback form for the bg/text tokens:

```typescript
const cssVarsCode = `/* Global override - affects all tooltips */
:root {
  --ui-tooltip-bg: var(--color-foreground, var(--ui-color-foreground));
  --ui-tooltip-text: var(--color-background, white);
  --ui-tooltip-radius: 0.5rem;
  --ui-tooltip-padding-x: 1rem;
  --ui-tooltip-padding-y: 0.5rem;
}

/* Scoped override - only affects tooltips in this container */
.custom-tooltips {
  --ui-tooltip-font-size: 0.75rem;
  --ui-tooltip-max-width: 16rem;
}`;
```

Commit: `feat(82-02): fix tooltipCSSVars defaults to match tailwind.css :root (shadow, bg/text double-fallback)`
  </action>
  <verify>
    <automated>grep "ui-tooltip-shadow" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/TooltipPage.tsx</automated>
  </verify>
  <done>
grep for "ui-tooltip-shadow" shows the corrected two-layer shadow value `0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)` — the stale `0 4px 6px -1px` value is gone. The --ui-tooltip-bg default is `var(--color-foreground, var(--ui-color-foreground))` (double-fallback form).
  </done>
</task>

</tasks>

<verification>
1. `grep "ui-tooltip-shadow" apps/docs/src/pages/components/TooltipPage.tsx` — shows corrected shadow (no `4px 6px` in output)
2. `grep "ui-tooltip-bg" apps/docs/src/pages/components/TooltipPage.tsx` — shows `var(--color-foreground, var(--ui-color-foreground))` (double-fallback)
3. `grep "ui-tooltip-text" apps/docs/src/pages/components/TooltipPage.tsx` — shows `var(--color-background, white)` (double-fallback)
4. `grep -c "ui-tooltip" apps/docs/src/pages/components/TooltipPage.tsx` — returns 10+ (10 in array + references in cssVarsCode)
</verification>

<success_criteria>
- tooltipCSSVars array has exactly 10 entries
- --ui-tooltip-bg default is `var(--color-foreground, var(--ui-color-foreground))`
- --ui-tooltip-text default is `var(--color-background, white)`
- --ui-tooltip-shadow default is `0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)` (two-layer)
- All other 7 token defaults are unchanged (they were already correct)
- cssVarsCode example updated to use double-fallback bg/text forms
</success_criteria>

<output>
After completion, create `.planning/phases/82-tooltip/82-02-SUMMARY.md`
</output>
