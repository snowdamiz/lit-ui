---
phase: 82-tooltip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/styles/tailwind.css
autonomous: true
requirements:
  - TTP-01

must_haves:
  truths:
    - "Tooltip dark mode is governed entirely by the semantic .dark token cascade — the two hardcoded .dark --ui-tooltip-* var(--color-gray-*) declarations are removed"
    - "The :root block with 10 --ui-tooltip-* tokens remains untouched"
    - "No --ui-tooltip-* declarations exist in the .dark block after cleanup"
  artifacts:
    - path: "packages/core/src/styles/tailwind.css"
      provides: "Tooltip .dark block removed (2 var(--color-gray-*) declarations); :root 10-token block unchanged"
      contains: "ui-tooltip-bg"
  key_links:
    - from: "packages/core/src/styles/tailwind.css .dark block"
      to: "semantic .dark cascade via --color-foreground, --color-background"
      via: "double-fallback var() in :root"
      pattern: "--ui-tooltip-bg: var\\(--color-foreground"
---

<objective>
Remove the 2 hardcoded .dark --ui-tooltip-* declarations from tailwind.css, following the established pattern from Phases 70-01 through 81-01.

Purpose: The tooltip's :root tokens already use double-fallback var() form — `--ui-tooltip-bg: var(--color-foreground, ...)` and `--ui-tooltip-text: var(--color-background, white)`. In dark mode, `--color-foreground` resolves to `var(--color-gray-50)` and `--color-background` resolves to `var(--color-gray-950)` via the semantic .dark cascade, which is exactly what the two redundant .dark overrides set. The .dark overrides are unnecessary.

Output: tailwind.css .dark block no longer contains any --ui-tooltip-* declarations. The :root 10-token block is unchanged.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

The established pattern (Phases 70-01 through 81-01): remove all hardcoded .dark component token overrides where the :root tokens already use double-fallback var() pointing to semantic tokens. The semantic .dark cascade handles the rest via:
  .dark { --color-background: var(--color-gray-950) }
  .dark { --color-foreground: var(--color-gray-50) }

**Current .dark block (lines 206-208 in tailwind.css):**
  /* Tooltip dark mode - inverted stays inverted, just swap values */
  --ui-tooltip-bg: var(--color-gray-50);
  --ui-tooltip-text: var(--color-gray-950);

**Why these are redundant:**
- :root has `--ui-tooltip-bg: var(--color-foreground, var(--ui-color-foreground))` — in .dark, --color-foreground = var(--color-gray-50). Result is identical to the hardcoded override.
- :root has `--ui-tooltip-text: var(--color-background, white)` — in .dark, --color-background = var(--color-gray-950). Result is identical to the hardcoded override.

**No exceptions required:** Unlike checkbox check-color (white literal) or switch thumb-bg (white literal) or time-picker oklch colors, these tooltip .dark values are NOT literals with different light/dark values. They are semantic var() references that cascade correctly.

**Current :root block (lines 765-778 in tailwind.css) — DO NOT TOUCH:**
  --ui-tooltip-bg: var(--color-foreground, var(--ui-color-foreground));
  --ui-tooltip-text: var(--color-background, white);
  --ui-tooltip-radius: 0.375rem;
  --ui-tooltip-padding-x: 0.75rem;
  --ui-tooltip-padding-y: 0.375rem;
  --ui-tooltip-font-size: 0.875rem;
  --ui-tooltip-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
  --ui-tooltip-arrow-size: 8px;
  --ui-tooltip-max-width: 20rem;
  --ui-tooltip-z-index: 50;
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove .dark tooltip block (2 declarations + comment)</name>
  <files>packages/core/src/styles/tailwind.css</files>
  <action>
In the .dark { } block of tailwind.css, find the "/* Tooltip dark mode - inverted stays inverted, just swap values */" comment (approximately line 206) and the two --ui-tooltip-* declarations that follow it.

Remove these 3 lines entirely:
  - /* Tooltip dark mode - inverted stays inverted, just swap values */
  - --ui-tooltip-bg: var(--color-gray-50);
  - --ui-tooltip-text: var(--color-gray-950);

Do NOT touch the :root block (approximately lines 765-778). The 10 --ui-tooltip-* tokens in :root remain unchanged.

Do NOT add any replacement comment — no tooltip-related lines should remain in .dark.

Commit: `feat(82-01): remove .dark tooltip block (2 gray declarations cascade via semantic tokens)`
  </action>
  <verify>
    <automated>grep -c "ui-tooltip" /Users/sn0w/Documents/dev/lit-components/packages/core/src/styles/tailwind.css</automated>
  </verify>
  <done>
grep shows exactly 10 matches for "ui-tooltip" in tailwind.css — all in :root. The 2 removed .dark declarations are gone. No --ui-tooltip-* lines remain in the .dark block.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "ui-tooltip" packages/core/src/styles/tailwind.css` — all 10 matches are in the :root block; none in .dark
2. `grep -n "\.dark" packages/core/src/styles/tailwind.css | grep "tooltip"` — returns no output (no .dark tooltip overrides remain)
3. `grep "ui-tooltip-bg\|ui-tooltip-text" packages/core/src/styles/tailwind.css` — shows exactly 2 entries, both in :root
</verification>

<success_criteria>
- .dark block no longer contains "/* Tooltip dark mode */" comment
- .dark block no longer contains --ui-tooltip-bg or --ui-tooltip-text declarations
- :root block has exactly 10 --ui-tooltip-* tokens, all unchanged
- Pattern matches all prior phases (70-01 through 81-01)
</success_criteria>

<output>
After completion, create `.planning/phases/82-tooltip/82-01-SUMMARY.md`
</output>
