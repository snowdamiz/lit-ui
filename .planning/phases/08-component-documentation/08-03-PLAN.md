---
phase: 08-component-documentation
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - docs/src/lib/ui-dialog/dialog.ts
  - docs/src/lib/ui-dialog/tailwind-element.ts
  - docs/src/lib/ui-dialog/index.ts
  - docs/src/pages/components/ButtonPage.tsx
  - docs/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User can view Button documentation at /components/button"
    - "Button documentation shows live examples for all variants and sizes"
    - "Button API reference documents all props, slots, and events"
    - "User can navigate to Dialog documentation via next link"
  artifacts:
    - path: "docs/src/lib/ui-dialog/dialog.ts"
      provides: "Dialog component copy for docs preview"
      contains: "ui-dialog"
    - path: "docs/src/pages/components/ButtonPage.tsx"
      provides: "Complete Button documentation page"
      exports: ["ButtonPage"]
    - path: "docs/src/App.tsx"
      provides: "Route for /components/button"
      contains: "ButtonPage"
  key_links:
    - from: "docs/src/pages/components/ButtonPage.tsx"
      to: "docs/src/components/ExampleBlock.tsx"
      via: "import ExampleBlock"
      pattern: "import.*ExampleBlock"
    - from: "docs/src/pages/components/ButtonPage.tsx"
      to: "docs/src/contexts/FrameworkContext.tsx"
      via: "FrameworkProvider wrapper"
      pattern: "FrameworkProvider"
    - from: "docs/src/App.tsx"
      to: "docs/src/pages/components/ButtonPage.tsx"
      via: "Route element"
      pattern: "ButtonPage"
---

<objective>
Create the Button documentation page with live examples, API reference, and set up Dialog component for preview. Wire routing for /components/button.

Purpose: Users can learn Button component usage through examples and reference documentation.
Output: Complete Button documentation page at /components/button with all variants, sizes, states, and API reference.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-component-documentation/08-RESEARCH.md
@.planning/phases/08-component-documentation/08-01-SUMMARY.md
@.planning/phases/08-component-documentation/08-02-SUMMARY.md

# Source components for accurate API docs
@src/components/button/button.ts
@src/components/dialog/dialog.ts

# Existing docs infrastructure
@docs/src/lib/ui-button/button.ts
@docs/src/lib/ui-button/tailwind-element.ts
@docs/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Dialog component to docs lib for preview</name>
  <files>docs/src/lib/ui-dialog/dialog.ts, docs/src/lib/ui-dialog/tailwind-element.ts, docs/src/lib/ui-dialog/index.ts</files>
  <action>
Set up ui-dialog component in docs similar to how ui-button was set up in Phase 7.

1. Create docs/src/lib/ui-dialog/ directory

2. Copy docs/src/lib/ui-button/tailwind-element.ts to docs/src/lib/ui-dialog/tailwind-element.ts (same base class)

3. Create docs/src/lib/ui-dialog/dialog.ts:
   - Copy from src/components/dialog/dialog.ts
   - Update import for TailwindElement to use local './tailwind-element'
   - Keep all functionality intact

4. Create docs/src/lib/ui-dialog/index.ts:
   - Export Dialog class
   - Side-effect registration

5. Add CSS custom properties for dialog theming in docs/src/styles/dialog-preview.css:
```css
ui-dialog {
  --color-card: theme('colors.white');
  --color-card-foreground: theme('colors.gray.900');
  --color-muted-foreground: theme('colors.gray.600');
}
```

6. Import dialog-preview.css in docs/src/main.tsx (alongside button-preview.css)

This follows the same pattern established in Phase 7 for ui-button.
  </action>
  <verify>Files exist. Import ui-dialog in a test file to verify custom element registers without errors</verify>
  <done>Dialog component available in docs for live previews with proper theming</done>
</task>

<task type="auto">
  <name>Task 2: Create ButtonPage documentation</name>
  <files>docs/src/pages/components/ButtonPage.tsx</files>
  <action>
Create the Button documentation page with all required sections.

Page structure:
1. Wrap entire page in FrameworkProvider
2. Header with title "Button" and description
3. Hero example (basic primary button)
4. Examples section with:
   - Variants (primary, secondary, outline, ghost, destructive) - grid layout
   - Sizes (sm, md, lg)
   - Loading state
   - Icon buttons (with placeholder icons using lucide-react)
5. API Reference section with:
   - Props table (variant, size, type, disabled, loading)
   - Slots table (default, icon-start, icon-end)
   - Note: Button has no custom events
6. PrevNextNav (prev: Getting Started, next: Dialog)

Import existing ui-button from '../lib/ui-button' (side-effect import already exists).

For JSX typing, add ui-button to global JSX.IntrinsicElements if not already (check LivePreview.tsx for pattern).

Button props data (from source):
```tsx
const buttonProps: PropDef[] = [
  { name: 'variant', type: '"primary" | "secondary" | "outline" | "ghost" | "destructive"', default: '"primary"', description: 'The visual style of the button.' },
  { name: 'size', type: '"sm" | "md" | "lg"', default: '"md"', description: 'The size affecting padding and font size.' },
  { name: 'type', type: '"button" | "submit" | "reset"', default: '"button"', description: 'The button type for form behavior.' },
  { name: 'disabled', type: 'boolean', default: 'false', description: 'Whether the button is disabled. Uses aria-disabled for accessibility.' },
  { name: 'loading', type: 'boolean', default: 'false', description: 'Shows a pulsing dots spinner and prevents interaction.' },
];

const buttonSlots: SlotDef[] = [
  { name: '(default)', description: 'Button text content.' },
  { name: 'icon-start', description: 'Icon placed before the text. Scales with button font-size.' },
  { name: 'icon-end', description: 'Icon placed after the text. Scales with button font-size.' },
];
```

Code examples - since web components use same syntax in all frameworks:
```tsx
// Variants example
const variantsCode = `<ui-button variant="primary">Primary</ui-button>
<ui-button variant="secondary">Secondary</ui-button>
<ui-button variant="outline">Outline</ui-button>
<ui-button variant="ghost">Ghost</ui-button>
<ui-button variant="destructive">Destructive</ui-button>`;
```

For each example, use ExampleBlock with preview containing actual ui-button elements.

Create directory docs/src/pages/components/ if it doesn't exist.
  </action>
  <verify>TypeScript compiles. Page renders with all sections visible</verify>
  <done>Button documentation page complete with examples and API reference</done>
</task>

<task type="auto">
  <name>Task 3: Update routing for Button page</name>
  <files>docs/src/App.tsx</files>
  <action>
Update App.tsx to use the new ButtonPage instead of Placeholder.

1. Import ButtonPage:
```tsx
import { ButtonPage } from './pages/components/ButtonPage'
```

2. Replace the Button route:
```tsx
{/* Before */}
<Route path="components/button" element={<Placeholder />} />

{/* After */}
<Route path="components/button" element={<ButtonPage />} />
```

Keep all other routes as Placeholder for now.
  </action>
  <verify>`cd docs && npm run dev` - Navigate to /components/button shows the Button documentation page</verify>
  <done>Button documentation accessible at /components/button with proper routing</done>
</task>

</tasks>

<verification>
1. `cd docs && npm run build` - TypeScript compiles without errors
2. `cd docs && npm run dev` - Dev server starts
3. Navigate to /components/button - Button documentation page renders
4. All five variants visible in examples section
5. All three sizes visible
6. Loading state example works (shows spinner)
7. API reference tables render props and slots
8. PrevNextNav shows "Getting Started" as prev and "Dialog" as next
9. Getting Started page still works (no regression)
</verification>

<success_criteria>
- Dialog component set up in docs/src/lib/ui-dialog/
- ButtonPage renders at /components/button
- All button variants shown in live examples
- All button sizes shown in live examples
- Loading state example functional
- Props table shows 5 props with correct details
- Slots table shows 3 slots
- PrevNextNav links to Getting Started and Dialog
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-component-documentation/08-03-SUMMARY.md`
</output>
