---
phase: 08-component-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/components/PropsTable.tsx
  - docs/src/components/SlotsTable.tsx
  - docs/src/components/EventsTable.tsx
  - docs/src/components/PrevNextNav.tsx
autonomous: true

must_haves:
  truths:
    - "Props are displayed in a scannable table format with name, type, default, description"
    - "Slots are displayed in a table with name and description"
    - "Events are displayed with name, detail type, and description"
    - "Users can navigate between documentation pages via prev/next links"
  artifacts:
    - path: "docs/src/components/PropsTable.tsx"
      provides: "Props documentation table component"
      exports: ["PropsTable", "PropDef"]
    - path: "docs/src/components/SlotsTable.tsx"
      provides: "Slots documentation table component"
      exports: ["SlotsTable", "SlotDef"]
    - path: "docs/src/components/EventsTable.tsx"
      provides: "Events documentation table component"
      exports: ["EventsTable", "EventDef"]
    - path: "docs/src/components/PrevNextNav.tsx"
      provides: "Previous/next navigation component"
      exports: ["PrevNextNav"]
  key_links:
    - from: "docs/src/components/PrevNextNav.tsx"
      to: "react-router"
      via: "Link component"
      pattern: "import.*Link.*from.*react-router"
---

<objective>
Create API reference table components (PropsTable, SlotsTable, EventsTable) and bottom navigation (PrevNextNav) for component documentation pages.

Purpose: Provide consistent, scannable API documentation format and easy navigation between docs pages.
Output: Four reusable components for documenting component APIs and navigating documentation.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-component-documentation/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PropsTable component</name>
  <files>docs/src/components/PropsTable.tsx</files>
  <action>
Create a styled table component for documenting component props.

Interface:
```tsx
export interface PropDef {
  name: string;
  type: string;
  default?: string;
  description: string;
}

interface PropsTableProps {
  props: PropDef[];
}
```

Table structure:
- Header row: Prop | Type | Default | Description
- Body rows: Map over props array
- Styling:
  - overflow-x-auto wrapper for mobile scrolling
  - w-full table
  - text-sm for readability
  - border-b border-gray-200 for header
  - border-b border-gray-100 for body rows
  - py-3 px-4 cell padding
  - font-mono for prop name and type
  - text-primary color for prop names (use CSS variable or gray-900)
  - text-gray-600 for types
  - text-gray-500 for defaults
  - text-gray-700 for descriptions
  - Default shows '—' when undefined

Export both PropDef interface and PropsTable component.
  </action>
  <verify>TypeScript compiles. Component renders correct table structure with proper styling</verify>
  <done>PropsTable displays props in a scannable, well-styled table format</done>
</task>

<task type="auto">
  <name>Task 2: Create SlotsTable and EventsTable components</name>
  <files>docs/src/components/SlotsTable.tsx, docs/src/components/EventsTable.tsx</files>
  <action>
Create two table components following the same pattern as PropsTable.

**SlotsTable** (docs/src/components/SlotsTable.tsx):
```tsx
export interface SlotDef {
  name: string;
  description: string;
}

interface SlotsTableProps {
  slots: SlotDef[];
}
```
- Header: Slot | Description
- Two columns only
- Same styling pattern as PropsTable
- Slot names in font-mono

**EventsTable** (docs/src/components/EventsTable.tsx):
```tsx
export interface EventDef {
  name: string;
  detail: string;  // TypeScript type string for event detail
  description: string;
}

interface EventsTableProps {
  events: EventDef[];
}
```
- Header: Event | Detail | Description
- Three columns
- Same styling pattern as PropsTable
- Event names and detail types in font-mono

Export interfaces and components from each file.
  </action>
  <verify>TypeScript compiles. Both components render correct table structures</verify>
  <done>SlotsTable and EventsTable provide consistent documentation format for component slots and events</done>
</task>

<task type="auto">
  <name>Task 3: Create PrevNextNav component</name>
  <files>docs/src/components/PrevNextNav.tsx</files>
  <action>
Create a bottom navigation component for moving between documentation pages.

```tsx
import { Link } from 'react-router';
import { ChevronLeft, ChevronRight } from 'lucide-react';

interface NavLink {
  title: string;
  href: string;
}

interface PrevNextNavProps {
  prev?: NavLink;
  next?: NavLink;
}

export function PrevNextNav({ prev, next }: PrevNextNavProps) {
  return (
    <nav className="mt-16 pt-8 border-t border-gray-200 flex justify-between">
      {prev ? (
        <Link
          to={prev.href}
          className="flex items-center gap-2 text-gray-600 hover:text-gray-900 transition-colors"
        >
          <ChevronLeft className="w-4 h-4" />
          <div>
            <div className="text-sm text-gray-500">Previous</div>
            <div className="font-medium">{prev.title}</div>
          </div>
        </Link>
      ) : (
        <div /> {/* Empty div for flex spacing */}
      )}

      {next ? (
        <Link
          to={next.href}
          className="flex items-center gap-2 text-gray-600 hover:text-gray-900 transition-colors text-right"
        >
          <div>
            <div className="text-sm text-gray-500">Next</div>
            <div className="font-medium">{next.title}</div>
          </div>
          <ChevronRight className="w-4 h-4" />
        </Link>
      ) : (
        <div />
      )}
    </nav>
  );
}
```

Features:
- Uses react-router Link for client-side navigation
- Lucide icons for arrows
- Flex layout with justify-between
- Border-top separator
- "Previous" and "Next" labels above titles
- Both props optional (renders empty div for spacing if missing)
  </action>
  <verify>TypeScript compiles. Component renders prev/next links correctly with proper alignment</verify>
  <done>PrevNextNav enables easy navigation between documentation pages with consistent styling</done>
</task>

</tasks>

<verification>
1. `cd docs && npm run build` - TypeScript compiles without errors
2. All four files exist with correct exports:
   - PropsTable.tsx exports PropsTable, PropDef
   - SlotsTable.tsx exports SlotsTable, SlotDef
   - EventsTable.tsx exports EventsTable, EventDef
   - PrevNextNav.tsx exports PrevNextNav
3. Tables render correct column headers and data mapping
</verification>

<success_criteria>
- PropsTable renders 4-column table (Prop, Type, Default, Description)
- SlotsTable renders 2-column table (Slot, Description)
- EventsTable renders 3-column table (Event, Detail, Description)
- PrevNextNav renders navigation links with proper alignment
- All components use consistent styling (borders, padding, typography)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-component-documentation/08-02-SUMMARY.md`
</output>
