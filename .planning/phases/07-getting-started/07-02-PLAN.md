---
phase: 07-getting-started
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - docs/src/lib/ui-button/button.ts
  - docs/src/lib/ui-button/tailwind-element.ts
  - docs/src/lib/ui-button/index.ts
  - docs/src/styles/button-preview.css
  - docs/src/components/LivePreview.tsx
  - docs/src/pages/GettingStarted.tsx
  - docs/src/App.tsx
  - docs/src/nav.ts
autonomous: true

must_haves:
  truths:
    - "User finds installation instructions for npx lit-ui init"
    - "User can follow quick start to add first component"
    - "User understands project structure after running init"
    - "Live preview shows actual rendered Button component"
  artifacts:
    - path: "docs/src/pages/GettingStarted.tsx"
      provides: "Complete getting started documentation page"
      exports: ["GettingStarted"]
    - path: "docs/src/components/LivePreview.tsx"
      provides: "Live preview container for web components"
      exports: ["LivePreview"]
    - path: "docs/src/lib/ui-button/button.ts"
      provides: "Button component for live preview"
      contains: "@customElement('ui-button')"
  key_links:
    - from: "docs/src/pages/GettingStarted.tsx"
      to: "docs/src/components/CodeBlock.tsx"
      via: "CodeBlock import"
      pattern: "import.*CodeBlock.*from"
    - from: "docs/src/pages/GettingStarted.tsx"
      to: "docs/src/components/FrameworkTabs.tsx"
      via: "FrameworkTabs import"
      pattern: "import.*FrameworkTabs.*from"
    - from: "docs/src/pages/GettingStarted.tsx"
      to: "docs/src/components/LivePreview.tsx"
      via: "LivePreview import"
      pattern: "import.*LivePreview.*from"
    - from: "docs/src/App.tsx"
      to: "docs/src/pages/GettingStarted.tsx"
      via: "Route element"
      pattern: "element=.*GettingStarted"
---

<objective>
Create the complete Getting Started documentation page with installation instructions, quick start guide, project structure explanation, live Button preview, and navigation wiring.

Purpose: This is the primary onboarding experience for new users. They should be able to go from zero to using their first lit-ui component by following this single page.

Output: A fully functional Getting Started page accessible at /getting-started with live component preview.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-getting-started/07-CONTEXT.md
@.planning/phases/07-getting-started/07-RESEARCH.md

# Components from Plan 01
@docs/src/components/CodeBlock.tsx
@docs/src/components/FrameworkTabs.tsx

# Existing structure
@docs/src/App.tsx
@docs/src/nav.ts
@docs/src/index.css

# Button component source (for copying)
@src/components/button/button.ts
@src/base/tailwind-element.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Button component for live preview</name>
  <files>
    docs/src/lib/ui-button/button.ts
    docs/src/lib/ui-button/tailwind-element.ts
    docs/src/lib/ui-button/index.ts
    docs/src/styles/button-preview.css
    docs/src/main.tsx
  </files>
  <action>
1. Create docs/src/lib/ui-button/ directory

2. Create a simplified TailwindElement base class at docs/src/lib/ui-button/tailwind-element.ts:
   - This is a standalone version for the docs preview (no external dependencies)
   - Import LitElement and CSSResultGroup from 'lit'
   - Create minimal Tailwind styles inline (just what the button needs):
     - Color tokens: --color-primary, --color-primary-foreground, etc.
     - Basic utilities for button: bg-*, text-*, px-*, py-*, rounded-*, etc.
   - Export TailwindElement class that adopts these styles

3. Copy the button component to docs/src/lib/ui-button/button.ts:
   - Copy from src/components/button/button.ts
   - Change import path: `import { TailwindElement } from './tailwind-element'`
   - Keep all button functionality intact

4. Create docs/src/lib/ui-button/index.ts:
   - Export Button class and type aliases
   - Side-effect: registers 'ui-button' custom element

5. Create docs/src/styles/button-preview.css with minimal Tailwind-like styles for the button:
   ```css
   /* Minimal styles for ui-button preview in docs */
   ui-button {
     --color-primary: oklch(0.55 0.15 250);
     --color-primary-foreground: white;
     --color-secondary: oklch(0.95 0 0);
     --color-secondary-foreground: oklch(0.20 0 0);
     --color-destructive: oklch(0.55 0.20 25);
     --color-destructive-foreground: white;
     --color-accent: oklch(0.95 0 0);
     --color-foreground: oklch(0.15 0 0);
     --color-border: oklch(0.85 0 0);
     --color-ring: oklch(0.55 0.15 250);
   }
   ```

6. Import the button-preview.css in docs/src/main.tsx:
   ```tsx
   import './styles/button-preview.css'
   ```

Note: The button uses Shadow DOM with inline styles, so it mostly self-contains. The CSS variables just need to be available in the light DOM for the component to read.
  </action>
  <verify>
    - Directory exists: docs/src/lib/ui-button/
    - Files exist: button.ts, tailwind-element.ts, index.ts
    - CSS exists: docs/src/styles/button-preview.css
    - TypeScript compiles: `cd docs && npx tsc --noEmit`
  </verify>
  <done>Button component is set up in docs with self-contained styles, ready for live preview import.</done>
</task>

<task type="auto">
  <name>Task 2: Create LivePreview and GettingStarted page</name>
  <files>
    docs/src/components/LivePreview.tsx
    docs/src/pages/GettingStarted.tsx
  </files>
  <action>
1. Create docs/src/components/LivePreview.tsx:
   - Import the button: `import '../lib/ui-button'` (side-effect import registers element)
   - Add TypeScript JSX declaration for ui-button (see RESEARCH.md Pattern 3)
   - Render a preview container with:
     - p-6 bg-gray-50 rounded-lg border border-gray-200
     - Label: "Result:" in text-sm text-gray-600 mb-4
     - The actual `<ui-button variant="primary">Click me</ui-button>` element
   - Export LivePreview component

2. Create docs/src/pages/GettingStarted.tsx with the full page content:

Structure (single scrollable page per CONTEXT.md decisions):

**Header section:**
- h1: "Getting Started"
- Lead paragraph: "Get up and running in 2 minutes."

**Section 1: Installation** (id="installation", class="scroll-mt-20")
- h2: "Installation"
- Prose explaining: "Initialize lit-ui in your existing project. This works with React, Vue, Svelte, or any framework."
- CodeBlock with bash:
  ```
  npx lit-ui init
  ```
- Paragraph explaining what init does (friendly tone):
  - Creates `lit-ui.json` config file
  - Sets up `src/lib/lit-ui/` with base files (TailwindElement, theme config)
  - Creates `src/components/ui/` directory for your components
- Small note: "Using yarn or pnpm? Just use `yarn dlx lit-ui init` or `pnpm dlx lit-ui init` instead."

**Section 2: Quick Start** (id="quick-start", class="scroll-mt-20")
- h2: "Add Your First Component"
- Prose: "Now let's add your first component. We'll start with the Button."
- CodeBlock with bash:
  ```
  npx lit-ui add button
  ```
- Prose: "This copies the Button component to your project. Now you can use it in your app:"
- FrameworkTabs with React/Vue/Svelte usage examples:

  React:
  ```tsx
  import '../components/ui/button'

  function App() {
    return <ui-button variant="primary">Click me</ui-button>
  }
  ```

  Vue:
  ```vue
  <script setup>
  import '../components/ui/button'
  </script>

  <template>
    <ui-button variant="primary">Click me</ui-button>
  </template>
  ```

  Svelte:
  ```svelte
  <script>
    import '../components/ui/button'
  </script>

  <ui-button variant="primary">Click me</ui-button>
  ```

- LivePreview component showing the rendered button
- Prose: "That's it! The button is a native web component, so it works in any framework."

**Section 3: Project Structure** (id="project-structure", class="scroll-mt-20")
- h2: "Project Structure"
- Prose: "After running init and adding a component, your project will have:"
- CodeBlock with plaintext showing file tree:
  ```
  your-project/
  ├── lit-ui.json           # Configuration file
  └── src/
      ├── lib/
      │   └── lit-ui/
      │       ├── tailwind-element.ts   # Base class for components
      │       ├── tailwind.css          # Theme configuration
      │       └── host-defaults.css     # Shadow DOM CSS workaround
      └── components/
          └── ui/
              └── button.ts             # Your Button component
  ```
- Brief descriptions (use dl/dt/dd or styled divs):
  - lit-ui.json: Stores your component path preferences
  - tailwind-element.ts: Base class that injects Tailwind into Shadow DOM
  - tailwind.css: Your theme tokens (colors, spacing, etc.)
  - button.ts: The actual component - you own this code, customize freely

**Section 4: What's Next** (id="whats-next", class="scroll-mt-20")
- h2: "What's Next?"
- Grid of 3 cards (link placeholders for now since pages don't exist):
  - "Components" - Explore Button, Dialog, and more
  - "Theming" - Customize colors and design tokens
  - "Framework Guides" - React, Vue, and Svelte integration tips

Use prose max-w-none for content width. Style headings, paragraphs, code appropriately.
Maintain friendly, explanatory tone per CONTEXT.md (explains why, not just what).
  </action>
  <verify>
    - File exists: docs/src/components/LivePreview.tsx
    - File exists: docs/src/pages/GettingStarted.tsx
    - TypeScript compiles: `cd docs && npx tsc --noEmit`
  </verify>
  <done>GettingStarted page has all four sections with CodeBlock, FrameworkTabs, and LivePreview integration. Content is friendly and explanatory.</done>
</task>

<task type="auto">
  <name>Task 3: Wire routes and update navigation</name>
  <files>
    docs/src/App.tsx
    docs/src/nav.ts
  </files>
  <action>
1. Update docs/src/nav.ts:
   - Change "Getting Started" section to have single item:
     ```ts
     {
       title: "Getting Started",
       items: [
         { title: "Getting Started", href: "/getting-started" },
       ],
     },
     ```
   - Remove the separate "Introduction", "Installation", "Quick Start" items
   - Keep Components and Guides sections as-is

2. Update docs/src/App.tsx:
   - Import GettingStarted: `import { GettingStarted } from './pages/GettingStarted'`
   - Update routes:
     - Change index route to redirect to /getting-started OR make index show GettingStarted
     - Add route: `<Route path="getting-started" element={<GettingStarted />} />`
     - Remove old /installation and /quick-start routes (they'll be handled by catch-all)
   - Keep other routes (components/*, guides/*) with Placeholder

3. Decision: For index route (/), show GettingStarted directly. This makes sense as the docs landing page should be the getting started guide.
  </action>
  <verify>
    - Dev server starts: `cd docs && npm run dev`
    - Navigate to http://localhost:5173/ shows Getting Started page
    - Navigate to http://localhost:5173/getting-started shows same page
    - Sidebar shows single "Getting Started" link in first section
    - Click Getting Started in sidebar navigates correctly
  </verify>
  <done>Navigation updated with single Getting Started link. Routes wired so / and /getting-started both show the Getting Started page.</done>
</task>

</tasks>

<verification>
1. `cd docs && npm run dev` starts without errors
2. http://localhost:5173/ shows Getting Started page with all sections
3. Code blocks have syntax highlighting (colored tokens visible)
4. Copy button shows checkmark when clicked
5. Framework tabs switch between React/Vue/Svelte code
6. Live preview shows actual rendered Button (blue, clickable)
7. Sidebar navigation works correctly
</verification>

<success_criteria>
- User can visit / and see complete Getting Started documentation
- Installation section explains `npx lit-ui init` and what it creates
- Quick Start section shows how to add and use Button component
- Framework tabs work for React, Vue, Svelte examples
- Live preview renders actual ui-button web component
- Project structure section explains file organization
- What's Next section links to future content areas
- Navigation reflects new structure
</success_criteria>

<output>
After completion, create `.planning/phases/07-getting-started/07-02-SUMMARY.md`
</output>
