---
phase: 07-getting-started
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/package.json
  - docs/src/components/CodeBlock.tsx
  - docs/src/components/FrameworkTabs.tsx
autonomous: true

must_haves:
  truths:
    - "Code blocks display syntax highlighting with colored tokens"
    - "Copy button shows checkmark feedback after copying"
    - "Framework tabs switch between React, Vue, Svelte code"
  artifacts:
    - path: "docs/src/components/CodeBlock.tsx"
      provides: "Syntax-highlighted code block with copy button"
      exports: ["CodeBlock"]
    - path: "docs/src/components/FrameworkTabs.tsx"
      provides: "Tab switcher for framework-specific code"
      exports: ["FrameworkTabs"]
  key_links:
    - from: "docs/src/components/CodeBlock.tsx"
      to: "prism-react-renderer"
      via: "Highlight component import"
      pattern: "import.*from 'prism-react-renderer'"
    - from: "docs/src/components/FrameworkTabs.tsx"
      to: "docs/src/components/CodeBlock.tsx"
      via: "CodeBlock import"
      pattern: "import.*CodeBlock.*from"
---

<objective>
Create reusable documentation components for code display: CodeBlock with syntax highlighting and copy button, FrameworkTabs for React/Vue/Svelte code switching.

Purpose: These components are the foundation for all code examples in the documentation. They need to be built first so the Getting Started page can use them.

Output: Two React components ready for use in documentation pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-getting-started/07-RESEARCH.md

# Existing docs structure
@docs/src/components/Sidebar.tsx
@docs/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install prism-react-renderer and create CodeBlock component</name>
  <files>
    docs/package.json
    docs/src/components/CodeBlock.tsx
  </files>
  <action>
1. Install prism-react-renderer in the docs directory:
   ```bash
   cd docs && npm install prism-react-renderer
   ```

2. Create CodeBlock.tsx with:
   - Import `Highlight` and `themes` from 'prism-react-renderer'
   - Props: `code: string`, `language: string`, `filename?: string`
   - useState for `copied` state (boolean)
   - Copy handler using `navigator.clipboard.writeText(code)`, sets copied=true, resets after 2s
   - Render structure:
     - Outer div with `code-block` class (already styled in index.css), rounded-lg, overflow-hidden
     - Optional filename bar (if filename prop provided): px-4 py-2, border-b border-gray-800, text-gray-400 text-sm font-mono
     - Copy button: absolute top-3 right-3, p-2, text-gray-400 hover:text-white, shows Copy icon or Check icon based on copied state
     - Highlight component with themes.nightOwl theme (dark code blocks match existing .code-block styling)
     - Pre element with p-4 overflow-x-auto text-sm, code element inside
     - Map tokens to div/span elements as shown in RESEARCH.md pattern

3. Use lucide-react icons (already installed): Copy, Check

4. Apply .trim() to code prop to remove leading/trailing whitespace
  </action>
  <verify>
    - `cd docs && npm ls prism-react-renderer` shows installed
    - File exists: docs/src/components/CodeBlock.tsx
    - TypeScript compiles: `cd docs && npx tsc --noEmit`
  </verify>
  <done>CodeBlock component exports and compiles without errors. Uses nightOwl theme for syntax highlighting, includes copy button with feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Create FrameworkTabs component</name>
  <files>docs/src/components/FrameworkTabs.tsx</files>
  <action>
1. Create FrameworkTabs.tsx with:
   - Import CodeBlock from './CodeBlock'
   - Import useState from 'react'
   - Type definition: `type Framework = 'react' | 'vue' | 'svelte'`
   - Props interface: `{ react: string; vue: string; svelte: string }`
   - useState for active framework, default to 'react'
   - Language map: { react: 'tsx', vue: 'vue', svelte: 'svelte' }

2. Render structure:
   - Outer div
   - Tab bar: flex, border-b border-gray-200 (light theme to match docs), role="tablist"
   - Three tab buttons (React, Vue, Svelte):
     - px-4 py-2 text-sm font-medium
     - Active: text-gray-900 border-b-2 border-gray-900
     - Inactive: text-gray-500 hover:text-gray-700
     - role="tab", aria-selected={active === fw}
     - onClick to set active framework
   - CodeBlock component with selected code and language

3. Tab labels should be capitalized: "React", "Vue", "Svelte"
  </action>
  <verify>
    - File exists: docs/src/components/FrameworkTabs.tsx
    - TypeScript compiles: `cd docs && npx tsc --noEmit`
  </verify>
  <done>FrameworkTabs component exports and compiles. Switches between React/Vue/Svelte code with proper tab styling and accessibility attributes.</done>
</task>

</tasks>

<verification>
1. Both components exist and export correctly
2. TypeScript compilation passes: `cd docs && npx tsc --noEmit`
3. Dev server starts without errors: `cd docs && npm run dev`
</verification>

<success_criteria>
- prism-react-renderer installed in docs/package.json
- CodeBlock.tsx exists with Highlight integration and copy button
- FrameworkTabs.tsx exists with tab switching logic
- Both components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-getting-started/07-01-SUMMARY.md`
</output>
