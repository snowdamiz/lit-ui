---
phase: 20-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/src/pages/Installation.tsx
  - apps/docs/src/nav.ts
  - apps/docs/src/App.tsx
  - apps/docs/src/pages/GettingStarted.tsx
autonomous: true

must_haves:
  truths:
    - "User finds NPM installation as the primary recommended approach"
    - "User finds copy-source as alternative with trade-off explanation"
    - "User knows to run 'npm install @lit-ui/core @lit-ui/button' for NPM mode"
    - "Getting Started page links to Installation for detailed setup"
  artifacts:
    - path: "apps/docs/src/pages/Installation.tsx"
      provides: "NPM + copy-source installation documentation"
      min_lines: 100
    - path: "apps/docs/src/nav.ts"
      provides: "Updated navigation with Installation page"
      contains: "Installation"
  key_links:
    - from: "apps/docs/src/nav.ts"
      to: "/installation"
      via: "href in navigation array"
      pattern: "href.*installation"
    - from: "apps/docs/src/App.tsx"
      to: "Installation component"
      via: "Route element"
      pattern: "path=\"installation\".*Installation"
---

<objective>
Create NPM installation documentation as primary path with copy-source as alternative.

Purpose: Fulfill DOC-01 (NPM installation guide) by creating a dedicated Installation page that presents npm install as the recommended approach while documenting copy-source as an alternative for developers who want source code ownership.

Output: Installation.tsx page, updated navigation, updated App.tsx routing, simplified GettingStarted.tsx that links to Installation.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-documentation/20-CONTEXT.md
@.planning/phases/20-documentation/20-RESEARCH.md
@apps/docs/src/pages/GettingStarted.tsx
@apps/docs/src/nav.ts
@apps/docs/src/App.tsx
@apps/docs/src/components/CodeBlock.tsx
@apps/docs/src/components/PrevNextNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Installation page</name>
  <files>apps/docs/src/pages/Installation.tsx</files>
  <action>
Create Installation.tsx following the pattern from GettingStarted.tsx (header with title/description, numbered sections with icons, PrevNextNav at bottom).

Page structure:
1. **Header**: "Installation" title, description about NPM vs copy-source choice
2. **Section 1: NPM Installation (Recommended)**
   - npm install command: `npm install @lit-ui/core @lit-ui/button @lit-ui/dialog`
   - Note about pnpm/yarn working too
   - Usage example showing import + JSX
   - Brief note: "Components are web components - use directly in any framework"
3. **Section 2: Copy-Source Installation (Alternative)**
   - Brief explanation: "Own and customize the source code"
   - npx lit-ui init command
   - npx lit-ui add button command
   - Link to GettingStarted for full walkthrough
4. **Section 3: Choosing Your Approach**
   - Trade-offs table or bullet list:
     - NPM: Automatic updates, smaller bundle, no customization
     - Copy-source: Full ownership, customize everything, manual updates
5. **PrevNextNav**: prev=Getting Started, next=SSR Guide (or Button if SSR not ready)

Use existing CodeBlock component for all code snippets. Use info boxes for tips (same pattern as GettingStarted.tsx). Follow exact Tailwind classes from GettingStarted.tsx for consistency.

Code examples (minimal, per CONTEXT.md):
```typescript
const npmInstallCommand = `npm install @lit-ui/core @lit-ui/button @lit-ui/dialog`;
const singlePackage = `npm install @lit-ui/button`;
const usageExample = `import '@lit-ui/button';

// Use in JSX
<lui-button variant="primary">Click me</lui-button>`;
```
  </action>
  <verify>File exists at apps/docs/src/pages/Installation.tsx and has no TypeScript errors</verify>
  <done>Installation.tsx page renders with NPM as primary path and copy-source as alternative</done>
</task>

<task type="auto">
  <name>Task 2: Update navigation and routing</name>
  <files>apps/docs/src/nav.ts, apps/docs/src/App.tsx</files>
  <action>
Update nav.ts:
1. Add Installation to Overview section items array AFTER Getting Started:
```typescript
{ title: "Getting Started", href: "/getting-started" },
{ title: "Installation", href: "/installation" },  // Add this
```
2. Add a new "Guides" section for future SSR and Migration pages:
```typescript
{
  title: "Guides",
  items: [
    { title: "SSR Setup", href: "/guides/ssr" },
    { title: "Migration", href: "/guides/migration" },
  ],
},
```

Update App.tsx:
1. Import Installation: `import { Installation } from './pages/Installation'`
2. Add route inside DocsLayout Route: `<Route path="installation" element={<Installation />} />`
3. Add placeholder routes for guides (will use Placeholder component until implemented):
```tsx
{/* Guides */}
<Route path="guides/ssr" element={<Placeholder />} />
<Route path="guides/migration" element={<Placeholder />} />
```
  </action>
  <verify>Run `pnpm --filter docs build` - no errors, navigation includes Installation and Guides section</verify>
  <done>Navigation shows Installation under Overview and Guides section appears in sidebar</done>
</task>

<task type="auto">
  <name>Task 3: Update GettingStarted to link to Installation</name>
  <files>apps/docs/src/pages/GettingStarted.tsx</files>
  <action>
Update GettingStarted.tsx to be an overview that links to Installation for details:

1. Update the Installation section (Section 1) to be briefer:
   - Keep the npx lit-ui init command for quick start
   - Add a note: "For npm packages or detailed installation options, see the Installation guide"
   - Add link to /installation

2. Update PrevNextNav at bottom:
   - Change `next={{ title: 'Button', href: '/components/button' }}`
   - To: `next={{ title: 'Installation', href: '/installation' }}`

3. Keep the rest of the page unchanged (Quick Start, Project Structure, What's Next) - these focus on the copy-source workflow which is still valid for the quick start path.

This makes GettingStarted a "quick start with copy-source" while Installation is the comprehensive installation reference.
  </action>
  <verify>Navigate to /getting-started in dev server, verify link to Installation appears and Next button goes to Installation</verify>
  <done>GettingStarted page links to Installation guide for detailed options</done>
</task>

</tasks>

<verification>
1. `pnpm --filter docs dev` - docs site runs without errors
2. Navigate to /installation - page renders with NPM section first, copy-source second
3. Navigate to /getting-started - links to Installation, Next points to Installation
4. Sidebar shows: Overview (Getting Started, Installation), Guides (SSR Setup, Migration), Components
5. Guides routes show Placeholder until 20-02 and 20-03 complete
</verification>

<success_criteria>
- Installation page exists with NPM as primary, copy-source as alternative
- nav.ts includes Installation and new Guides section
- App.tsx has routes for /installation and /guides/* paths
- GettingStarted links to Installation for detailed setup
- No TypeScript errors, docs build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/20-documentation/20-01-SUMMARY.md`
</output>
