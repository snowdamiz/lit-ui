---
phase: 30-cli-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/src/pages/components/InputPage.tsx
  - apps/docs/src/App.tsx
  - apps/docs/src/components/LivePreview.tsx
autonomous: true

must_haves:
  truths:
    - "Developer visits /components/input and sees usage examples"
    - "Developer sees props table with all 17 input props"
    - "Developer sees validation patterns demonstrated"
    - "Developer sees slots table for prefix and suffix"
  artifacts:
    - path: "apps/docs/src/pages/components/InputPage.tsx"
      provides: "Input component documentation page"
      min_lines: 400
    - path: "apps/docs/src/App.tsx"
      provides: "Route for /components/input"
      contains: "InputPage"
  key_links:
    - from: "apps/docs/src/App.tsx"
      to: "InputPage"
      via: "Route component"
      pattern: "components/input.*InputPage"
    - from: "apps/docs/src/pages/components/InputPage.tsx"
      to: "@lit-ui/input"
      via: "side-effect import"
      pattern: "import.*@lit-ui/input"
---

<objective>
Create comprehensive Input component documentation page with live examples, props table, and validation patterns.

Purpose: Developers can learn how to use the Input component through interactive examples and complete API reference.
Output: InputPage.tsx with 8-12 examples covering types, sizes, validation, password toggle, clearable, prefix/suffix, and states.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cli-documentation/30-CONTEXT.md
@.planning/phases/30-cli-documentation/30-RESEARCH.md

@apps/docs/src/pages/components/ButtonPage.tsx
@apps/docs/src/pages/components/DialogPage.tsx
@apps/docs/src/App.tsx
@apps/docs/src/components/LivePreview.tsx
@packages/input/src/input.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSX types for lui-input</name>
  <files>apps/docs/src/components/LivePreview.tsx</files>
  <action>
Add lui-input JSX type declaration to LivePreview.tsx (alongside existing lui-button declaration):

```typescript
// Side-effect import to register the custom elements from built library
import '@lit-ui/button';
import '@lit-ui/input';

// TypeScript JSX declaration for lui-button
declare global {
  namespace JSX {
    interface IntrinsicElements {
      'lui-button': React.DetailedHTMLProps<
        React.HTMLAttributes<HTMLElement> & {
          variant?: 'primary' | 'secondary' | 'outline' | 'ghost' | 'destructive';
          size?: 'sm' | 'md' | 'lg';
          loading?: boolean;
          disabled?: boolean;
          'btn-class'?: string;
        },
        HTMLElement
      >;
      'lui-input': React.DetailedHTMLProps<
        React.HTMLAttributes<HTMLElement> & {
          type?: 'text' | 'email' | 'password' | 'number' | 'search';
          size?: 'sm' | 'md' | 'lg';
          name?: string;
          value?: string;
          placeholder?: string;
          label?: string;
          'helper-text'?: string;
          required?: boolean;
          'required-indicator'?: 'asterisk' | 'text';
          disabled?: boolean;
          readonly?: boolean;
          minlength?: number;
          maxlength?: number;
          pattern?: string;
          min?: number;
          max?: number;
          clearable?: boolean;
          'show-count'?: boolean;
        },
        HTMLElement
      >;
    }
  }
}
```
  </action>
  <verify>`cd apps/docs && pnpm exec tsc --noEmit` compiles without lui-input type errors</verify>
  <done>lui-input JSX type declared for TypeScript in React docs app</done>
</task>

<task type="auto">
  <name>Task 2: Create InputPage.tsx</name>
  <files>apps/docs/src/pages/components/InputPage.tsx</files>
  <action>
Create InputPage.tsx following ButtonPage.tsx pattern exactly. Include:

**Header section:**
- Title: "Input"
- Description: "A customizable input component with validation, sizes, password toggle, and prefix/suffix slots."

**Examples section (8-12 examples per CONTEXT.md):**

1. **Types** - Show all 5 input types: text, email, password, number, search
2. **Sizes** - Show sm, md, lg sizes
3. **With Label** - Input with label attribute
4. **Helper Text** - Input with helper-text attribute showing guidance
5. **Validation States** - Show required + error state (pre-triggered per CONTEXT.md - set empty value with touched state effect, or show separate error example)
6. **Password Toggle** - type="password" shows eye icon toggle
7. **Clearable** - clearable attribute with value shows X button
8. **Prefix/Suffix Slots** - Demonstrate slots with currency symbol prefix, unit suffix
9. **Disabled State** - disabled attribute
10. **Character Counter** - show-count with maxlength

**API Reference section:**
- PropsTable with all 17 props from RESEARCH.md
- SlotsTable with prefix and suffix slots
- CSS Parts table (manual HTML table like ButtonPage)
- CSS Custom Properties table (reference --ui-input-* tokens)
- Events info card stating "Input uses standard DOM events like input, change, blur"

**Navigation:**
- prev: Dialog (/components/dialog)
- next: Textarea (/components/textarea)

**Code examples:** Use same syntax for html/react/vue/svelte since web components.

**Side-effect import:** Add `import '@lit-ui/input';` at top to register component.

**Per CONTEXT.md decisions:**
- Some examples can start in error state for visibility (show validation UX)
- Combined helper + error text example showing both states
- Component-only focus, no full form submission demos
  </action>
  <verify>`cd apps/docs && pnpm exec tsc --noEmit` and `pnpm run build` succeeds</verify>
  <done>InputPage.tsx exists with 8+ examples covering all major features and complete API reference</done>
</task>

<task type="auto">
  <name>Task 3: Add Input route to App.tsx</name>
  <files>apps/docs/src/App.tsx</files>
  <action>
1. Add import at top:
   ```typescript
   import { InputPage } from './pages/components/InputPage'
   ```

2. Add route in Components section (after dialog route):
   ```tsx
   <Route path="components/input" element={<InputPage />} />
   ```
  </action>
  <verify>Run `cd apps/docs && pnpm run dev` and navigate to /components/input - page renders</verify>
  <done>Route /components/input exists and renders InputPage</done>
</task>

</tasks>

<verification>
1. `cd apps/docs && pnpm exec tsc --noEmit` - TypeScript compiles without errors
2. `cd apps/docs && pnpm run build` - Production build succeeds
3. Navigate to http://localhost:5173/components/input - Page renders with examples
4. Count examples - should be 8-12 interactive demonstrations
5. Props table shows all 17 props
6. Slots table shows prefix and suffix
</verification>

<success_criteria>
- InputPage.tsx exists with complete page structure
- 8-12 interactive examples covering types, sizes, validation, password, clearable, slots, states
- PropsTable with 17 props
- SlotsTable with 2 slots (prefix, suffix)
- CSS Parts table
- CSS Custom Properties table
- Route accessible at /components/input
- lui-input JSX type declared in LivePreview.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/30-cli-documentation/30-02-SUMMARY.md`
</output>
