---
phase: 30-cli-documentation
plan: 03
type: execute
wave: 2
depends_on: ["30-02"]
files_modified:
  - apps/docs/src/pages/components/TextareaPage.tsx
  - apps/docs/src/App.tsx
  - apps/docs/src/components/LivePreview.tsx
autonomous: true

must_haves:
  truths:
    - "Developer visits /components/textarea and sees usage examples"
    - "Developer sees props table with all 17 textarea props"
    - "Developer sees auto-resize demo that grows as user types"
    - "Developer sees character counter demonstration"
  artifacts:
    - path: "apps/docs/src/pages/components/TextareaPage.tsx"
      provides: "Textarea component documentation page"
      min_lines: 350
    - path: "apps/docs/src/App.tsx"
      provides: "Route for /components/textarea"
      contains: "TextareaPage"
  key_links:
    - from: "apps/docs/src/App.tsx"
      to: "TextareaPage"
      via: "Route component"
      pattern: "components/textarea.*TextareaPage"
    - from: "apps/docs/src/pages/components/TextareaPage.tsx"
      to: "@lit-ui/textarea"
      via: "side-effect import"
      pattern: "import.*@lit-ui/textarea"
---

<objective>
Create comprehensive Textarea component documentation page with live examples, props table, and auto-resize demo.

Purpose: Developers can learn how to use the Textarea component through interactive examples including the signature auto-resize feature.
Output: TextareaPage.tsx with 8-12 examples covering sizes, resize modes, auto-resize, validation, character counter, and states.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cli-documentation/30-CONTEXT.md
@.planning/phases/30-cli-documentation/30-RESEARCH.md

@apps/docs/src/pages/components/ButtonPage.tsx
@apps/docs/src/pages/components/DialogPage.tsx
@apps/docs/src/App.tsx
@packages/textarea/src/textarea.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSX types for lui-textarea</name>
  <files>apps/docs/src/components/LivePreview.tsx</files>
  <action>
Add lui-textarea JSX type declaration to LivePreview.tsx (alongside existing lui-button and lui-input declarations):

```typescript
'lui-textarea': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    size?: 'sm' | 'md' | 'lg';
    name?: string;
    value?: string;
    placeholder?: string;
    label?: string;
    'helper-text'?: string;
    rows?: number;
    resize?: 'none' | 'vertical' | 'horizontal' | 'both';
    autoresize?: boolean;
    'max-rows'?: number;
    'max-height'?: string;
    required?: boolean;
    'required-indicator'?: 'asterisk' | 'text';
    disabled?: boolean;
    readonly?: boolean;
    minlength?: number;
    maxlength?: number;
    'show-count'?: boolean;
  },
  HTMLElement
>;
```

Also add the side-effect import:
```typescript
import '@lit-ui/textarea';
```
  </action>
  <verify>`cd apps/docs && pnpm exec tsc --noEmit` compiles without lui-textarea type errors</verify>
  <done>lui-textarea JSX type declared for TypeScript in React docs app</done>
</task>

<task type="auto">
  <name>Task 2: Create TextareaPage.tsx</name>
  <files>apps/docs/src/pages/components/TextareaPage.tsx</files>
  <action>
Create TextareaPage.tsx following ButtonPage.tsx pattern exactly. Include:

**Header section:**
- Title: "Textarea"
- Description: "A multi-line text input with auto-resize, validation, and character count."

**Examples section (8-12 examples per CONTEXT.md):**

1. **Basic Textarea** - Default textarea with placeholder
2. **Sizes** - Show sm, md, lg sizes
3. **With Label** - Textarea with label attribute
4. **Helper Text** - Textarea with helper-text attribute
5. **Resize Modes** - Show resize="none", "vertical" (default), "horizontal", "both"
6. **Auto-resize** - autoresize attribute - START EMPTY per CONTEXT.md so user types and watches it grow. This is the signature interactive demo.
7. **Constrained Auto-resize** - autoresize with max-rows or max-height to limit growth
8. **Character Counter** - show-count with maxlength showing "45/200" format
9. **Validation** - required with error state (pre-triggered for visibility)
10. **Disabled State** - disabled attribute

**API Reference section:**
- PropsTable with all 17 props from RESEARCH.md
- No SlotsTable (textarea has no slots per RESEARCH.md)
- CSS Parts table (wrapper, label, helper, textarea, counter, error)
- CSS Custom Properties table (reference --ui-textarea-* and shared --ui-input-* tokens)
- Events info card stating "Textarea uses standard DOM events like input, change, blur"

**Navigation:**
- prev: Input (/components/input)
- next: null or first guide

**Code examples:** Use same syntax for html/react/vue/svelte since web components.

**Side-effect import:** Add `import '@lit-ui/textarea';` at top to register component.

**Per CONTEXT.md decisions:**
- Auto-resize demo starts empty - user types to experience resize behavior
- Character counter can be combined with auto-resize or shown separately (Claude's discretion - recommend separate for clarity)
- Constrained resize (maxRows/maxHeight) can be combined or separate example
  </action>
  <verify>`cd apps/docs && pnpm exec tsc --noEmit` and `pnpm run build` succeeds</verify>
  <done>TextareaPage.tsx exists with 8+ examples covering all major features and complete API reference</done>
</task>

<task type="auto">
  <name>Task 3: Add Textarea route to App.tsx</name>
  <files>apps/docs/src/App.tsx</files>
  <action>
1. Add import at top:
   ```typescript
   import { TextareaPage } from './pages/components/TextareaPage'
   ```

2. Add route in Components section (after input route):
   ```tsx
   <Route path="components/textarea" element={<TextareaPage />} />
   ```
  </action>
  <verify>Run `cd apps/docs && pnpm run dev` and navigate to /components/textarea - page renders</verify>
  <done>Route /components/textarea exists and renders TextareaPage</done>
</task>

</tasks>

<verification>
1. `cd apps/docs && pnpm exec tsc --noEmit` - TypeScript compiles without errors
2. `cd apps/docs && pnpm run build` - Production build succeeds
3. Navigate to http://localhost:5173/components/textarea - Page renders with examples
4. Count examples - should be 8-12 interactive demonstrations
5. Auto-resize demo: type text, textarea grows
6. Props table shows all 17 props
7. No slots section (textarea has no slots)
</verification>

<success_criteria>
- TextareaPage.tsx exists with complete page structure
- 8-12 interactive examples covering sizes, resize modes, auto-resize, validation, character counter, states
- Auto-resize demo starts empty and grows as user types
- PropsTable with 17 props
- No SlotsTable (textarea has no slots)
- CSS Parts table
- CSS Custom Properties table
- Route accessible at /components/textarea
- lui-textarea JSX type declared in LivePreview.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/30-cli-documentation/30-03-SUMMARY.md`
</output>
