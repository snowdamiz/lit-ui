---
phase: 25-docs-site-dark-mode
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - apps/docs/src/layouts/DocsLayout.tsx
  - apps/docs/src/components/Sidebar.tsx
  - apps/docs/src/components/NavSection.tsx
  - apps/docs/src/components/MobileNav.tsx
autonomous: true

must_haves:
  truths:
    - "Sidebar background changes to dark in dark mode"
    - "Navigation links have appropriate dark mode contrast"
    - "Active nav item styling visible in dark mode"
    - "Mobile nav sheet has dark mode styling"
  artifacts:
    - path: "apps/docs/src/layouts/DocsLayout.tsx"
      provides: "Layout with dark mode background and sidebar"
      contains: "dark:"
    - path: "apps/docs/src/components/NavSection.tsx"
      provides: "Nav links with dark mode styling"
      contains: "dark:"
    - path: "apps/docs/src/components/MobileNav.tsx"
      provides: "Mobile sheet with dark mode styling"
      contains: "dark:"
  key_links:
    - from: "apps/docs/src/layouts/DocsLayout.tsx"
      to: "bg-background"
      via: "Tailwind class"
      pattern: "bg-background"
---

<objective>
Add dark mode styling to all navigation components.

Purpose: Navigation (sidebar, mobile nav, links) updates visually when theme changes, maintaining readability and contrast.
Output: DocsLayout, Sidebar, NavSection, and MobileNav with dark mode classes.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-docs-site-dark-mode/25-RESEARCH.md
@.planning/phases/25-docs-site-dark-mode/25-CONTEXT.md
@.planning/phases/25-docs-site-dark-mode/25-01-SUMMARY.md
@apps/docs/src/layouts/DocsLayout.tsx
@apps/docs/src/components/Sidebar.tsx
@apps/docs/src/components/NavSection.tsx
@apps/docs/src/components/MobileNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark mode styling to DocsLayout</name>
  <files>apps/docs/src/layouts/DocsLayout.tsx</files>
  <action>
Update DocsLayout.tsx with dark mode classes for the layout container and sidebar.

Current classes to update:

1. Root div (currently `min-h-screen bg-background relative`):
   - `bg-background` already uses CSS variable, should work with dark mode
   - No changes needed if CSS variables are set correctly

2. Sidebar aside (currently `bg-white/80 backdrop-blur-sm border-gray-200`):
```
bg-white/80 dark:bg-gray-950/80 backdrop-blur-sm border-r border-gray-200 dark:border-gray-800
```

3. The grid-pattern and hero-gradient divs use CSS classes that will be updated in plan 01.
   Verify they have the CSS classes applied correctly.

The `bg-background` class should automatically pick up the CSS variable value which changes in dark mode (set in 25-01).
  </action>
  <verify>DocsLayout has dark classes on sidebar, background uses CSS variable</verify>
  <done>DocsLayout sidebar and container have dark mode styling</done>
</task>

<task type="auto">
  <name>Task 2: Add dark mode styling to NavSection</name>
  <files>apps/docs/src/components/NavSection.tsx</files>
  <action>
Update NavSection.tsx with dark mode classes for section headers and nav links.

1. Section header h3 (currently `text-gray-400`):
```
text-gray-400 dark:text-gray-500
```

2. NavLink active state (currently `bg-gray-100 text-gray-900 font-medium`):
```
bg-gray-100 dark:bg-gray-800 text-gray-900 dark:text-gray-100 font-medium
```

3. NavLink inactive state (currently `text-gray-600 hover:bg-gray-100 hover:text-gray-900`):
```
text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800 hover:text-gray-900 dark:hover:text-gray-100
```

4. Icon span active (currently `text-gray-500`):
```
text-gray-500 dark:text-gray-400
```

5. Icon span inactive (currently `text-gray-400 group-hover:text-gray-500`):
```
text-gray-400 dark:text-gray-500 group-hover:text-gray-500 dark:group-hover:text-gray-400
```

Keep `transition-all duration-150` on the NavLink.
  </action>
  <verify>NavSection links have dark mode hover/active states, good contrast</verify>
  <done>Navigation links update styling in dark mode</done>
</task>

<task type="auto">
  <name>Task 3: Add dark mode styling to MobileNav</name>
  <files>apps/docs/src/components/MobileNav.tsx</files>
  <action>
Update MobileNav.tsx with dark mode classes for the mobile navigation sheet.

1. Menu button (currently `text-gray-600 hover:text-gray-900 hover:bg-gray-100`):
```
text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 hover:bg-gray-100 dark:hover:bg-gray-800
```

2. Sheet overlay (currently `bg-black/50`):
   - Keep as is, black overlay works for both modes

3. Sheet content (currently `bg-white/95 backdrop-blur-md`):
```
bg-white/95 dark:bg-gray-950/95 backdrop-blur-md shadow-lg
```

4. Sheet header (currently `border-b border-gray-200`):
```
border-b border-gray-200 dark:border-gray-800
```

5. "Navigation" text (currently `font-semibold text-gray-900`):
```
font-semibold text-gray-900 dark:text-gray-100
```

6. Close button (currently `text-gray-500 hover:text-gray-900 hover:bg-gray-100`):
```
text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100 hover:bg-gray-100 dark:hover:bg-gray-800
```

The NavSection components inside will use their own dark mode styling from Task 2.
  </action>
  <verify>Mobile nav sheet has dark background and proper contrast in dark mode</verify>
  <done>Mobile navigation sheet fully styled for dark mode</done>
</task>

</tasks>

<verification>
1. Run `cd apps/docs && npm run build` - should succeed
2. Run `cd apps/docs && npm run dev` and verify:
   - Toggle to dark mode using header toggle
   - Sidebar background changes to dark
   - Nav links have proper contrast and hover states
   - Active nav item clearly visible
   - Open mobile nav (resize to mobile) - sheet is dark
   - All text readable with good contrast
3. Test both light and dark modes thoroughly
</verification>

<success_criteria>
- DocsLayout sidebar has dark mode background
- NavSection links have dark hover/active states
- MobileNav sheet has dark mode styling
- All navigation text maintains good contrast in dark mode
- No hardcoded light-only colors remain in navigation components
</success_criteria>

<output>
After completion, create `.planning/phases/25-docs-site-dark-mode/25-03-SUMMARY.md`
</output>
