---
phase: 37-cli-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/utils/install-component.ts
  - packages/cli/src/commands/list.ts
autonomous: true

must_haves:
  truths:
    - "Developer runs `npx lit-ui add select --npm` and @lit-ui/select is installed"
    - "Developer runs `lit-ui list` and sees Select under Form category"
  artifacts:
    - path: "packages/cli/src/utils/install-component.ts"
      provides: "NPM mode mapping for select"
      contains: "select: '@lit-ui/select'"
    - path: "packages/cli/src/commands/list.ts"
      provides: "Select in categorized list"
      contains: "'select'"
  key_links:
    - from: "packages/cli/src/utils/install-component.ts"
      to: "@lit-ui/select package"
      via: "componentToPackage mapping"
      pattern: "select.*@lit-ui/select"
---

<objective>
Add Select component to CLI NPM mode and update list command categories.

Purpose: Enable developers to install Select via `lit-ui add select --npm` and see it in the component list.
Output: Updated install-component.ts with select mapping and list.ts with select in Form category.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-cli-and-documentation/37-RESEARCH.md

# Reference files
@packages/cli/src/utils/install-component.ts
@packages/cli/src/commands/list.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Select to NPM mode component mapping</name>
  <files>packages/cli/src/utils/install-component.ts</files>
  <action>
Add `select: '@lit-ui/select'` to the `componentToPackage` map object.

The map currently has:
```typescript
export const componentToPackage: Record<string, string> = {
  button: '@lit-ui/button',
  dialog: '@lit-ui/dialog',
  input: '@lit-ui/input',
  textarea: '@lit-ui/textarea',
};
```

Add `select: '@lit-ui/select',` after the textarea entry to maintain alphabetical consistency with how components were added (button, dialog, input, textarea, select).
  </action>
  <verify>
Read the file and confirm `select: '@lit-ui/select'` is present in the componentToPackage object.
  </verify>
  <done>
The componentToPackage map includes `select: '@lit-ui/select'` entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Select to list command Form category</name>
  <files>packages/cli/src/commands/list.ts</files>
  <action>
Update the categories object to include 'select' in the Form category.

Currently:
```typescript
const categories: Record<string, string[]> = {
  'Form': ['input', 'textarea'],
  'Feedback': ['dialog'],
  'Actions': ['button'],
};
```

Change to:
```typescript
const categories: Record<string, string[]> = {
  'Form': ['input', 'textarea', 'select'],
  'Feedback': ['dialog'],
  'Actions': ['button'],
};
```
  </action>
  <verify>
Read the file and confirm 'select' appears in the Form category array.
  </verify>
  <done>
The list command's Form category includes 'select'.
  </done>
</task>

</tasks>

<verification>
1. Verify install-component.ts contains `select: '@lit-ui/select'`
2. Verify list.ts Form category contains 'select'
3. Run type check: `pnpm --filter lit-ui exec tsc --noEmit`
</verification>

<success_criteria>
- componentToPackage map includes select mapping
- list command categories include select under Form
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-cli-and-documentation/37-01-SUMMARY.md`
</output>
