---
phase: 19-publishing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/README.md
  - packages/button/README.md
  - packages/dialog/README.md
  - packages/ssr/README.md
  - packages/cli/README.md
autonomous: true

must_haves:
  truths:
    - "npm package pages display quick-start README content"
    - "Each README has install command and basic usage example"
    - "READMEs link to documentation site"
  artifacts:
    - path: "packages/core/README.md"
      provides: "Quick-start documentation for @lit-ui/core"
      min_lines: 20
    - path: "packages/button/README.md"
      provides: "Quick-start documentation for @lit-ui/button"
      min_lines: 20
    - path: "packages/ssr/README.md"
      provides: "Quick-start documentation for @lit-ui/ssr"
      min_lines: 20
  key_links:
    - from: "packages/*/README.md"
      to: "https://lit-ui.dev"
      via: "Documentation link"
      pattern: "lit-ui.dev"
---

<objective>
Create README files for all publishable packages.

Purpose: npm displays README.md on package pages. Missing READMEs cause poor discoverability and user confusion.
Output: Quick-start README files following consistent template (per CONTEXT.md decision).
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-publishing/19-CONTEXT.md
@.planning/phases/19-publishing/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README for @lit-ui/core</name>
  <files>packages/core/README.md</files>
  <action>
Create README.md with quick-start format:

```markdown
# @lit-ui/core

SSR-aware base component and utilities for Lit web components with Tailwind styling.

## Installation

```bash
npm install @lit-ui/core
```

## Quick Start

Extend `TailwindElement` for components that use Tailwind CSS in Shadow DOM:

```typescript
import { TailwindElement } from '@lit-ui/core';
import { html, css } from 'lit';
import { customElement } from 'lit/decorators.js';

@customElement('my-component')
export class MyComponent extends TailwindElement {
  render() {
    return html`
      <div class="p-4 bg-primary text-primary-foreground rounded">
        Hello World
      </div>
    `;
  }
}
```

## Features

- **SSR Compatible**: Automatic inline styles for server-side rendering
- **Tailwind Integration**: Constructable stylesheets for Shadow DOM
- **Design Tokens**: Access tokens via `@lit-ui/core/tokens`
- **FOUC Prevention**: Import `@lit-ui/core/fouc.css` for flash-free loading

## Documentation

Full documentation: [https://lit-ui.dev](https://lit-ui.dev)

## License

MIT
```
  </action>
  <verify>
```bash
test -f packages/core/README.md && wc -l packages/core/README.md
```
Should show 40+ lines.
  </verify>
  <done>@lit-ui/core has README with install, usage, features, docs link</done>
</task>

<task type="auto">
  <name>Task 2: Create READMEs for component packages</name>
  <files>
    packages/button/README.md
    packages/dialog/README.md
  </files>
  <action>
Create README.md for @lit-ui/button:

```markdown
# @lit-ui/button

Accessible button component built with Lit and Tailwind CSS.

## Installation

```bash
npm install @lit-ui/button @lit-ui/core
```

## Quick Start

```html
<script type="module">
  import '@lit-ui/button';
</script>

<lui-button variant="default">Click me</lui-button>
<lui-button variant="primary">Primary</lui-button>
<lui-button variant="destructive">Delete</lui-button>
```

## Variants

- `default` - Standard button styling
- `primary` - Primary action emphasis
- `secondary` - Secondary action styling
- `outline` - Outlined button
- `ghost` - Minimal button styling
- `destructive` - Destructive/danger actions
- `link` - Link-styled button

## Sizes

- `sm` - Small button
- `default` - Default size
- `lg` - Large button
- `icon` - Icon-only button

## Features

- **Form Participation**: Works with native HTML forms
- **Loading State**: Built-in loading spinner
- **Icon Support**: Left and right icon slots
- **SSR Compatible**: Works with server-side rendering

## Documentation

Full documentation: [https://lit-ui.dev/components/button](https://lit-ui.dev/components/button)

## License

MIT
```

Create README.md for @lit-ui/dialog:

```markdown
# @lit-ui/dialog

Accessible dialog/modal component built with Lit and Tailwind CSS.

## Installation

```bash
npm install @lit-ui/dialog @lit-ui/core
```

## Quick Start

```html
<script type="module">
  import '@lit-ui/dialog';
</script>

<lui-dialog id="my-dialog">
  <span slot="title">Dialog Title</span>
  <p>Dialog content goes here.</p>
  <div slot="footer">
    <lui-button variant="outline" onclick="document.getElementById('my-dialog').close()">
      Cancel
    </lui-button>
    <lui-button variant="primary">Confirm</lui-button>
  </div>
</lui-dialog>

<lui-button onclick="document.getElementById('my-dialog').showModal()">
  Open Dialog
</lui-button>
```

## Features

- **Focus Trap**: Automatic focus management within dialog
- **Keyboard Navigation**: Escape to close, Tab cycling
- **ARIA Support**: Proper accessibility attributes
- **Animations**: Smooth open/close transitions
- **Nested Dialogs**: Support for dialog stacking
- **SSR Compatible**: Works with server-side rendering

## Documentation

Full documentation: [https://lit-ui.dev/components/dialog](https://lit-ui.dev/components/dialog)

## License

MIT
```
  </action>
  <verify>
```bash
test -f packages/button/README.md && test -f packages/dialog/README.md && echo "Both exist"
wc -l packages/button/README.md packages/dialog/README.md
```
Should show 40+ lines each.
  </verify>
  <done>Button and Dialog packages have READMEs with install, usage, features</done>
</task>

<task type="auto">
  <name>Task 3: Create READMEs for SSR and CLI packages</name>
  <files>
    packages/ssr/README.md
    packages/cli/README.md
  </files>
  <action>
Create README.md for @lit-ui/ssr:

```markdown
# @lit-ui/ssr

Server-side rendering utilities for @lit-ui components.

## Installation

```bash
npm install @lit-ui/ssr @lit-labs/ssr
```

## Quick Start

### Server-Side Rendering

```typescript
import { renderToString } from '@lit-ui/ssr';
import '@lit-ui/button';

const html = await renderToString('<lui-button>Click me</lui-button>');
// Returns HTML with Declarative Shadow DOM
```

### Client-Side Hydration

Import hydration support before any Lit components:

```typescript
// Must be first import!
import '@lit-ui/ssr/hydration';

// Then import components
import '@lit-ui/button';
import '@lit-ui/dialog';
```

## Server Detection

```typescript
import { isServer } from '@lit-ui/ssr';

if (isServer) {
  // Server-only code
} else {
  // Client-only code
}
```

## Framework Integration

See framework-specific examples:
- [Next.js](https://github.com/user/lit-ui/tree/main/examples/nextjs)
- [Astro](https://github.com/user/lit-ui/tree/main/examples/astro)
- [Express](https://github.com/user/lit-ui/tree/main/examples/express)

## Documentation

Full documentation: [https://lit-ui.dev](https://lit-ui.dev)

## License

MIT
```

Create README.md for lit-ui CLI:

```markdown
# lit-ui

CLI for adding lit-ui components to your project.

## Installation

```bash
npx lit-ui init
```

## Commands

### Initialize Project

```bash
npx lit-ui init
```

Sets up lit-ui in your project with choice of:
- **copy-source**: Copy component source files (full ownership)
- **npm**: Install packages from npm (dependency management)

### Add Components

```bash
npx lit-ui add button
npx lit-ui add dialog
```

### List Components

```bash
npx lit-ui list
```

### Migrate to NPM Mode

```bash
npx lit-ui migrate
```

Convert from copy-source to npm package mode.

## Modes

### Copy-Source Mode (default)
Components are copied to your project. You own the code and can modify freely.

### NPM Mode
Components installed as npm packages. Receive updates via npm.

## Documentation

Full documentation: [https://lit-ui.dev](https://lit-ui.dev)

## License

MIT
```
  </action>
  <verify>
```bash
test -f packages/ssr/README.md && test -f packages/cli/README.md && echo "Both exist"
wc -l packages/ssr/README.md packages/cli/README.md
```
Should show 40+ lines each.
  </verify>
  <done>SSR and CLI packages have READMEs with install, usage, features</done>
</task>

</tasks>

<verification>
1. All 5 packages have README.md files
2. Each README has: title, install command, quick start, docs link, license
3. READMEs follow consistent format
4. No placeholder content remaining
</verification>

<success_criteria>
- `ls packages/*/README.md | wc -l` returns 5
- Each README has install and usage sections
- Each README links to https://lit-ui.dev
</success_criteria>

<output>
After completion, create `.planning/phases/19-publishing/19-02-SUMMARY.md`
</output>
