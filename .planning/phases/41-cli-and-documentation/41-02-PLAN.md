---
phase: 41-cli-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/package.json
  - apps/docs/src/components/LivePreview.tsx
  - apps/docs/src/nav.ts
  - apps/docs/src/App.tsx
  - apps/docs/src/pages/components/ButtonPage.tsx
  - apps/docs/src/pages/components/DialogPage.tsx
  - apps/docs/src/pages/components/InputPage.tsx
  - apps/docs/src/pages/components/TextareaPage.tsx
  - apps/docs/src/pages/components/SelectPage.tsx
autonomous: true

must_haves:
  truths:
    - "Docs site navigation shows Checkbox, Radio, Switch in Components section (alphabetically)"
    - "Clicking navigation links routes to /components/checkbox, /components/radio, /components/switch"
    - "PrevNextNav chain flows: Button -> Checkbox -> Dialog -> Input -> Radio -> Select -> Switch -> Textarea"
    - "TypeScript accepts lui-checkbox, lui-checkbox-group, lui-radio, lui-radio-group, lui-switch in JSX"
  artifacts:
    - path: "apps/docs/src/nav.ts"
      provides: "Navigation entries for Checkbox, Radio, Switch"
      contains: "Checkbox"
    - path: "apps/docs/src/App.tsx"
      provides: "Route elements for checkbox, radio, switch"
      contains: "CheckboxPage"
    - path: "apps/docs/src/components/LivePreview.tsx"
      provides: "JSX type declarations for 5 new custom elements"
      contains: "lui-checkbox"
    - path: "apps/docs/package.json"
      provides: "Workspace dependencies for checkbox, radio, switch packages"
      contains: "@lit-ui/checkbox"
  key_links:
    - from: "apps/docs/src/nav.ts"
      to: "apps/docs/src/App.tsx"
      via: "href paths match route paths"
      pattern: "/components/checkbox"
    - from: "apps/docs/src/App.tsx"
      to: "pages/components/CheckboxPage.tsx"
      via: "lazy import"
      pattern: "CheckboxPage"
---

<objective>
Set up docs infrastructure for the three new component pages: package dependencies, JSX types, navigation, routing, and PrevNextNav chain updates.

Purpose: Create the foundation that docs pages need — without this, the new pages can't compile or be navigated to.
Output: Updated docs infrastructure files ready for page creation in subsequent plans.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-cli-and-documentation/41-RESEARCH.md

# Reference files
@apps/docs/package.json
@apps/docs/src/components/LivePreview.tsx
@apps/docs/src/nav.ts
@apps/docs/src/App.tsx
@apps/docs/src/pages/components/ButtonPage.tsx (PrevNextNav only)
@apps/docs/src/pages/components/DialogPage.tsx (PrevNextNav only)
@apps/docs/src/pages/components/InputPage.tsx (PrevNextNav only)
@apps/docs/src/pages/components/TextareaPage.tsx (PrevNextNav only)
@apps/docs/src/pages/components/SelectPage.tsx (PrevNextNav only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace dependencies and JSX types</name>
  <files>
    apps/docs/package.json
    apps/docs/src/components/LivePreview.tsx
  </files>
  <action>
**package.json:** Add three workspace dependencies in the `dependencies` section (alphabetically):

```json
"@lit-ui/checkbox": "workspace:*",
"@lit-ui/radio": "workspace:*",
"@lit-ui/switch": "workspace:*",
```

Insert after `@lit-ui/button` (checkbox), after `@lit-ui/input` (radio), and after `@lit-ui/select` (switch) to maintain alphabetical order.

Then run `pnpm install` from the workspace root to update the lockfile.

**LivePreview.tsx:** Add side-effect imports at the top (after existing imports):

```typescript
import '@lit-ui/checkbox';
import '@lit-ui/radio';
import '@lit-ui/switch';
```

Add JSX type declarations inside the existing `IntrinsicElements` interface (after lui-option-group). Add all 5 new custom elements:

```typescript
'lui-checkbox': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    checked?: boolean;
    disabled?: boolean;
    required?: boolean;
    indeterminate?: boolean;
    name?: string;
    value?: string;
    label?: string;
    size?: 'sm' | 'md' | 'lg';
  },
  HTMLElement
>;
'lui-checkbox-group': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    label?: string;
    disabled?: boolean;
    required?: boolean;
    error?: string;
    'select-all'?: boolean;
  },
  HTMLElement
>;
'lui-radio': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    value?: string;
    checked?: boolean;
    disabled?: boolean;
    label?: string;
    size?: 'sm' | 'md' | 'lg';
  },
  HTMLElement
>;
'lui-radio-group': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    name?: string;
    value?: string;
    required?: boolean;
    disabled?: boolean;
    label?: string;
    error?: string;
  },
  HTMLElement
>;
'lui-switch': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    checked?: boolean;
    disabled?: boolean;
    required?: boolean;
    name?: string;
    value?: string;
    label?: string;
    size?: 'sm' | 'md' | 'lg';
  },
  HTMLElement
>;
```
  </action>
  <verify>
1. Read package.json and confirm @lit-ui/checkbox, @lit-ui/radio, @lit-ui/switch are present
2. Read LivePreview.tsx and confirm all 5 new JSX type declarations exist
3. Confirm pnpm install completed successfully
  </verify>
  <done>
package.json has all 3 new workspace dependencies. LivePreview.tsx has JSX types for lui-checkbox, lui-checkbox-group, lui-radio, lui-radio-group, lui-switch. pnpm lockfile updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update navigation, routing, and PrevNextNav chain</name>
  <files>
    apps/docs/src/nav.ts
    apps/docs/src/App.tsx
    apps/docs/src/pages/components/ButtonPage.tsx
    apps/docs/src/pages/components/DialogPage.tsx
    apps/docs/src/pages/components/InputPage.tsx
    apps/docs/src/pages/components/TextareaPage.tsx
    apps/docs/src/pages/components/SelectPage.tsx
  </files>
  <action>
**nav.ts:** Update the Components section items array to include the new components in alphabetical order:

```typescript
{
  title: "Components",
  items: [
    { title: "Button", href: "/components/button" },
    { title: "Checkbox", href: "/components/checkbox" },
    { title: "Dialog", href: "/components/dialog" },
    { title: "Input", href: "/components/input" },
    { title: "Radio", href: "/components/radio" },
    { title: "Select", href: "/components/select" },
    { title: "Switch", href: "/components/switch" },
    { title: "Textarea", href: "/components/textarea" },
  ],
},
```

**App.tsx:** Add imports and routes for the three new pages. Add imports at the top:

```typescript
import { CheckboxPage } from './pages/components/CheckboxPage'
import { RadioPage } from './pages/components/RadioPage'
import { SwitchPage } from './pages/components/SwitchPage'
```

Add route elements in the Components section (alphabetically):

```tsx
<Route path="components/checkbox" element={<CheckboxPage />} />
<Route path="components/radio" element={<RadioPage />} />
<Route path="components/switch" element={<SwitchPage />} />
```

NOTE: The actual page files (CheckboxPage.tsx, RadioPage.tsx, SwitchPage.tsx) will be created in Plans 03-05. App.tsx will have import errors until those plans complete. This is expected — the infrastructure must be in place first.

**PrevNextNav chain updates:** The new alphabetical order is:
Button -> Checkbox -> Dialog -> Input -> Radio -> Select -> Switch -> Textarea

Update each existing page's PrevNextNav:

- **ButtonPage.tsx**: Change `next` from `{ title: 'Dialog', href: '/components/dialog' }` to `{ title: 'Checkbox', href: '/components/checkbox' }`. Keep prev as-is.

- **DialogPage.tsx**: Change `prev` from `{ title: 'Button', href: '/components/button' }` to `{ title: 'Checkbox', href: '/components/checkbox' }`. Keep next as-is (if it has one) or add `next={{ title: 'Input', href: '/components/input' }}`.

  Check: DialogPage currently has `prev={{ title: 'Button', href: '/components/button' }}` and no next. Update to:
  ```tsx
  <PrevNextNav
    prev={{ title: 'Checkbox', href: '/components/checkbox' }}
    next={{ title: 'Input', href: '/components/input' }}
  />
  ```

- **InputPage.tsx**: Currently `prev=Dialog, next=Textarea`. Change to `prev=Dialog, next=Radio`:
  ```tsx
  <PrevNextNav
    prev={{ title: 'Dialog', href: '/components/dialog' }}
    next={{ title: 'Radio', href: '/components/radio' }}
  />
  ```

- **SelectPage.tsx**: Currently `prev=Textarea, next=Theme Configurator`. Change to `prev=Radio, next=Switch`:
  ```tsx
  <PrevNextNav
    prev={{ title: 'Radio', href: '/components/radio' }}
    next={{ title: 'Switch', href: '/components/switch' }}
  />
  ```

- **TextareaPage.tsx**: Currently `prev=Input` with no next. Change to `prev=Switch, next=Theme Configurator` (or keep no next if Textarea is the last component — but since Select previously had next=Theme Configurator, Textarea should get it now):
  ```tsx
  <PrevNextNav
    prev={{ title: 'Switch', href: '/components/switch' }}
    next={{ title: 'Theme Configurator', href: '/configurator' }}
  />
  ```
  </action>
  <verify>
1. Read nav.ts and confirm 8 component entries in alphabetical order
2. Read App.tsx and confirm 3 new route elements and imports
3. Read each existing page and verify PrevNextNav props match the chain: Button->Checkbox->Dialog->Input->Radio->Select->Switch->Textarea
  </verify>
  <done>
Navigation has 8 alphabetically ordered component entries. App.tsx has routes for checkbox, radio, switch. PrevNextNav chain flows correctly: Button->Checkbox->Dialog->Input->Radio->Select->Switch->Textarea->Theme Configurator.
  </done>
</task>

</tasks>

<verification>
1. nav.ts has 8 component entries in alphabetical order
2. App.tsx has 3 new imports and 3 new Route elements
3. LivePreview.tsx has JSX types for all 5 new custom elements
4. package.json has 3 new workspace dependencies
5. PrevNextNav chain is correct across all 5 existing pages
6. pnpm install completed without errors
</verification>

<success_criteria>
- Navigation sidebar shows Checkbox, Radio, Switch in correct alphabetical position
- Routes exist for /components/checkbox, /components/radio, /components/switch
- TypeScript recognizes all 5 new custom element types in JSX
- PrevNextNav flows correctly through all 8 component pages
</success_criteria>

<output>
After completion, create `.planning/phases/41-cli-and-documentation/41-02-SUMMARY.md`
</output>
