---
phase: 41-cli-and-documentation
plan: 03
type: execute
wave: 2
depends_on: ["41-02"]
files_modified:
  - apps/docs/src/pages/components/CheckboxPage.tsx
autonomous: true

must_haves:
  truths:
    - "Developer sees Checkbox docs page with interactive examples for basic, checked, indeterminate, sizes, disabled, and required states"
    - "Developer sees CheckboxGroup examples with label, select-all, disabled propagation, and required validation"
    - "Developer finds API reference with props table, events, CSS custom properties, and CSS parts"
    - "Developer can navigate to Checkbox page from sidebar and via PrevNextNav from Button/Dialog"
  artifacts:
    - path: "apps/docs/src/pages/components/CheckboxPage.tsx"
      provides: "Complete Checkbox documentation page"
      exports: ["CheckboxPage"]
      min_lines: 300
  key_links:
    - from: "apps/docs/src/pages/components/CheckboxPage.tsx"
      to: "@lit-ui/checkbox"
      via: "side-effect import"
      pattern: "import '@lit-ui/checkbox'"
    - from: "apps/docs/src/pages/components/CheckboxPage.tsx"
      to: "PrevNextNav chain"
      via: "prev=Button, next=Dialog"
      pattern: "prev.*Button.*next.*Dialog"
---

<objective>
Create the Checkbox documentation page with interactive examples, CheckboxGroup examples, and API reference.

Purpose: Developers can learn how to use lui-checkbox and lui-checkbox-group through interactive examples and API documentation.
Output: CheckboxPage.tsx following the established docs page pattern (InputPage.tsx structure).
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-cli-and-documentation/41-RESEARCH.md

# Pattern reference (follow this structure exactly)
@apps/docs/src/pages/components/InputPage.tsx

# Component source (for API reference accuracy)
@packages/checkbox/src/checkbox.ts
@packages/checkbox/src/checkbox-group.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CheckboxPage.tsx with examples and API reference</name>
  <files>apps/docs/src/pages/components/CheckboxPage.tsx</files>
  <action>
Create `apps/docs/src/pages/components/CheckboxPage.tsx` following the exact pattern from InputPage.tsx.

**Imports:**
```typescript
import { FrameworkProvider } from '../../contexts/FrameworkContext';
import { ExampleBlock } from '../../components/ExampleBlock';
import { PropsTable, type PropDef } from '../../components/PropsTable';
import { SlotsTable, type SlotDef } from '../../components/SlotsTable';
import { PrevNextNav } from '../../components/PrevNextNav';
import { CodeBlock } from '../../components/CodeBlock';

// Side-effect import to register custom elements
import '@lit-ui/checkbox';
```

**Props data arrays (from research and component source):**

`checkboxProps: PropDef[]` — 8 props:
- checked (boolean, false) — Whether the checkbox is checked
- disabled (boolean, false) — Whether the checkbox is disabled
- required (boolean, false) — Whether the checkbox is required for form submission
- indeterminate (boolean, false) — Whether to show indeterminate (dash) state
- name (string, "") — Form submission name
- value (string, "on") — Form submission value when checked
- label (string, "") — Label text displayed next to the checkbox
- size ("sm" | "md" | "lg", "md") — Size variant

`checkboxGroupProps: PropDef[]` — 5 props:
- label (string, "") — Group label text
- disabled (boolean, false) — Disables all child checkboxes
- required (boolean, false) — At least one checkbox must be checked
- error (string, "") — Error message displayed below the group
- select-all (boolean, false) — Shows a select-all parent checkbox

**Code examples (define as template literal strings at top of file):**

1. `basicCode` — Basic checkbox: `<lui-checkbox label="Accept terms"></lui-checkbox>`
2. `checkedCode` — Pre-checked: `<lui-checkbox checked label="Subscribed"></lui-checkbox>`
3. `indeterminateCode` — Indeterminate state: `<lui-checkbox indeterminate label="Select all"></lui-checkbox>`
4. `sizesCode` — Three sizes (sm, md, lg)
5. `disabledCode` — Disabled states (unchecked + checked)
6. `requiredCode` — Required checkbox
7. `groupCode` — CheckboxGroup with label and children
8. `selectAllCode` — CheckboxGroup with select-all
9. `groupDisabledCode` — CheckboxGroup with disabled propagation
10. `groupRequiredCode` — CheckboxGroup with required validation + error
11. `formCode` — Checkbox in a form showing form participation

Use the HTML examples from the research file (41-RESEARCH.md, Code Examples section).

**Page structure (following InputPage.tsx pattern):**

1. Page header with title "Checkbox" and description "Accessible checkbox with indeterminate state, CheckboxGroup with select-all, and full form participation."
2. **Examples section** — Each example uses `<ExampleBlock>` with `preview` (live web component), and `html`/`react`/`vue`/`svelte` code tabs (all same code since web components). Group examples logically:
   - Basic Usage (basic, checked, indeterminate)
   - Sizes (sm, md, lg)
   - States (disabled, required)
   - CheckboxGroup (basic group, select-all, disabled group, required group)
   - Form Integration
3. **Custom Styling section** — CSS custom properties table (--ui-checkbox-* tokens from component source) and CSS parts
4. **API Reference section** — PropsTable for checkbox props, PropsTable for checkbox-group props, events list (ui-change for both), slots
5. **PrevNextNav** — `prev={{ title: 'Button', href: '/components/button' }}` `next={{ title: 'Dialog', href: '/components/dialog' }}`

**Events to document:**
- `ui-change` on lui-checkbox: `{ checked: boolean, value: string | null }`
- `ui-change` on lui-checkbox-group (from select-all): `{ allChecked: boolean, checkedCount: number, totalCount: number }`

Export as named export: `export function CheckboxPage() { ... }`

**IMPORTANT:** For ExampleBlock preview props, render the actual web components as JSX (e.g., `<lui-checkbox label="Accept terms"></lui-checkbox>`). The JSX types were declared in Plan 02.
  </action>
  <verify>
1. File exists at apps/docs/src/pages/components/CheckboxPage.tsx
2. Exports CheckboxPage function component
3. Has at least 10 ExampleBlock sections
4. Has PropsTable for both checkbox and checkbox-group
5. Has PrevNextNav with prev=Button, next=Dialog
6. TypeScript compiles: `pnpm --filter docs exec tsc --noEmit`
  </verify>
  <done>
CheckboxPage.tsx exists with interactive examples covering basic, checked, indeterminate, sizes, disabled, required, group, select-all, form integration. API reference includes props tables for both checkbox and checkbox-group, events, CSS custom properties. PrevNextNav links to Button (prev) and Dialog (next).
  </done>
</task>

</tasks>

<verification>
1. CheckboxPage.tsx exists and exports CheckboxPage
2. All checkbox examples render correctly in browser
3. CheckboxGroup examples demonstrate select-all, disabled propagation, required validation
4. API reference tables are complete and accurate
5. PrevNextNav chain is correct (Button <- Checkbox -> Dialog)
6. TypeScript compiles without errors
7. Dev server renders the page: `pnpm --filter docs dev` then visit /components/checkbox
</verification>

<success_criteria>
- Checkbox docs page is fully functional with interactive examples
- Both lui-checkbox and lui-checkbox-group are documented
- Page follows exact same structure as InputPage.tsx
- All examples from research are represented
</success_criteria>

<output>
After completion, create `.planning/phases/41-cli-and-documentation/41-03-SUMMARY.md`
</output>
