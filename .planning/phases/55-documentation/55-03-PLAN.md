---
phase: 55-documentation
plan: 03
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - apps/docs/src/pages/components/ToastPage.tsx
autonomous: true

must_haves:
  truths:
    - "Toast documentation page shows interactive examples with buttons that trigger real toast() calls"
    - "Toast page documents the imperative API: toast(), toast.success(), toast.error(), toast.warning(), toast.info(), toast.promise(), toast.dismiss(), toast.dismissAll()"
    - "Toast page has full API reference: toaster props (3), toast CSS custom properties (18), and CSS parts"
    - "Toast page has accessibility notes about role=status, aria-live, and reduced motion"
    - "PrevNextNav links are correct: prev=Time Picker, next=Tooltip"
  artifacts:
    - path: "apps/docs/src/pages/components/ToastPage.tsx"
      provides: "Complete toast documentation page with imperative API docs"
      min_lines: 500
  key_links:
    - from: "apps/docs/src/pages/components/ToastPage.tsx"
      to: "@lit-ui/toast"
      via: "import { toast } from '@lit-ui/toast'"
      pattern: "import.*toast.*from '@lit-ui/toast'"
---

<objective>
Create the documentation page for the Toast component with its unique imperative API documentation.

Purpose: Toast is fundamentally different from other components — it uses an imperative `toast()` function, not just HTML markup. The documentation must clearly show the JS API alongside the standard component reference.

Output: ToastPage.tsx (~650 lines) following DialogPage.tsx structure but with additional imperative API section.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-documentation/55-RESEARCH.md
@apps/docs/src/pages/components/DialogPage.tsx (template — follow this exact pattern for structure)
@packages/toast/src/api.ts (source of truth for imperative API)
@packages/toast/src/toast.ts (source of truth for toast element)
@packages/toast/src/toaster.ts (source of truth for toaster element)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToastPage.tsx with imperative API documentation</name>
  <files>apps/docs/src/pages/components/ToastPage.tsx</files>
  <action>
Create `apps/docs/src/pages/components/ToastPage.tsx` following DialogPage.tsx structure. This page is unique because toast uses an imperative `toast()` function — the examples use React onClick handlers calling `toast()`, NOT just HTML markup.

**Imports:**
- FrameworkProvider, ExampleBlock, PropsTable (PropDef), PrevNextNav, CodeBlock
- Side-effect: `import '@lit-ui/toast'`
- Named: `import { toast } from '@lit-ui/toast'`
- Do NOT import SlotsTable or EventsTable (toast doesn't expose meaningful slots/events to the user)

**Data Arrays (from research):**

Toaster Props (3 items — these are for the `<lui-toaster>` container):
| name | type | default | description |
|------|------|---------|-------------|
| position | ToastPosition | '"bottom-right"' | Position of toast container. Options: top-left, top-center, top-right, bottom-left, bottom-center, bottom-right |
| max-visible | number | '3' | Maximum number of visible toasts at once |
| gap | number | '12' | Gap between toasts in pixels |

CSS Custom Properties (18 items — from research toastCSSVars array):
All --ui-toast-* variables including base styles and variant colors (success, error, warning, info with -bg, -border, -icon each)

**Page Sections (follow DialogPage order, with added Imperative API section):**

1. **Header**: Title "Toast", description about notification system with imperative API, queue management, variants, and auto-dismiss

2. **Quick Start / Imperative API Section** (unique to toast — insert before Examples):
   This is the key differentiator. Show a CodeBlock with the full API surface:
   ```ts
   import { toast } from '@lit-ui/toast';

   // Basic
   toast('Hello world');

   // Variants
   toast.success('Saved!');
   toast.error('Something went wrong');
   toast.warning('Careful!');
   toast.info('FYI...');

   // With options
   toast('Custom toast', {
     description: 'More details here',
     duration: 8000,
     dismissible: true,
     action: { label: 'Undo', onClick: () => undoAction() },
     position: 'top-right',
   });

   // Promise
   toast.promise(fetchData(), {
     loading: 'Loading...',
     success: 'Done!',
     error: 'Failed',
   });

   // Dismiss
   const id = toast('Persistent', { duration: 0 });
   toast.dismiss(id);
   toast.dismissAll();
   ```

3. **Examples Section** with icon header:
   - "Basic Toast" — Button onClick calls `toast('Hello world')`. Preview is a button, NOT the toast element. Framework tabs show the import + function call pattern.
   - "Variants" — 4 buttons (Success, Error, Warning, Info) each calling the corresponding variant. Framework tabs.
   - "Toast with Description" — Button calling `toast('Title', { description: 'Longer description text' })`. Framework tabs.
   - "Toast with Action" — Button calling `toast('Item deleted', { action: { label: 'Undo', onClick: () => toast.success('Undone!') } })`. Framework tabs.

4. **Accessibility Section**:
   - Info/success toasts use `role="status"` with `aria-live="polite"` (non-interrupting)
   - Error/warning toasts use `role="alert"` with `aria-live="assertive"` (interrupting)
   - Auto-dismiss pauses on hover and keyboard focus
   - `prefers-reduced-motion` disables enter/exit animations
   - Toasts are dismissible via close button for keyboard users

5. **Toaster Configuration Section** (unique to toast):
   Show how to customize the toaster container by adding `<lui-toaster position="top-center" max-visible="5">` to the page. CodeBlock with HTML example.

6. **Custom Styling Section**:
   - CSS Custom Properties subsection with CodeBlock showing variant color overrides
   - Note the 4 variant groups (success, error, warning, info) each with -bg, -border, -icon

7. **API Reference Section** with icon header:
   - "Imperative API" subsection: A table (manually built like CSS vars table in DialogPage) showing each function signature:
     | Function | Arguments | Description |
     | toast(message) | message: string, options?: ToastOptions | Show default toast |
     | toast.success(message) | message: string, options?: ToastOptions | Show success variant |
     | toast.error(message) | " | Show error variant |
     | toast.warning(message) | " | Show warning variant |
     | toast.info(message) | " | Show info variant |
     | toast.promise(promise, msgs) | promise: Promise, { loading, success, error } | Promise-tracking toast |
     | toast.dismiss(id) | id: string | Dismiss specific toast |
     | toast.dismissAll() | — | Dismiss all toasts |
   - "Toaster Props" subsection (PropsTable with 3 props)
   - CSS Custom Properties table (18 entries)

8. **PrevNextNav**: prev={{ title: 'Time Picker', href: '/components/time-picker' }} next={{ title: 'Tooltip', href: '/components/tooltip' }}

**Interactive Preview Components:**
Toast demos are unique — they use button clicks, not rendered elements:
```tsx
function BasicToastDemo() {
  return (
    <lui-button onClick={() => toast('Hello world!')}>
      Show Toast
    </lui-button>
  );
}

function VariantsDemo() {
  return (
    <div className="flex flex-wrap gap-3">
      <lui-button onClick={() => toast.success('Changes saved!')}>Success</lui-button>
      <lui-button onClick={() => toast.error('Something went wrong')}>Error</lui-button>
      <lui-button onClick={() => toast.warning('Are you sure?')}>Warning</lui-button>
      <lui-button onClick={() => toast.info('New update available')}>Info</lui-button>
    </div>
  );
}
```

**Framework code tabs pattern for toast:**
Unlike other components, toast code tabs show JS imports, not HTML elements:
```tsx
const basicToastCode = {
  html: `<script type="module">
  import { toast } from '@lit-ui/toast';
  document.querySelector('#toast-btn')
    .addEventListener('click', () => toast('Hello world!'));
</script>
<button id="toast-btn">Show Toast</button>`,
  react: `import { toast } from '@lit-ui/toast';

function App() {
  return <button onClick={() => toast('Hello world!')}>Show Toast</button>;
}`,
  vue: `<script setup>
import { toast } from '@lit-ui/toast';
</script>
<template>
  <button @click="toast('Hello world!')">Show Toast</button>
</template>`,
  svelte: `<script>
  import { toast } from '@lit-ui/toast';
</script>
<button on:click={() => toast('Hello world!')}>Show Toast</button>`,
};
```
  </action>
  <verify>
    - File exists and exports `ToastPage` function
    - Contains FrameworkProvider wrapper
    - Contains `import { toast } from '@lit-ui/toast'`
    - Contains at least 3 ExampleBlock usages
    - Contains PropsTable with 3 toaster props
    - Contains imperative API reference table with 8 function entries
    - Contains CSS vars table with 18 entries
    - Contains PrevNextNav with correct prev/next
    - `wc -l apps/docs/src/pages/components/ToastPage.tsx` is 500+ lines
    - Preview demos use onClick handlers calling toast() functions
  </verify>
  <done>
    ToastPage.tsx is a complete documentation page with imperative API reference (CodeBlock + table), interactive button-based examples (basic, variants, description, action), accessibility notes about role/aria-live, toaster configuration section, CSS custom properties documentation (18 vars including variant colors), and correct PrevNextNav links.
  </done>
</task>

</tasks>

<verification>
- ToastPage.tsx exists with 500+ lines
- Exports named `ToastPage` function
- Follows DialogPage.tsx section structure with additions for imperative API
- Interactive demos use `toast()` function calls, not static HTML
- API Reference covers both imperative functions AND toaster element props
- All 18 CSS custom properties documented
- Accessibility section covers role/aria-live semantics and reduced motion
- PrevNextNav: prev=Time Picker, next=Tooltip
</verification>

<success_criteria>
- ToastPage.tsx has imperative API CodeBlock, 4 interactive button-click examples, 8 API function entries, 3 toaster props, 18 CSS vars, accessibility notes
- Framework code tabs show JS `import { toast }` pattern, not HTML element usage
- Page structure matches DialogPage.tsx with added Imperative API and Toaster Configuration sections
- PrevNextNav links complete the alphabetical chain: Time Picker -> Toast -> Tooltip
</success_criteria>

<output>
After completion, create `.planning/phases/55-documentation/55-03-SUMMARY.md`
</output>
