---
phase: 75-checkbox
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/src/pages/components/CheckboxPage.tsx
autonomous: true
requirements: [CHK-02]

must_haves:
  truths:
    - "The checkboxCSSVars array in CheckboxPage.tsx covers all tokens defined in the :root Checkbox block of tailwind.css"
    - "Each CSS token entry has a default value matching the exact tailwind.css :root value (rem/px for layout tokens, double-fallback var() for color tokens)"
    - "The docs page event table lists both the lui-checkbox ui-change event and the lui-checkbox-group ui-change event"
  artifacts:
    - path: "apps/docs/src/pages/components/CheckboxPage.tsx"
      provides: "Expanded CSS token table with all checkbox tokens"
      contains: "--ui-checkbox-size-sm"
  key_links:
    - from: "apps/docs/src/pages/components/CheckboxPage.tsx (checkboxCSSVars array)"
      to: "packages/core/src/styles/tailwind.css (:root checkbox block)"
      via: "default values matching tailwind.css :root exactly"
      pattern: "checkboxCSSVars.*name.*--ui-checkbox"
---

<objective>
Update CheckboxPage.tsx to expand the CSS token documentation table from 12 entries to the full set of tokens defined in tailwind.css :root, and verify all event documentation is accurate.

Purpose: The current checkboxCSSVars array has 12 entries but the :root block defines 20+ tokens (including size, border-width, font-size, indeterminate state tokens). Developers overriding checkbox appearance need the complete token reference. The same expansion was done for Input (Phase 72-02, 7→16 tokens), Textarea (Phase 73-02, 7→16 tokens), and Select (Phase 74-02, 7→27 tokens).

Output: checkboxCSSVars expanded to include all tokens from the :root checkbox block: size (sm/md/lg), border-width, radius, label-gap, transition, font-size (sm/md/lg), all color tokens with exact tailwind.css :root defaults, group-gap.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current checkboxCSSVars (12 entries) vs full :root token set -->

Current checkboxCSSVars in CheckboxPage.tsx (INCOMPLETE — 12 entries):
```typescript
const checkboxCSSVars: CSSVarDef[] = [
  { name: '--ui-checkbox-bg', default: 'var(--color-background)', description: 'Unchecked background color.' },
  { name: '--ui-checkbox-bg-checked', default: 'var(--color-primary)', description: 'Checked/indeterminate background color.' },
  { name: '--ui-checkbox-border', default: 'var(--color-border)', description: 'Border color.' },
  { name: '--ui-checkbox-border-checked', default: 'var(--color-primary)', description: 'Border color when checked.' },
  { name: '--ui-checkbox-border-error', default: 'var(--color-destructive)', description: 'Border color on validation error.' },
  { name: '--ui-checkbox-check-color', default: 'var(--color-primary-foreground)', description: 'Checkmark/dash icon color.' },
  { name: '--ui-checkbox-radius', default: 'var(--radius-sm)', description: 'Border radius of the checkbox box.' },
  { name: '--ui-checkbox-ring', default: 'var(--color-ring)', description: 'Focus ring color.' },
  { name: '--ui-checkbox-transition', default: '150ms', description: 'Transition duration for animations.' },
  { name: '--ui-checkbox-label-gap', default: '0.5rem', description: 'Gap between checkbox and label.' },
  { name: '--ui-checkbox-group-gap', default: '0.5rem', description: 'Gap between items in a checkbox group.' },
  { name: '--ui-checkbox-text-error', default: 'var(--color-destructive)', description: 'Error text color.' },
];
```

Full :root token block in tailwind.css (SOURCE OF TRUTH):
```css
/* Box dimensions per size */
--ui-checkbox-size-sm: 1rem;
--ui-checkbox-size-md: 1.25rem;
--ui-checkbox-size-lg: 1.5rem;

/* Layout */
--ui-checkbox-radius: 0.25rem;
--ui-checkbox-label-gap: 0.5rem;
--ui-checkbox-transition: 150ms;
--ui-checkbox-border-width: 2px;

/* Label typography */
--ui-checkbox-font-size-sm: 0.875rem;
--ui-checkbox-font-size-md: 1rem;
--ui-checkbox-font-size-lg: 1.125rem;

/* Default state (unchecked) */
--ui-checkbox-bg: var(--color-background, white);
--ui-checkbox-border: var(--color-border, var(--ui-color-border));

/* Checked state */
--ui-checkbox-bg-checked: var(--color-primary, var(--ui-color-primary));
--ui-checkbox-border-checked: var(--color-primary, var(--ui-color-primary));
--ui-checkbox-check-color: white;

/* Indeterminate state */
--ui-checkbox-bg-indeterminate: var(--color-primary, var(--ui-color-primary));
--ui-checkbox-border-indeterminate: var(--color-primary, var(--ui-color-primary));

/* Focus state */
--ui-checkbox-ring: var(--color-ring, var(--ui-color-ring));

/* Error state */
--ui-checkbox-border-error: var(--color-destructive, var(--ui-color-destructive));
--ui-checkbox-text-error: var(--color-destructive, var(--ui-color-destructive));

/* Group tokens */
--ui-checkbox-group-gap: 0.5rem;
```

Discrepancies to fix in current checkboxCSSVars:
1. `--ui-checkbox-bg` default: docs say `var(--color-background)` but tailwind.css says `var(--color-background, white)` — use double-fallback form
2. `--ui-checkbox-bg-checked` default: docs say `var(--color-primary)` but tailwind.css says `var(--color-primary, var(--ui-color-primary))` — use double-fallback form
3. `--ui-checkbox-border` default: docs say `var(--color-border)` — should be `var(--color-border, var(--ui-color-border))`
4. `--ui-checkbox-border-checked` default: docs say `var(--color-primary)` — should be `var(--color-primary, var(--ui-color-primary))`
5. `--ui-checkbox-border-error` default: docs say `var(--color-destructive)` — should be `var(--color-destructive, var(--ui-color-destructive))`
6. `--ui-checkbox-check-color` default: docs say `var(--color-primary-foreground)` but tailwind.css says `white` (hardcoded) — fix to `white`
7. `--ui-checkbox-radius` default: docs say `var(--radius-sm)` but tailwind.css says `0.25rem` — fix to `0.25rem`
8. `--ui-checkbox-ring` default: docs say `var(--color-ring)` — should be `var(--color-ring, var(--ui-color-ring))`
9. `--ui-checkbox-text-error` default: docs say `var(--color-destructive)` — should be `var(--color-destructive, var(--ui-color-destructive))`
10. MISSING: `--ui-checkbox-size-sm`, `--ui-checkbox-size-md`, `--ui-checkbox-size-lg`
11. MISSING: `--ui-checkbox-border-width`
12. MISSING: `--ui-checkbox-font-size-sm`, `--ui-checkbox-font-size-md`, `--ui-checkbox-font-size-lg`
13. MISSING: `--ui-checkbox-bg-indeterminate`, `--ui-checkbox-border-indeterminate`

Events are already accurate — both lui-checkbox ui-change and lui-checkbox-group ui-change (from select-all) are documented. No changes needed there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand checkboxCSSVars in CheckboxPage.tsx to full token set</name>
  <files>apps/docs/src/pages/components/CheckboxPage.tsx</files>
  <action>
In apps/docs/src/pages/components/CheckboxPage.tsx, replace the checkboxCSSVars array with the complete token set matching the tailwind.css :root block exactly.

Replace the current 12-entry array with this expanded set (24 entries), organized by category — layout/size first, then color tokens:

```typescript
const checkboxCSSVars: CSSVarDef[] = [
  // Size
  { name: '--ui-checkbox-size-sm', default: '1rem', description: 'Checkbox box width and height at sm size.' },
  { name: '--ui-checkbox-size-md', default: '1.25rem', description: 'Checkbox box width and height at md size (default).' },
  { name: '--ui-checkbox-size-lg', default: '1.5rem', description: 'Checkbox box width and height at lg size.' },
  // Layout
  { name: '--ui-checkbox-radius', default: '0.25rem', description: 'Border radius of the checkbox box.' },
  { name: '--ui-checkbox-border-width', default: '2px', description: 'Border width of the checkbox box.' },
  { name: '--ui-checkbox-label-gap', default: '0.5rem', description: 'Gap between checkbox box and label text.' },
  { name: '--ui-checkbox-transition', default: '150ms', description: 'Transition duration for check/uncheck animations.' },
  { name: '--ui-checkbox-group-gap', default: '0.5rem', description: 'Gap between items in a checkbox group.' },
  // Typography
  { name: '--ui-checkbox-font-size-sm', default: '0.875rem', description: 'Label font size at sm size.' },
  { name: '--ui-checkbox-font-size-md', default: '1rem', description: 'Label font size at md size (default).' },
  { name: '--ui-checkbox-font-size-lg', default: '1.125rem', description: 'Label font size at lg size.' },
  // Unchecked state
  { name: '--ui-checkbox-bg', default: 'var(--color-background, white)', description: 'Background color when unchecked.' },
  { name: '--ui-checkbox-border', default: 'var(--color-border, var(--ui-color-border))', description: 'Border color when unchecked.' },
  // Checked state
  { name: '--ui-checkbox-bg-checked', default: 'var(--color-primary, var(--ui-color-primary))', description: 'Background color when checked.' },
  { name: '--ui-checkbox-border-checked', default: 'var(--color-primary, var(--ui-color-primary))', description: 'Border color when checked.' },
  { name: '--ui-checkbox-check-color', default: 'white', description: 'Checkmark and dash icon color.' },
  // Indeterminate state
  { name: '--ui-checkbox-bg-indeterminate', default: 'var(--color-primary, var(--ui-color-primary))', description: 'Background color in indeterminate state.' },
  { name: '--ui-checkbox-border-indeterminate', default: 'var(--color-primary, var(--ui-color-primary))', description: 'Border color in indeterminate state.' },
  // Focus state
  { name: '--ui-checkbox-ring', default: 'var(--color-ring, var(--ui-color-ring))', description: 'Focus ring color.' },
  // Error state
  { name: '--ui-checkbox-border-error', default: 'var(--color-destructive, var(--ui-color-destructive))', description: 'Border color on validation error.' },
  { name: '--ui-checkbox-text-error', default: 'var(--color-destructive, var(--ui-color-destructive))', description: 'Error message text color.' },
];
```

Do NOT change any other part of the file — no examples, no props tables, no events section, no navigation links.
  </action>
  <verify>
    <automated>grep -c "ui-checkbox-size-sm\|ui-checkbox-border-width\|ui-checkbox-font-size-sm\|ui-checkbox-bg-indeterminate" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/CheckboxPage.tsx</automated>
  </verify>
  <done>checkboxCSSVars has 21 entries covering all tokens from tailwind.css :root checkbox block. grep for any of the 4 previously-missing token names returns at least 4 matches. Default values match tailwind.css :root exactly (double-fallback form for var() tokens, exact values for structural tokens).</done>
</task>

</tasks>

<verification>
1. Run: `grep -c "name: '--ui-checkbox-" apps/docs/src/pages/components/CheckboxPage.tsx` — should return 21 (or count of entries in updated array)
2. Run: `grep "ui-checkbox-size-sm" apps/docs/src/pages/components/CheckboxPage.tsx` — should show the size token entry
3. Run: `grep "ui-checkbox-bg-indeterminate" apps/docs/src/pages/components/CheckboxPage.tsx` — should show the indeterminate token entry
4. Run: `grep "check-color.*white" apps/docs/src/pages/components/CheckboxPage.tsx` — should show default is 'white' not 'var(--color-primary-foreground)'
</verification>

<success_criteria>
- checkboxCSSVars array has 21 entries covering all tokens in the tailwind.css :root checkbox block
- Default values match tailwind.css :root exactly — double-fallback var() form for color tokens, exact rem/px/ms for structural tokens
- --ui-checkbox-check-color default is 'white' (not 'var(--color-primary-foreground)')
- --ui-checkbox-radius default is '0.25rem' (not 'var(--radius-sm)')
- Newly added: size (sm/md/lg), border-width, font-size (sm/md/lg), indeterminate (bg + border) tokens
- CHK-02 satisfied: Checkbox docs page is accurate and up-to-date
</success_criteria>

<output>
After completion, create `.planning/phases/75-checkbox/75-02-SUMMARY.md` with the before count (12), after count, and list of tokens added/corrected.
</output>
