---
phase: 50-documentation
plan: 03
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - apps/docs/src/pages/components/DatePickerPage.tsx
autonomous: true

must_haves:
  truths:
    - "User can see comprehensive Date Picker documentation with interactive examples"
    - "User can see examples for basic usage, min/max constraints, presets, natural language, inline mode, and custom format"
    - "User can read API reference with all 14 props and events"
  artifacts:
    - path: "apps/docs/src/pages/components/DatePickerPage.tsx"
      provides: "Date Picker documentation page"
      min_lines: 300
      contains: "lui-date-picker"
  key_links:
    - from: "apps/docs/src/pages/components/DatePickerPage.tsx"
      to: "@lit-ui/date-picker"
      via: "side-effect import"
      pattern: "import '@lit-ui/date-picker'"
    - from: "apps/docs/src/pages/components/DatePickerPage.tsx"
      to: "apps/docs/src/App.tsx"
      via: "route registration"
      pattern: "DatePickerPage"
---

<objective>
Create the Date Picker documentation page following the established CalendarPage.tsx template.

Purpose: Document the Date Picker component (Phases 44-45) with interactive examples, accessibility notes, CSS custom properties, and complete API reference.
Output: DatePickerPage.tsx with examples demonstrating all key features.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/50-documentation/50-RESEARCH.md
@apps/docs/src/pages/components/CalendarPage.tsx
@packages/date-picker/src/date-picker.ts
@packages/date-picker/src/preset-types.ts
@packages/date-picker/src/natural-language.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DatePickerPage.tsx</name>
  <files>apps/docs/src/pages/components/DatePickerPage.tsx</files>
  <action>
Create `apps/docs/src/pages/components/DatePickerPage.tsx` following the exact CalendarPage.tsx structure: FrameworkProvider wrapper, header, examples with ExampleBlock, accessibility section, CSS custom properties, API reference with PropsTable/EventsTable, and PrevNextNav.

**Imports:**
```tsx
import { FrameworkProvider } from '../../contexts/FrameworkContext';
import { ExampleBlock } from '../../components/ExampleBlock';
import { PropsTable, type PropDef } from '../../components/PropsTable';
import { EventsTable, type EventDef } from '../../components/EventsTable';
import { PrevNextNav } from '../../components/PrevNextNav';
import { CodeBlock } from '../../components/CodeBlock';

import '@lit-ui/date-picker';
```

**Props array** (14 props from research, source: packages/date-picker/src/date-picker.ts @property decorators):
Use the exact props from the research section "Date Picker Props Array". Include all 14: value, name, locale, placeholder, helper-text, min-date, max-date, required, disabled, inline, error, label, presets (JS-only), format (JS-only).

**Events array:**
- `change` - Fired when date is selected. Detail: `{ date: Date | null, isoString: string }`

**CSS Custom Properties** - Extract from date-picker.ts `static styles`. The date picker likely reuses --ui-calendar-* variables plus --ui-input-* variables. List the relevant ones.

**Examples to create (7 examples):**

1. **Basic** - `<lui-date-picker label="Date"></lui-date-picker>` -- same code all frameworks.

2. **Pre-selected Date** - `<lui-date-picker label="Birthday" value="2026-02-14"></lui-date-picker>` -- same code all frameworks.

3. **Date Constraints** - `<lui-date-picker label="Appointment" min-date="2026-01-10" max-date="2026-03-31"></lui-date-picker>` -- same code all frameworks.

4. **Required with Validation** - `<lui-date-picker label="Start Date" required></lui-date-picker>` -- same code all frameworks. Note about form integration and validation messages.

5. **Natural Language Input** - Text description explaining users can type "tomorrow", "next friday", "in 3 days" etc. Example shows the basic component (NL parsing is built-in). Same code all frameworks.

6. **Presets** - Needs framework-specific code since `presets` is a JS-only property (attribute: false). Show HTML with script setting presets=true, React with ref, Vue with ref/onMounted, Svelte with bind:this. Explain that `presets={true}` shows default presets (Today, Tomorrow, Next Week) and custom presets can be passed as an array.

7. **Inline Mode** - `<lui-date-picker label="Date" inline></lui-date-picker>` -- same code all frameworks. Explain calendar is always visible without popup.

**Accessibility section** (numbered list following CalendarPage pattern):
1. Uses native input with label association for screen readers
2. Calendar popup inherits all keyboard navigation from lui-calendar (arrows, Home/End, PageUp/Down)
3. Escape closes popup and returns focus to input
4. aria-expanded on input indicates popup state
5. aria-invalid and error messages linked via aria-describedby
6. Natural language and typed date formats provide alternative input method to calendar

**PrevNextNav:**
```tsx
<PrevNextNav
  prev={{ title: 'Checkbox', href: '/components/checkbox' }}
  next={{ title: 'Date Range Picker', href: '/components/date-range-picker' }}
/>
```

Export as named export: `export function DatePickerPage()`
  </action>
  <verify>
Run `cd /Users/sn0w/Documents/dev/lit-components && npx tsc --noEmit -p apps/docs/tsconfig.json 2>&1 | head -20` to verify compilation.
Verify file exists and has expected structure: `grep -c 'ExampleBlock\|PropsTable\|PrevNextNav' apps/docs/src/pages/components/DatePickerPage.tsx`
  </verify>
  <done>DatePickerPage.tsx exists with 7 interactive examples (basic, pre-selected, constraints, validation, natural language, presets, inline), accessibility section, CSS custom properties, API reference with 14 props and 1 event, and correct PrevNextNav links.</done>
</task>

</tasks>

<verification>
1. DatePickerPage.tsx compiles without TypeScript errors
2. Page follows CalendarPage.tsx structure exactly
3. All 14 props documented in PropsTable
4. 7 examples cover all key features
5. PrevNextNav: prev=Checkbox, next=Date Range Picker
</verification>

<success_criteria>
- User can navigate to /components/date-picker and see complete documentation
- Interactive examples render lui-date-picker elements correctly
- API reference is complete and accurate
</success_criteria>

<output>
After completion, create `.planning/phases/50-documentation/50-03-SUMMARY.md`
</output>
