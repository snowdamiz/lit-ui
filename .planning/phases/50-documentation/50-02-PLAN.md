---
phase: 50-documentation
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - apps/docs/src/pages/components/CalendarPage.tsx
autonomous: true

must_haves:
  truths:
    - "User can see documentation for Phase 42-43 advanced features (gestures, animations, multi-month, week numbers, decade/century views)"
    - "User can see interactive examples for CalendarMulti and advanced calendar features"
    - "User sees updated API reference with all calendar props including advanced ones"
  artifacts:
    - path: "apps/docs/src/pages/components/CalendarPage.tsx"
      provides: "Complete calendar documentation with basic and advanced features"
      contains: "lui-calendar-multi"
  key_links:
    - from: "apps/docs/src/pages/components/CalendarPage.tsx"
      to: "@lit-ui/calendar"
      via: "side-effect import registers custom elements"
      pattern: "import '@lit-ui/calendar'"
---

<objective>
Audit and update CalendarPage.tsx to document Phase 42-43 advanced features that were added after the original documentation was created.

Purpose: The existing CalendarPage only covers basic Phase 42 features (6 props, basic examples). Phase 43 added multi-month display, decade/century views, gesture navigation, animations, week numbers, renderDay callback, and responsive layout. These need documentation.
Output: Complete CalendarPage.tsx with examples for all calendar features.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50-documentation/50-RESEARCH.md
@apps/docs/src/pages/components/CalendarPage.tsx
@packages/calendar/src/calendar.ts
@packages/calendar/src/calendar-multi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit CalendarPage.tsx and add advanced feature documentation</name>
  <files>apps/docs/src/pages/components/CalendarPage.tsx</files>
  <action>
Read the current CalendarPage.tsx and the calendar component source files (`packages/calendar/src/calendar.ts`, `packages/calendar/src/calendar-multi.ts`) to identify all features not yet documented.

Add the following sections/examples to CalendarPage.tsx, preserving all existing content:

**New props to add to `calendarProps` array** (audit `calendar.ts` for @property decorators added in Phase 43):
- `display-month` (string) - Display a specific month without selecting a date (YYYY-MM-DD or YYYY-MM format)
- `hide-navigation` (boolean) - Hide month navigation controls (used by CalendarMulti)
- `show-week-numbers` (boolean) - Show ISO week numbers column
- Any other @property decorators added in Phase 43 that aren't in the existing 6 props

**New examples to add** (after existing examples, before Accessibility section):

1. **Multi-Month Display** - Show `<lui-calendar-multi></lui-calendar-multi>` rendering 2 side-by-side calendars. Include HTML/React/Vue/Svelte code variants. Note: lui-calendar-multi needs a JSX type declaration added to CalendarPage (local to the file or to LivePreview.tsx -- check if it was added in 50-01, if not add inline declaration at top of file).

2. **Week Numbers** - Show `<lui-calendar show-week-numbers></lui-calendar>` with ISO week numbers visible. Simple attribute-only example, same code for all frameworks.

3. **Decade/Century Navigation** - Add a text note explaining that clicking the month/year heading drills into year view, then decade view, then century view. No interactive example needed since this is built-in behavior, but mention keyboard shortcuts (Escape to go back up).

**Update Accessibility section** to mention:
- Swipe gesture support on touch devices
- prefers-reduced-motion respected for animations
- Week number buttons are keyboard-accessible

**Update Events section** to add `week-select` event if it exists.

**Add lui-calendar-multi JSX type** to LivePreview.tsx if not already present. Check if `lui-calendar-multi` was included in 50-01. If not, add it to LivePreview.tsx:
```ts
'lui-calendar-multi': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    value?: string;
    locale?: string;
    'min-date'?: string;
    'max-date'?: string;
    months?: number;
    'show-week-numbers'?: boolean;
  },
  HTMLElement
>;
```

Follow the exact same page structure and Tailwind classes as the existing CalendarPage sections. Each new example uses `<section>` with `<h3>`, description `<p>`, and `<ExampleBlock>`.
  </action>
  <verify>
Run `cd /Users/sn0w/Documents/dev/lit-components && npx tsc --noEmit -p apps/docs/tsconfig.json 2>&1 | head -20` to verify compilation.
Verify the page has examples for multi-month, week numbers by grepping: `grep -c 'lui-calendar-multi\|show-week-numbers\|Decade' apps/docs/src/pages/components/CalendarPage.tsx`
  </verify>
  <done>CalendarPage.tsx documents all Phase 42-43 features including multi-month display, week numbers, decade/century views, gesture navigation, and animations. Props array is complete with all @property decorators from the source.</done>
</task>

</tasks>

<verification>
1. CalendarPage.tsx compiles without TypeScript errors
2. Page includes multi-month example with lui-calendar-multi
3. Page includes week numbers example
4. Props array includes display-month, hide-navigation, show-week-numbers
5. Accessibility section mentions gesture/animation support
</verification>

<success_criteria>
- User can see documentation for all Phase 42-43 calendar features
- Interactive examples render correctly for CalendarMulti and week numbers
- API reference is complete with all calendar props
</success_criteria>

<output>
After completion, create `.planning/phases/50-documentation/50-02-SUMMARY.md`
</output>
