---
phase: 50-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/src/components/LivePreview.tsx
  - apps/docs/src/nav.ts
  - apps/docs/src/App.tsx
  - packages/cli/src/registry/registry.json
  - packages/cli/src/utils/install-component.ts
  - apps/docs/src/pages/components/ButtonPage.tsx
  - apps/docs/src/pages/components/CalendarPage.tsx
  - apps/docs/src/pages/components/CheckboxPage.tsx
  - apps/docs/src/pages/components/DialogPage.tsx
  - apps/docs/src/pages/components/TextareaPage.tsx
autonomous: true

must_haves:
  truths:
    - "Docs site compiles with lui-date-picker, lui-date-range-picker, lui-time-picker JSX elements"
    - "CLI 'lui list' shows date-picker, date-range-picker, time-picker as available components"
    - "Sidebar navigation shows Date Picker, Date Range Picker, Time Picker in alphabetical position"
    - "PrevNextNav chain is unbroken across all component pages"
  artifacts:
    - path: "apps/docs/src/components/LivePreview.tsx"
      provides: "JSX IntrinsicElements declarations for 3 new custom elements"
      contains: "lui-date-picker"
    - path: "packages/cli/src/registry/registry.json"
      provides: "CLI registry entries for 3 date/time components"
      contains: "date-picker"
    - path: "packages/cli/src/utils/install-component.ts"
      provides: "Component-to-package mappings for 3 new components"
      contains: "date-range-picker"
    - path: "apps/docs/src/nav.ts"
      provides: "Navigation entries for 3 new components and 3 guide pages"
      contains: "Date Picker"
    - path: "apps/docs/src/App.tsx"
      provides: "Route registrations for 3 component pages and 3 guide pages"
      contains: "DatePickerPage"
  key_links:
    - from: "apps/docs/src/nav.ts"
      to: "apps/docs/src/App.tsx"
      via: "href paths match route paths"
      pattern: "components/date-picker"
    - from: "packages/cli/src/registry/registry.json"
      to: "packages/cli/src/utils/install-component.ts"
      via: "component name maps to package name"
      pattern: "date-picker.*@lit-ui/date-picker"
---

<objective>
Infrastructure setup for Phase 50 documentation: JSX type declarations, CLI registry entries, navigation/routing registration, and PrevNextNav chain fixes.

Purpose: Unblock all doc page plans by adding JSX types (required for compilation), CLI entries (required for installability), and navigation/routing (required for page access). Fix PrevNextNav chain to maintain unbroken forward/back navigation across all component pages.
Output: All shared integration files updated. Doc pages can be created in parallel in Wave 2.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-documentation/50-RESEARCH.md
@apps/docs/src/components/LivePreview.tsx
@apps/docs/src/nav.ts
@apps/docs/src/App.tsx
@packages/cli/src/registry/registry.json
@packages/cli/src/utils/install-component.ts
@packages/date-picker/src/jsx.d.ts
@packages/date-range-picker/src/jsx.d.ts
@packages/time-picker/src/jsx.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSX types, component imports, CLI registry entries, and install-component mappings</name>
  <files>
    apps/docs/src/components/LivePreview.tsx
    packages/cli/src/registry/registry.json
    packages/cli/src/utils/install-component.ts
  </files>
  <action>
**LivePreview.tsx** -- Add side-effect imports and JSX IntrinsicElements declarations for all three new components:

1. Add imports at top (after existing `import '@lit-ui/calendar'`):
```ts
import '@lit-ui/date-picker';
import '@lit-ui/date-range-picker';
import '@lit-ui/time-picker';
```

2. Add to IntrinsicElements inside the existing `declare global` block (after `lui-switch`):

```ts
'lui-date-picker': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    value?: string;
    name?: string;
    locale?: string;
    placeholder?: string;
    'helper-text'?: string;
    'min-date'?: string;
    'max-date'?: string;
    label?: string;
    error?: string;
    required?: boolean;
    disabled?: boolean;
    inline?: boolean;
  },
  HTMLElement
>;
'lui-date-range-picker': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    'start-date'?: string;
    'end-date'?: string;
    name?: string;
    locale?: string;
    placeholder?: string;
    'helper-text'?: string;
    'min-date'?: string;
    'max-date'?: string;
    'min-days'?: number;
    'max-days'?: number;
    label?: string;
    error?: string;
    required?: boolean;
    disabled?: boolean;
    comparison?: boolean;
    'compare-start-date'?: string;
    'compare-end-date'?: string;
  },
  HTMLElement
>;
'lui-time-picker': React.DetailedHTMLProps<
  React.HTMLAttributes<HTMLElement> & {
    value?: string;
    name?: string;
    label?: string;
    placeholder?: string;
    required?: boolean;
    disabled?: boolean;
    readonly?: boolean;
    hour12?: boolean;
    locale?: string;
    step?: number;
    'min-time'?: string;
    'max-time'?: string;
    'allow-overnight'?: boolean;
    'show-timezone'?: boolean;
    timezone?: string;
    voice?: boolean;
    'interface-mode'?: 'clock' | 'dropdown' | 'both' | 'wheel' | 'range';
  },
  HTMLElement
>;
```

Note: Exclude JS-only properties (presets, format, businessHours, additionalTimezones) from JSX attributes per the pattern established in Phase 45-05/48-06. These are set via ref in React.

**registry.json** -- Add 3 entries to the `components` array (after the "calendar" entry):

```json
{
  "name": "date-picker",
  "description": "Accessible date picker with calendar popup, text input parsing, and form participation",
  "files": [
    { "path": "components/date-picker/date-picker.ts", "type": "component" },
    { "path": "components/date-picker/date-input-parser.ts", "type": "component" },
    { "path": "components/date-picker/natural-language.ts", "type": "component" },
    { "path": "components/date-picker/preset-types.ts", "type": "component" }
  ],
  "dependencies": ["date-fns", "@floating-ui/dom"],
  "registryDependencies": ["calendar"]
},
{
  "name": "date-range-picker",
  "description": "Accessible date range picker with dual calendars, comparison mode, and ISO 8601 interval submission",
  "files": [
    { "path": "components/date-range-picker/date-range-picker.ts", "type": "component" },
    { "path": "components/date-range-picker/range-utils.ts", "type": "component" },
    { "path": "components/date-range-picker/range-preset-types.ts", "type": "component" }
  ],
  "dependencies": ["date-fns", "@floating-ui/dom"],
  "registryDependencies": ["calendar"]
},
{
  "name": "time-picker",
  "description": "Accessible time picker with clock face, dropdown, voice input, and timezone support",
  "files": [
    { "path": "components/time-picker/time-picker.ts", "type": "component" },
    { "path": "components/time-picker/time-input.ts", "type": "component" },
    { "path": "components/time-picker/clock-face.ts", "type": "component" },
    { "path": "components/time-picker/time-dropdown.ts", "type": "component" },
    { "path": "components/time-picker/time-utils.ts", "type": "component" },
    { "path": "components/time-picker/time-presets.ts", "type": "component" },
    { "path": "components/time-picker/timezone-display.ts", "type": "component" },
    { "path": "components/time-picker/time-range-slider.ts", "type": "component" },
    { "path": "components/time-picker/time-scroll-wheel.ts", "type": "component" },
    { "path": "components/time-picker/time-voice-input.ts", "type": "component" }
  ],
  "dependencies": ["@floating-ui/dom"],
  "registryDependencies": ["calendar"]
}
```

**install-component.ts** -- Add 3 entries to the `componentToPackage` map (after the `switch` entry):

```ts
calendar: '@lit-ui/calendar',
'date-picker': '@lit-ui/date-picker',
'date-range-picker': '@lit-ui/date-range-picker',
'time-picker': '@lit-ui/time-picker',
```

Note: Calendar is already imported in LivePreview.tsx but missing from `componentToPackage`. Add it too.
  </action>
  <verify>
Run `cd /Users/sn0w/Documents/dev/lit-components && npx tsc --noEmit -p apps/docs/tsconfig.json 2>&1 | head -20` to verify JSX types compile.
Run `node -e "const r = require('./packages/cli/src/registry/registry.json'); console.log(r.components.map(c=>c.name))"` to verify registry entries.
  </verify>
  <done>LivePreview.tsx has JSX declarations for lui-date-picker, lui-date-range-picker, lui-time-picker. registry.json has 3 new component entries with correct dependencies and registryDependencies. install-component.ts maps all 4 date/time component names to packages.</done>
</task>

<task type="auto">
  <name>Task 2: Update navigation, routing, and PrevNextNav chain</name>
  <files>
    apps/docs/src/nav.ts
    apps/docs/src/App.tsx
    apps/docs/src/pages/components/ButtonPage.tsx
    apps/docs/src/pages/components/CheckboxPage.tsx
    apps/docs/src/pages/components/DialogPage.tsx
    apps/docs/src/pages/components/TextareaPage.tsx
  </files>
  <action>
**nav.ts** -- Update the Components section to add 3 new entries in alphabetical position. Update the Guides section to add 3 new guide entries:

Components section (full list, alphabetical):
```ts
{ title: "Button", href: "/components/button" },
{ title: "Calendar", href: "/components/calendar" },
{ title: "Checkbox", href: "/components/checkbox" },
{ title: "Date Picker", href: "/components/date-picker" },
{ title: "Date Range Picker", href: "/components/date-range-picker" },
{ title: "Dialog", href: "/components/dialog" },
{ title: "Input", href: "/components/input" },
{ title: "Radio", href: "/components/radio" },
{ title: "Select", href: "/components/select" },
{ title: "Switch", href: "/components/switch" },
{ title: "Textarea", href: "/components/textarea" },
{ title: "Time Picker", href: "/components/time-picker" },
```

Guides section (add alphabetically before existing entries):
```ts
{ title: "Accessibility", href: "/guides/accessibility" },
{ title: "Agent Skills", href: "/guides/agent-skills" },
{ title: "Form Integration", href: "/guides/form-integration" },
{ title: "Internationalization", href: "/guides/i18n" },
{ title: "Migration", href: "/guides/migration" },
{ title: "SSR Setup", href: "/guides/ssr" },
{ title: "Styling", href: "/guides/styling" },
```

**App.tsx** -- Add lazy imports and Route elements for all 6 new pages:

Imports (add after existing component page imports). Note: Guide pages go in `pages/` (not `pages/guides/`) matching the existing pattern from `SSRGuide.tsx`, `MigrationGuide.tsx`, `StylingPage.tsx`. Verify with `ls apps/docs/src/pages/*Guide.tsx` before creating.
```tsx
import { DatePickerPage } from './pages/components/DatePickerPage'
import { DateRangePickerPage } from './pages/components/DateRangePickerPage'
import { TimePickerPage } from './pages/components/TimePickerPage'
import { AccessibilityGuide } from './pages/AccessibilityGuide'
import { FormIntegrationGuide } from './pages/FormIntegrationGuide'
import { I18nGuide } from './pages/I18nGuide'
```

Routes (add in Components section after calendar route):
```tsx
<Route path="components/date-picker" element={<DatePickerPage />} />
<Route path="components/date-range-picker" element={<DateRangePickerPage />} />
<Route path="components/time-picker" element={<TimePickerPage />} />
```

Routes (add in Guides section after agent-skills route):
```tsx
<Route path="guides/accessibility" element={<AccessibilityGuide />} />
<Route path="guides/form-integration" element={<FormIntegrationGuide />} />
<Route path="guides/i18n" element={<I18nGuide />} />
```

**PrevNextNav updates** -- Fix the chain to be correct with new pages inserted. The full alphabetical chain is:
Button -> Calendar -> Checkbox -> Date Picker -> Date Range Picker -> Dialog -> Input -> Radio -> Select -> Switch -> Textarea -> Time Picker

Pages to update:

1. **ButtonPage.tsx**: Change `next` from `{ title: 'Checkbox', href: '/components/checkbox' }` to `{ title: 'Calendar', href: '/components/calendar' }` (fix existing skip)

2. **CheckboxPage.tsx**: Change `prev` from `{ title: 'Button', href: '/components/button' }` to `{ title: 'Calendar', href: '/components/calendar' }`. Change `next` from `{ title: 'Dialog', href: '/components/dialog' }` to `{ title: 'Date Picker', href: '/components/date-picker' }`.

3. **DialogPage.tsx**: Change `prev` from `{ title: 'Checkbox', href: '/components/checkbox' }` to `{ title: 'Date Range Picker', href: '/components/date-range-picker' }`.

4. **TextareaPage.tsx**: Change `next` from `{ title: 'Theme Configurator', href: '/configurator' }` to `{ title: 'Time Picker', href: '/components/time-picker' }`.

Note: CalendarPage.tsx PrevNextNav (prev=Button, next=Checkbox) is correct and does NOT need updating. InputPage.tsx (prev=Dialog, next=Radio), RadioPage.tsx, SelectPage.tsx, SwitchPage.tsx are all unchanged.
  </action>
  <verify>
Grep all PrevNextNav usages in component pages to verify the chain: `grep -A2 'PrevNextNav' apps/docs/src/pages/components/*.tsx`
Verify nav.ts has 12 component entries: `grep -c 'href.*components' apps/docs/src/nav.ts`
Verify App.tsx has all route paths: `grep 'path="components/' apps/docs/src/App.tsx | wc -l` should be 12.
  </verify>
  <done>Nav sidebar shows all 12 components alphabetically + 7 guide entries. App.tsx has routes for all 6 new pages (3 components + 3 guides). PrevNextNav chain is unbroken: Button->Calendar->Checkbox->Date Picker->Date Range Picker->Dialog->Input->Radio->Select->Switch->Textarea->Time Picker.</done>
</task>

</tasks>

<verification>
1. `cd apps/docs && npx tsc --noEmit` compiles without errors (JSX types valid)
2. registry.json is valid JSON with 12 component entries
3. nav.ts Components section has 12 entries in alphabetical order
4. App.tsx imports and routes exist for DatePickerPage, DateRangePickerPage, TimePickerPage, AccessibilityGuide, FormIntegrationGuide, I18nGuide
5. PrevNextNav chain is consistent across all component pages
</verification>

<success_criteria>
- Docs site compiles with new JSX type declarations
- CLI registry has date-picker, date-range-picker, time-picker entries with correct dependencies
- Sidebar navigation shows all 12 components alphabetically
- All 6 new routes registered in App.tsx
- PrevNextNav chain unbroken across all component pages
</success_criteria>

<output>
After completion, create `.planning/phases/50-documentation/50-01-SUMMARY.md`
</output>
