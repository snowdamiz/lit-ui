---
phase: 50-documentation
plan: 05
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - apps/docs/src/pages/components/TimePickerPage.tsx
autonomous: true

must_haves:
  truths:
    - "User can see comprehensive Time Picker documentation with interactive examples"
    - "User can see examples for basic usage, 12/24h format, clock/dropdown modes, timezone, voice input, presets, and business hours"
    - "User can read API reference with all 20 props and events"
    - "User can see sub-component documentation for publicly exported components"
  artifacts:
    - path: "apps/docs/src/pages/components/TimePickerPage.tsx"
      provides: "Time Picker documentation page"
      min_lines: 400
      contains: "lui-time-picker"
  key_links:
    - from: "apps/docs/src/pages/components/TimePickerPage.tsx"
      to: "@lit-ui/time-picker"
      via: "side-effect import"
      pattern: "import '@lit-ui/time-picker'"
---

<objective>
Create the Time Picker documentation page following the established CalendarPage.tsx template.

Purpose: Document the Time Picker component (Phases 48-49) with interactive examples, accessibility notes, CSS custom properties, sub-component documentation, and complete API reference. This is the most feature-rich component with 20 props and multiple interface modes.
Output: TimePickerPage.tsx with examples demonstrating all key features.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/50-documentation/50-RESEARCH.md
@apps/docs/src/pages/components/CalendarPage.tsx
@packages/time-picker/src/time-picker.ts
@packages/time-picker/src/time-presets.ts
@packages/time-picker/src/time-utils.ts
@packages/time-picker/src/timezone-display.ts
@packages/time-picker/src/time-range-slider.ts
@packages/time-picker/src/time-scroll-wheel.ts
@packages/time-picker/src/time-voice-input.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TimePickerPage.tsx</name>
  <files>apps/docs/src/pages/components/TimePickerPage.tsx</files>
  <action>
Create `apps/docs/src/pages/components/TimePickerPage.tsx` following CalendarPage.tsx structure.

**Imports:**
```tsx
import { FrameworkProvider } from '../../contexts/FrameworkContext';
import { ExampleBlock } from '../../components/ExampleBlock';
import { PropsTable, type PropDef } from '../../components/PropsTable';
import { EventsTable, type EventDef } from '../../components/EventsTable';
import { PrevNextNav } from '../../components/PrevNextNav';
import { CodeBlock } from '../../components/CodeBlock';

import '@lit-ui/time-picker';
```

**Props array** (20 props from research): value, name, label, placeholder, required, disabled, readonly, hour12, locale, step, min-time, max-time, allow-overnight, show-timezone, timezone, voice, interface-mode. Plus JS-only: presets, businessHours, additionalTimezones.

**Events array:**
- `change` - Fired when time is selected. Detail: `{ value: string, timeValue: TimeValue | null }`

**CSS Custom Properties** - Extract from time-picker.ts `static styles`. Include --ui-time-picker-* variables.

**Examples to create (9 examples):**

1. **Basic** - `<lui-time-picker label="Time"></lui-time-picker>` -- same all frameworks. Default shows both clock and dropdown.

2. **12-Hour Format** - `<lui-time-picker label="Time" hour12></lui-time-picker>` -- same all frameworks. Show AM/PM toggle.

3. **24-Hour Format** - `<lui-time-picker label="Time" hour12="false"></lui-time-picker>` -- same all frameworks.

4. **Clock Face Only** - `<lui-time-picker label="Time" interface-mode="clock"></lui-time-picker>` -- same all frameworks.

5. **Dropdown Only** - `<lui-time-picker label="Time" interface-mode="dropdown" step="15"></lui-time-picker>` -- same all frameworks. Show step intervals.

6. **Time Constraints** - `<lui-time-picker label="Appointment" min-time="09:00" max-time="17:00" step="30"></lui-time-picker>` -- same all frameworks. Business hours constraint.

7. **Timezone Display** - `<lui-time-picker label="Meeting" show-timezone timezone="America/New_York"></lui-time-picker>` -- same all frameworks. Show additional timezones via JS property (framework-specific for additionalTimezones array).

8. **Presets** - Framework-specific code (JS-only). Show `presets = true` for default presets (Morning, Afternoon, Evening, Now). HTML script, React ref, Vue ref/onMounted, Svelte bind:this.

9. **Voice Input** - `<lui-time-picker label="Time" voice></lui-time-picker>` -- same all frameworks. Note: requires browser Speech API support, shows microphone button when available.

**Sub-Components section** (after examples, before API Reference):
Add a section documenting the 4 publicly exported sub-components per Phase 43-08 decision:
- **TimezoneDisplay** (`lui-timezone-display`) - Standalone timezone comparison display
- **TimeRangeSlider** (`lui-time-range-slider`) - Visual time range slider with dual handles
- **TimeScrollWheel** (`lui-time-scroll-wheel`) - iOS-style scroll wheel time selector
- **TimeVoiceInput** (`lui-time-voice-input`) - Voice-activated time input

For each, provide a brief description (1-2 sentences) and note it can be used standalone via `interface-mode` or imported directly. No full PropsTable needed -- just mention key props inline.

**Accessibility section:**
1. TimeInput uses role="spinbutton" with aria-valuemin/max/now for hour, minute, second spinners
2. Arrow Up/Down increments/decrements values with proper wrapping
3. Clock face supports pointer events for mouse/touch selection
4. Dropdown uses role="listbox" with role="option" items
5. Tab cycles through input fields (hour, minute, AM/PM)
6. Voice input provides progressive enhancement (renders nothing without Speech API)
7. Timezone display uses role="status" for screen reader updates

**PrevNextNav:**
```tsx
<PrevNextNav
  prev={{ title: 'Textarea', href: '/components/textarea' }}
  next={{ title: 'Theme Configurator', href: '/configurator' }}
/>
```

Export as: `export function TimePickerPage()`
  </action>
  <verify>
Run `cd /Users/sn0w/Documents/dev/lit-components && npx tsc --noEmit -p apps/docs/tsconfig.json 2>&1 | head -20`
Verify structure: `grep -c 'ExampleBlock\|PropsTable\|PrevNextNav\|Sub-Component' apps/docs/src/pages/components/TimePickerPage.tsx`
  </verify>
  <done>TimePickerPage.tsx exists with 9 examples covering all interface modes and features, sub-components section for 4 exported sub-components, accessibility section, CSS custom properties, API reference with 20 props and 1 event, and correct PrevNextNav links.</done>
</task>

</tasks>

<verification>
1. TimePickerPage.tsx compiles without TypeScript errors
2. All 20 props documented in PropsTable
3. 9 examples cover all key features and interface modes
4. Sub-Components section documents 4 exported sub-components
5. PrevNextNav: prev=Textarea, next=Theme Configurator
</verification>

<success_criteria>
- User can navigate to /components/time-picker and see complete documentation
- Interactive examples render lui-time-picker in all interface modes
- Sub-component documentation covers all publicly exported components
</success_criteria>

<output>
After completion, create `.planning/phases/50-documentation/50-05-SUMMARY.md`
</output>
