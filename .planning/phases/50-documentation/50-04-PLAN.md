---
phase: 50-documentation
plan: 04
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - apps/docs/src/pages/components/DateRangePickerPage.tsx
autonomous: true

must_haves:
  truths:
    - "User can see comprehensive Date Range Picker documentation with interactive examples"
    - "User can see examples for basic usage, constraints, presets, comparison mode, and drag selection"
    - "User can read API reference with all props and events"
  artifacts:
    - path: "apps/docs/src/pages/components/DateRangePickerPage.tsx"
      provides: "Date Range Picker documentation page"
      min_lines: 300
      contains: "lui-date-range-picker"
  key_links:
    - from: "apps/docs/src/pages/components/DateRangePickerPage.tsx"
      to: "@lit-ui/date-range-picker"
      via: "side-effect import"
      pattern: "import '@lit-ui/date-range-picker'"
---

<objective>
Create the Date Range Picker documentation page following the established CalendarPage.tsx template.

Purpose: Document the Date Range Picker component (Phases 46-47) with interactive examples, accessibility notes, CSS custom properties, and complete API reference.
Output: DateRangePickerPage.tsx with examples demonstrating all key features.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/50-documentation/50-RESEARCH.md
@apps/docs/src/pages/components/CalendarPage.tsx
@packages/date-range-picker/src/date-range-picker.ts
@packages/date-range-picker/src/range-preset-types.ts
@packages/date-range-picker/src/range-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DateRangePickerPage.tsx</name>
  <files>apps/docs/src/pages/components/DateRangePickerPage.tsx</files>
  <action>
Create `apps/docs/src/pages/components/DateRangePickerPage.tsx` following the CalendarPage.tsx structure.

**Imports:**
```tsx
import { FrameworkProvider } from '../../contexts/FrameworkContext';
import { ExampleBlock } from '../../components/ExampleBlock';
import { PropsTable, type PropDef } from '../../components/PropsTable';
import { EventsTable, type EventDef } from '../../components/EventsTable';
import { PrevNextNav } from '../../components/PrevNextNav';
import { CodeBlock } from '../../components/CodeBlock';

import '@lit-ui/date-range-picker';
```

**Props array** (17 props from research): start-date, end-date, name, locale, placeholder, label, helper-text, min-date, max-date, min-days, max-days, required, disabled, error, comparison, compare-start-date, compare-end-date. Plus JS-only: presets.

**Events array:**
- `change` - Fired when range selection is complete. Detail: `{ startDate: string, endDate: string, isoInterval: string, compareStartDate?: string, compareEndDate?: string, compareIsoInterval?: string }`

**CSS Custom Properties** - Extract from date-range-picker.ts `static styles`. Include --ui-range-* variables for range highlighting colors.

**Examples to create (6 examples):**

1. **Basic** - `<lui-date-range-picker label="Date Range"></lui-date-range-picker>` -- same code all frameworks. Explain two-click selection: first click sets start, second click sets end.

2. **Pre-selected Range** - `<lui-date-range-picker label="Booking" start-date="2026-02-10" end-date="2026-02-17"></lui-date-range-picker>` -- same code all frameworks.

3. **Date and Duration Constraints** - `<lui-date-range-picker label="Trip" min-date="2026-01-01" max-date="2026-12-31" min-days="3" max-days="14"></lui-date-range-picker>` -- same code all frameworks. Explain min-days/max-days enforce duration limits.

4. **Presets** - Framework-specific code (JS-only property). Show setting `presets = true` for default presets (Last 7 Days, Last 30 Days, This Month, etc.) via HTML script, React ref, Vue ref/onMounted, Svelte bind:this. Mention custom preset arrays.

5. **Comparison Mode** - `<lui-date-range-picker label="Analytics" comparison></lui-date-range-picker>` -- same code all frameworks. Explain comparison mode allows selecting two ranges for side-by-side analysis.

6. **Drag Selection** - Text-only description (no separate example needed). Explain that users can click and drag from start to end date on desktop for faster range selection. This is built-in behavior.

**Accessibility section:**
1. Two-click selection announced via aria-live: "Start date selected: [date]", "Range selected: [start] to [end]"
2. Dual calendars share keyboard navigation from lui-calendar
3. Escape closes popup, returns focus to input
4. Range duration displayed in popup footer for confirmation
5. Drag selection fires same events as click selection
6. Form value submitted as ISO 8601 interval format (YYYY-MM-DD/YYYY-MM-DD)

**PrevNextNav:**
```tsx
<PrevNextNav
  prev={{ title: 'Date Picker', href: '/components/date-picker' }}
  next={{ title: 'Dialog', href: '/components/dialog' }}
/>
```

Export as: `export function DateRangePickerPage()`
  </action>
  <verify>
Run `cd /Users/sn0w/Documents/dev/lit-components && npx tsc --noEmit -p apps/docs/tsconfig.json 2>&1 | head -20`
Verify structure: `grep -c 'ExampleBlock\|PropsTable\|PrevNextNav' apps/docs/src/pages/components/DateRangePickerPage.tsx`
  </verify>
  <done>DateRangePickerPage.tsx exists with 6 examples (basic, pre-selected, constraints, presets, comparison, drag), accessibility section, CSS custom properties, API reference with 18 props and 1 event, and correct PrevNextNav links.</done>
</task>

</tasks>

<verification>
1. DateRangePickerPage.tsx compiles without TypeScript errors
2. All props from research documented in PropsTable
3. Examples cover all key features including comparison mode
4. PrevNextNav: prev=Date Picker, next=Dialog
</verification>

<success_criteria>
- User can navigate to /components/date-range-picker and see complete documentation
- Interactive examples render lui-date-range-picker correctly
- Comparison mode and presets have framework-specific code examples
</success_criteria>

<output>
After completion, create `.planning/phases/50-documentation/50-04-SUMMARY.md`
</output>
