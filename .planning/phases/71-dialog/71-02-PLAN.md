---
phase: 71-dialog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/src/pages/components/DialogPage.tsx
autonomous: true
requirements: [DLG-02]

must_haves:
  truths:
    - "CSS Custom Properties table lists user-facing --ui-dialog-* tokens with correct names (no --lui-* prefix)"
    - "CSS example code uses correct --ui-dialog-* property names"
    - "CSS vars count badge reflects the full set of documented properties"
  artifacts:
    - path: "apps/docs/src/pages/components/DialogPage.tsx"
      provides: "Accurate Dialog docs page with correct CSS token names"
      contains: "--ui-dialog-radius"
  key_links:
    - from: "apps/docs/src/pages/components/DialogPage.tsx (dialogCSSVars array)"
      to: "packages/core/src/styles/tailwind.css (:root --ui-dialog-* tokens)"
      via: "CSS property names matching tailwind.css token names"
      pattern: "ui-dialog-radius"
---

<objective>
Update the Dialog docs page (DialogPage.tsx) to reflect the accurate --ui-dialog-* CSS custom property names and a comprehensive token set.

Purpose: The docs page currently documents --lui-dialog-radius, --lui-dialog-shadow, --lui-dialog-padding (old --lui- prefix, only 3 tokens). The actual CSS tokens in tailwind.css :root use the --ui-dialog-* prefix and cover layout, color, typography, and spacing dimensions. The cssVarsCode example also uses the old --lui-dialog-* prefix. This is stale and incorrect — the same issue that was fixed for Button in Phase 70-02.

Output: DialogPage.tsx CSS Custom Properties table and example code updated to use correct --ui-dialog-* names and a representative set of documented tokens matching what's in tailwind.css.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-theme-foundation/THEME-SPEC.md
@.planning/phases/70-button/70-02-SUMMARY.md

<interfaces>
<!-- Current state of DialogPage.tsx CSS vars data — these need updating -->

Current dialogCSSVars array (WRONG — old --lui- prefix, only 3 tokens):
```tsx
const dialogCSSVars: CSSVarDef[] = [
  { name: '--lui-dialog-radius', default: 'var(--radius-lg)', description: 'Border radius of the dialog content.' },
  { name: '--lui-dialog-shadow', default: 'var(--shadow-lg)', description: 'Box shadow of the dialog content.' },
  { name: '--lui-dialog-padding', default: 'var(--spacing-6)', description: 'Inner padding of the dialog content.' },
];
```

Current cssVarsCode (WRONG — old --lui- prefix):
```tsx
const cssVarsCode = `/* Global override - affects all dialogs */
:root {
  --lui-dialog-radius: 1rem;
  --lui-dialog-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.4);
  --lui-dialog-padding: 2rem;
}

/* Scoped override - only affects dialogs in this container */
.compact-dialogs {
  --lui-dialog-padding: 1rem;
  --lui-dialog-radius: 0.5rem;
}`;
```

Actual tokens in tailwind.css :root (from THEME-SPEC.md — use these):
Layout: --ui-dialog-radius (0.5rem), --ui-dialog-shadow (shadow-lg value), --ui-dialog-padding (1.5rem), --ui-dialog-max-width-sm (24rem), --ui-dialog-max-width-md (28rem), --ui-dialog-max-width-lg (32rem)
Colors: --ui-dialog-bg (var(--color-card, var(--ui-color-card))), --ui-dialog-text (var(--color-card-foreground, var(--ui-color-card-foreground))), --ui-dialog-backdrop (rgba(0,0,0,0.5))
Typography: --ui-dialog-title-size (1.125rem), --ui-dialog-title-weight (600), --ui-dialog-body-color (var(--color-muted-foreground, var(--ui-color-muted-foreground)))
Spacing: --ui-dialog-header-margin (1rem), --ui-dialog-footer-margin (1.5rem), --ui-dialog-footer-gap (0.75rem)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS vars data and example code in DialogPage.tsx</name>
  <files>apps/docs/src/pages/components/DialogPage.tsx</files>
  <action>
In apps/docs/src/pages/components/DialogPage.tsx, make two targeted changes:

**Change 1 — dialogCSSVars array:** Replace the 3-entry array using old --lui-* prefix with a comprehensive set of user-facing --ui-dialog-* tokens. Document the high-value layout, color, typography, and spacing tokens — keep it focused on tokens users are most likely to override (aim for ~10-12 entries, not every single token):

```tsx
const dialogCSSVars: CSSVarDef[] = [
  { name: '--ui-dialog-radius', default: '0.5rem', description: 'Border radius of the dialog content panel.' },
  { name: '--ui-dialog-shadow', default: '0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)', description: 'Box shadow of the dialog content panel.' },
  { name: '--ui-dialog-padding', default: '1.5rem', description: 'Inner padding of the dialog content panel.' },
  { name: '--ui-dialog-max-width-sm', default: '24rem', description: 'Max-width for size="sm".' },
  { name: '--ui-dialog-max-width-md', default: '28rem', description: 'Max-width for size="md".' },
  { name: '--ui-dialog-max-width-lg', default: '32rem', description: 'Max-width for size="lg".' },
  { name: '--ui-dialog-bg', default: 'var(--color-card, var(--ui-color-card))', description: 'Background color of the dialog content panel.' },
  { name: '--ui-dialog-text', default: 'var(--color-card-foreground, var(--ui-color-card-foreground))', description: 'Text color of the dialog content panel.' },
  { name: '--ui-dialog-backdrop', default: 'rgba(0, 0, 0, 0.5)', description: 'Color of the backdrop overlay behind the dialog.' },
  { name: '--ui-dialog-title-size', default: '1.125rem', description: 'Font size of the dialog title.' },
  { name: '--ui-dialog-title-weight', default: '600', description: 'Font weight of the dialog title.' },
  { name: '--ui-dialog-body-color', default: 'var(--color-muted-foreground, var(--ui-color-muted-foreground))', description: 'Text color of the dialog body content.' },
];
```

**Change 2 — cssVarsCode string:** Update the --lui-dialog-* references to --ui-dialog-* in the example code:

```tsx
const cssVarsCode = `/* Global override - affects all dialogs */
:root {
  --ui-dialog-radius: 1rem;
  --ui-dialog-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.4);
  --ui-dialog-padding: 2rem;
}

/* Scoped override - only affects dialogs in this container */
.compact-dialogs {
  --ui-dialog-padding: 1rem;
  --ui-dialog-radius: 0.5rem;
}`;
```

Do NOT change any other part of DialogPage.tsx — props, slots, events, CSS parts, examples, or layout sections are accurate and should remain unchanged.
  </action>
  <verify>
    <automated>grep -c "lui-dialog" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/DialogPage.tsx</automated>
  </verify>
  <done>Running the grep command returns 0. The dialogCSSVars array has ~12 entries using --ui-dialog-* names. The cssVarsCode string uses --ui-dialog-radius, --ui-dialog-shadow, --ui-dialog-padding.</done>
</task>

</tasks>

<verification>
1. Run: `grep "lui-dialog" apps/docs/src/pages/components/DialogPage.tsx` — should return no matches
2. Run: `grep "ui-dialog-radius" apps/docs/src/pages/components/DialogPage.tsx` — should show correct usage in dialogCSSVars and cssVarsCode
3. Run: `grep -c "ui-dialog" apps/docs/src/pages/components/DialogPage.tsx` — should be > 5 (multiple token references)
</verification>

<success_criteria>
- No --lui-dialog-* references remain in DialogPage.tsx
- CSS Custom Properties table documents layout, color, typography, and spacing tokens using --ui-dialog-* names
- The CSS example code uses correct --ui-dialog-radius, --ui-dialog-shadow, --ui-dialog-padding names
- DLG-02 satisfied: Dialog docs page is accurate and up-to-date
</success_criteria>

<output>
After completion, create `.planning/phases/71-dialog/71-02-SUMMARY.md` with what changed and why (old --lui- prefix → correct --ui- prefix, expanded token list from 3 to ~12 entries).
</output>
