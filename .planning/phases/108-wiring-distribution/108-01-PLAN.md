---
phase: 108-wiring-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - WIRE-01
  - WIRE-02
  - WIRE-03

must_haves:
  truths:
    - "Running `pnpm run knowledge:compile` produces skill/lit-ui-knowledge.xml with no errors"
    - "Running `pnpm run knowledge:render` produces skill/lit-ui-knowledge.png with no errors"
    - "Running `pnpm run knowledge:build` runs compile then render in sequence (stops on failure)"
    - "Both artifacts are committed to git and appear at skill/lit-ui-knowledge.xml and skill/lit-ui-knowledge.png"
    - "Both artifacts are accessible at packages/cli/skill/ for CLI npm distribution"
  artifacts:
    - path: "package.json"
      provides: "Three knowledge:* npm scripts"
      contains: "knowledge:compile"
    - path: "skill/lit-ui-knowledge.xml"
      provides: "Committed XML knowledge artifact"
    - path: "skill/lit-ui-knowledge.png"
      provides: "Committed PNG knowledge artifact"
    - path: "packages/cli/skill"
      provides: "Symlink to root skill/ for CLI distribution"
  key_links:
    - from: "package.json scripts.knowledge:build"
      to: "scripts/compile-knowledge.ts + scripts/render-knowledge-image.ts"
      via: "pnpm run knowledge:compile && pnpm run knowledge:render"
      pattern: "knowledge:compile.*&&.*knowledge:render"
    - from: "packages/cli/skill"
      to: "skill/lit-ui-knowledge.xml and skill/lit-ui-knowledge.png"
      via: "symlink ../../skill"
      pattern: "packages/cli/skill -> ../../skill"
---

<objective>
Wire the three knowledge:* npm scripts into root package.json so both generation scripts are invocable by standard names, then verify that all three distribution requirements are satisfied.

Purpose: Phase 108 closes the v10.1 milestone. The scripts were built in Phases 106 and 107 but have no npm script entries — developers must type full node invocations manually. Adding the three entries is the only code change needed. WIRE-02 (artifacts committed) and WIRE-03 (CLI symlink) are already satisfied from prior phases.

Output: Root package.json with knowledge:compile, knowledge:render, and knowledge:build scripts. End-to-end verification confirming all three WIRE requirements pass.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-wiring-distribution/108-RESEARCH.md

<interfaces>
<!-- Key context extracted from the codebase. No exploration needed. -->

Root package.json current "scripts" block (add three entries to this):
```json
"scripts": {
  "dev": "pnpm -r --parallel run dev",
  "dev:apps": "concurrently -n landing,docs -c blue,green \"pnpm --filter lit-ui-landing dev\" \"pnpm --filter lit-ui-docs dev\"",
  "build": "pnpm -r run build",
  "build:packages": "pnpm --filter './packages/**' run build",
  "test": "pnpm -r run test",
  "lint": "pnpm -r run lint",
  "changeset": "changeset",
  "version": "changeset version",
  "ci:publish": "pnpm build:packages && changeset publish --access public"
}
```

Three scripts to add (exact values, validated in Phase 106/107 runs):
```json
"knowledge:compile": "node --experimental-strip-types scripts/compile-knowledge.ts",
"knowledge:render": "node --experimental-strip-types scripts/render-knowledge-image.ts",
"knowledge:build": "pnpm run knowledge:compile && pnpm run knowledge:render"
```

Repository state confirmed by research:
- skill/lit-ui-knowledge.xml — already committed (commit dfc1ddf)
- skill/lit-ui-knowledge.png — already committed (commit fa16cee)
- packages/cli/skill -> ../../skill — symlink already committed (Feb 27)
- packages/cli/package.json "files": ["dist", "skill"] — already present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add knowledge:* scripts to root package.json</name>
  <files>package.json</files>
  <action>
    Edit the root package.json "scripts" block to add three entries after the existing "ci:publish" entry:

    ```json
    "knowledge:compile": "node --experimental-strip-types scripts/compile-knowledge.ts",
    "knowledge:render": "node --experimental-strip-types scripts/render-knowledge-image.ts",
    "knowledge:build": "pnpm run knowledge:compile && pnpm run knowledge:render"
    ```

    Critical details:
    - Flag placement: `--experimental-strip-types` comes BEFORE the script path (node flag, not script argument)
    - knowledge:build uses `&&` (sequential, stops on failure) NOT `&` (parallel background)
    - knowledge:build calls sub-scripts via `pnpm run` (not `npm run`) for pnpm workspace consistency
    - Script paths are relative to monorepo root (where pnpm scripts execute automatically)

    Do NOT add any other changes — this is the only edit needed for this entire phase.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('/Users/sn0w/Documents/dev/lit-components/package.json', 'utf8')); ['knowledge:compile','knowledge:render','knowledge:build'].forEach(k => { if (!p.scripts[k]) throw new Error('Missing: ' + k); }); console.log('All 3 scripts present:', p.scripts['knowledge:compile'], '|', p.scripts['knowledge:render'], '|', p.scripts['knowledge:build']);"</automated>
  </verify>
  <done>package.json contains all three knowledge:* scripts with the exact values from research. JSON is valid (no parse errors).</done>
</task>

<task type="auto">
  <name>Task 2: Run end-to-end verification of all three WIRE requirements</name>
  <files></files>
  <action>
    Run the following verification sequence from the monorepo root. Each check corresponds to one WIRE requirement.

    **WIRE-01 — Scripts run correctly:**
    Run `pnpm run knowledge:compile` from /Users/sn0w/Documents/dev/lit-components. Expect: exits 0, skill/lit-ui-knowledge.xml present.
    Run `pnpm run knowledge:render` from /Users/sn0w/Documents/dev/lit-components. Expect: exits 0, skill/lit-ui-knowledge.png present.
    Run `pnpm run knowledge:build` from /Users/sn0w/Documents/dev/lit-components. Expect: exits 0, runs both in sequence.

    **WIRE-02 — Artifacts committed in git:**
    Run `git ls-files skill/lit-ui-knowledge.xml skill/lit-ui-knowledge.png` from monorepo root. Expect: both filenames printed (already committed from Phases 106/107).

    **WIRE-03 — Artifacts accessible via CLI path:**
    Run `ls packages/cli/skill/lit-ui-knowledge.xml packages/cli/skill/lit-ui-knowledge.png`. Expect: both files listed (symlink packages/cli/skill -> ../../skill already in place).

    If all checks pass, no further file changes are needed. This task is verification-only — do not modify any files.

    Note on WIRE-02 and WIRE-03: Both are already satisfied from prior phases. If they fail unexpectedly, investigate the symlink and git tracking rather than attempting to "fix" them with copy commands.
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/lit-components && pnpm run knowledge:build && git ls-files skill/lit-ui-knowledge.xml skill/lit-ui-knowledge.png && ls packages/cli/skill/lit-ui-knowledge.xml packages/cli/skill/lit-ui-knowledge.png && echo "ALL WIRE CHECKS PASSED"</automated>
  </verify>
  <done>
    - `pnpm run knowledge:compile` exits 0 and produces skill/lit-ui-knowledge.xml
    - `pnpm run knowledge:render` exits 0 and produces skill/lit-ui-knowledge.png
    - `pnpm run knowledge:build` exits 0 (runs both sequentially)
    - `git ls-files` outputs both artifact paths (WIRE-02 confirmed)
    - `ls packages/cli/skill/` shows both files accessible (WIRE-03 confirmed)
    - Terminal prints "ALL WIRE CHECKS PASSED"
  </done>
</task>

</tasks>

<verification>
All three WIRE requirements verified:
- WIRE-01: `pnpm run knowledge:build` exits 0 and runs both scripts in sequence
- WIRE-02: `git ls-files skill/lit-ui-knowledge.xml skill/lit-ui-knowledge.png` returns both paths
- WIRE-03: `ls packages/cli/skill/lit-ui-knowledge.xml packages/cli/skill/lit-ui-knowledge.png` succeeds
</verification>

<success_criteria>
- Root package.json contains knowledge:compile, knowledge:render, and knowledge:build scripts
- All three scripts execute correctly via pnpm run
- Both artifacts are confirmed committed to git
- Both artifacts are reachable via packages/cli/skill/ symlink
- v10.1 milestone is complete (all XMLC-*, PNGR-*, and WIRE-* requirements satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/108-wiring-distribution/108-01-SUMMARY.md`
</output>
