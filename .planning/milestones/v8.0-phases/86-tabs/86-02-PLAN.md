---
phase: 86-tabs
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/src/pages/components/TabsPage.tsx
autonomous: true
requirements:
  - TAB-02

must_haves:
  truths:
    - "tabsCSSVars array in TabsPage.tsx lists all 25 --ui-tabs-* tokens from tailwind.css :root with accurate double-fallback defaults"
    - "Color token defaults use double-fallback var() form matching tailwind.css :root exactly"
    - "The active-bg default correctly shows var(--color-background, white) not var(--color-background)"
    - "cssVarsCode example uses semantic token variable names"
  artifacts:
    - path: "apps/docs/src/pages/components/TabsPage.tsx"
      provides: "Accurate tabsCSSVars array with 25 entries and double-fallback color defaults"
      contains: "var(--color-muted, var(--ui-color-muted))"
  key_links:
    - from: "apps/docs/src/pages/components/TabsPage.tsx (tabsCSSVars)"
      to: "packages/core/src/styles/tailwind.css (:root tabs block)"
      via: "Default values match tailwind.css :root entries exactly"
      pattern: "var(--color-border, var(--ui-color-border)) form for color tokens"
---

<objective>
Update `tabsCSSVars` in `TabsPage.tsx` to have accurate double-fallback defaults matching `tailwind.css :root`. The current docs use single-var() form for all color token defaults.

Current issues:
1. Color token defaults use single `var()` form (e.g., `var(--color-border)`) instead of double-fallback form (e.g., `var(--color-border, var(--ui-color-border))`)
2. `--ui-tabs-tab-active-bg` shows `var(--color-background)` instead of `var(--color-background, white)`
3. `cssVarsCode` example could reference semantic token variables more accurately

Purpose: Docs accurately reflect all CSS custom properties and their real defaults so developers can override correctly.
Output: `TabsPage.tsx` with a `tabsCSSVars` array of 25 entries (count unchanged) with corrected double-fallback color defaults.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Exact tailwind.css :root tabs token block (ground truth for default values): -->

```css
  --ui-tabs-border: var(--color-border, var(--ui-color-border));

  --ui-tabs-list-bg: var(--color-muted, var(--ui-color-muted));
  --ui-tabs-list-padding: 0.25rem;
  --ui-tabs-list-radius: 0.375rem;
  --ui-tabs-list-gap: 0.25rem;

  --ui-tabs-tab-padding: 0.5rem 1rem;
  --ui-tabs-tab-radius: 0.25rem;
  --ui-tabs-tab-font-size: 0.875rem;
  --ui-tabs-tab-font-weight: 500;
  --ui-tabs-tab-text: var(--color-muted-foreground, var(--ui-color-muted-foreground));
  --ui-tabs-tab-bg: transparent;
  --ui-tabs-tab-hover-text: var(--color-foreground, var(--ui-color-foreground));
  --ui-tabs-tab-hover-bg: transparent;

  --ui-tabs-tab-active-text: var(--color-foreground, var(--ui-color-foreground));
  --ui-tabs-tab-active-bg: var(--color-background, white);
  --ui-tabs-tab-active-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);

  --ui-tabs-panel-padding: 1rem 0;
  --ui-tabs-panel-text: var(--color-foreground, var(--ui-color-foreground));

  --ui-tabs-ring: var(--color-ring, var(--ui-color-ring));

  --ui-tabs-transition: 150ms;

  --ui-tabs-indicator-color: var(--color-primary, var(--ui-color-primary));
  --ui-tabs-indicator-height: 2px;
  --ui-tabs-indicator-radius: 9999px;
  --ui-tabs-indicator-transition: 200ms;

  --ui-tabs-scroll-button-size: 2rem;
```

That is 25 token entries (count is correct in the current docs — only defaults need fixing).

Current stale tabsCSSVars defaults (color tokens that need double-fallback form):
- `--ui-tabs-border`: stale `var(--color-border)` → correct `var(--color-border, var(--ui-color-border))`
- `--ui-tabs-list-bg`: stale `var(--color-muted)` → correct `var(--color-muted, var(--ui-color-muted))`
- `--ui-tabs-tab-text`: stale `var(--color-muted-foreground)` → correct `var(--color-muted-foreground, var(--ui-color-muted-foreground))`
- `--ui-tabs-tab-hover-text`: stale `var(--color-foreground)` → correct `var(--color-foreground, var(--ui-color-foreground))`
- `--ui-tabs-tab-active-text`: stale `var(--color-foreground)` → correct `var(--color-foreground, var(--ui-color-foreground))`
- `--ui-tabs-tab-active-bg`: stale `var(--color-background)` → correct `var(--color-background, white)` (note: literal white fallback per :root)
- `--ui-tabs-panel-text`: stale `var(--color-foreground)` → correct `var(--color-foreground, var(--ui-color-foreground))`
- `--ui-tabs-ring`: stale `var(--color-ring)` → correct `var(--color-ring, var(--ui-color-ring))`
- `--ui-tabs-indicator-color`: stale `var(--color-primary)` → correct `var(--color-primary, var(--ui-color-primary))`

Established pattern from prior phases (e.g. 85-02, 84-02, 83-02): color token defaults use `var(--color-X, var(--ui-color-X))` double-fallback form. Exception: `--ui-tabs-tab-active-bg` uses literal `white` as the second fallback (same as `:root`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tabsCSSVars color defaults to double-fallback form</name>
  <files>apps/docs/src/pages/components/TabsPage.tsx</files>
  <action>
    In `apps/docs/src/pages/components/TabsPage.tsx`, replace the entire `tabsCSSVars` array constant with 25 accurate entries matching tailwind.css :root. The new array must be:

    ```typescript
    const tabsCSSVars: CSSVarDef[] = [
      { name: '--ui-tabs-border', default: 'var(--color-border, var(--ui-color-border))', description: 'Border color.' },
      { name: '--ui-tabs-list-bg', default: 'var(--color-muted, var(--ui-color-muted))', description: 'Tab list background.' },
      { name: '--ui-tabs-list-padding', default: '0.25rem', description: 'Tab list padding.' },
      { name: '--ui-tabs-list-radius', default: '0.375rem', description: 'Tab list border radius.' },
      { name: '--ui-tabs-list-gap', default: '0.25rem', description: 'Gap between tab buttons.' },
      { name: '--ui-tabs-tab-padding', default: '0.5rem 1rem', description: 'Tab button padding.' },
      { name: '--ui-tabs-tab-radius', default: '0.25rem', description: 'Tab button border radius.' },
      { name: '--ui-tabs-tab-font-size', default: '0.875rem', description: 'Tab font size.' },
      { name: '--ui-tabs-tab-font-weight', default: '500', description: 'Tab font weight.' },
      { name: '--ui-tabs-tab-text', default: 'var(--color-muted-foreground, var(--ui-color-muted-foreground))', description: 'Inactive tab text color.' },
      { name: '--ui-tabs-tab-bg', default: 'transparent', description: 'Inactive tab background.' },
      { name: '--ui-tabs-tab-hover-text', default: 'var(--color-foreground, var(--ui-color-foreground))', description: 'Hover tab text color.' },
      { name: '--ui-tabs-tab-hover-bg', default: 'transparent', description: 'Hover tab background.' },
      { name: '--ui-tabs-tab-active-text', default: 'var(--color-foreground, var(--ui-color-foreground))', description: 'Active tab text color.' },
      { name: '--ui-tabs-tab-active-bg', default: 'var(--color-background, white)', description: 'Active tab background.' },
      { name: '--ui-tabs-tab-active-shadow', default: '0 1px 2px 0 rgb(0 0 0 / 0.05)', description: 'Active tab box shadow.' },
      { name: '--ui-tabs-panel-padding', default: '1rem 0', description: 'Panel content padding.' },
      { name: '--ui-tabs-panel-text', default: 'var(--color-foreground, var(--ui-color-foreground))', description: 'Panel text color.' },
      { name: '--ui-tabs-ring', default: 'var(--color-ring, var(--ui-color-ring))', description: 'Focus ring color.' },
      { name: '--ui-tabs-transition', default: '150ms', description: 'Transition duration.' },
      { name: '--ui-tabs-indicator-color', default: 'var(--color-primary, var(--ui-color-primary))', description: 'Sliding indicator color.' },
      { name: '--ui-tabs-indicator-height', default: '2px', description: 'Sliding indicator height.' },
      { name: '--ui-tabs-indicator-radius', default: '9999px', description: 'Sliding indicator border radius.' },
      { name: '--ui-tabs-indicator-transition', default: '200ms', description: 'Sliding indicator transition duration.' },
      { name: '--ui-tabs-scroll-button-size', default: '2rem', description: 'Scroll button size for overflow.' },
    ];
    ```

    Do NOT change any other content in the file — examples, props tables, slots tables, events section, cssVarsCode, or JSX structure must remain untouched.
  </action>
  <verify>
    <automated>grep -c "ui-tabs-" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/TabsPage.tsx</automated>
    Expect: 25 entries in tabsCSSVars (at minimum 25 matches from the array, plus cssVarsCode occurrences — total will be higher).

    Also run:
    <automated>grep "ui-color-border\|ui-color-muted\|ui-color-foreground\|ui-color-ring\|ui-color-primary" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/TabsPage.tsx</automated>
    Expect: Matches confirming double-fallback var() form for color tokens.

    Also run:
    <automated>grep "color-background, white" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/TabsPage.tsx</automated>
    Expect: 1 match for --ui-tabs-tab-active-bg entry with literal white fallback.
  </verify>
  <done>tabsCSSVars has 25 entries with double-fallback var() defaults for all color tokens; --ui-tabs-tab-active-bg uses var(--color-background, white); all other token defaults match tailwind.css :root exactly.</done>
</task>

</tasks>

<verification>
grep "var(--color-muted, var(--ui-color-muted))" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/TabsPage.tsx
Expected: Found (correct double-fallback default for list-bg token).

grep "color-background, white" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/TabsPage.tsx
Expected: Found (active-bg literal white fallback preserved).

grep "var(--color-ring, var(--ui-color-ring))" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/TabsPage.tsx
Expected: Found (correct double-fallback default for ring token).
</verification>

<success_criteria>
- `tabsCSSVars` has exactly 25 entries
- All color token defaults use double-fallback `var(--color-X, var(--ui-color-X))` form (or literal white for active-bg)
- All defaults match `tailwind.css :root` exactly
</success_criteria>

<output>
After completion, create `.planning/phases/86-tabs/86-02-SUMMARY.md`
</output>
