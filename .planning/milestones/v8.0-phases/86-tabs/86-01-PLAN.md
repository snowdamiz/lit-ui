---
phase: 86-tabs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/styles/tailwind.css
autonomous: true
requirements:
  - TAB-01

must_haves:
  truths:
    - "Tabs renders with correct monochrome colors in dark mode via semantic cascade (no hardcoded gray values in .dark for tabs)"
    - "The .dark block in tailwind.css no longer contains any --ui-tabs-* declarations"
    - "The :root block tabs tokens are untouched and use double-fallback var() form for color tokens"
  artifacts:
    - path: "packages/core/src/styles/tailwind.css"
      provides: "Tabs token defaults in :root; all tabs .dark declarations removed"
      contains: "--ui-tabs-list-bg: var(--color-muted, var(--ui-color-muted))"
  key_links:
    - from: "packages/core/src/styles/tailwind.css (.dark block)"
      to: "tabs component dark mode appearance"
      via: "semantic cascade: .dark --color-muted cascades into --ui-tabs-list-bg"
      pattern: "Remove 8 lines (comment + 7 --ui-tabs-* declarations) from .dark block"
---

<objective>
Remove the 7 hardcoded tabs dark mode overrides from the `.dark` block in `tailwind.css`. The `:root` block already uses double-fallback semantic tokens that cascade correctly in dark mode — the explicit overrides are redundant and use raw `var(--color-gray-*)` values.

Unlike checkbox (check-color: white) or switch (thumb-bg: white), tabs has no exceptions — all 7 tokens cascade correctly via semantic `.dark` overrides. The `--ui-tabs-indicator-color` entry in `.dark` is identical to `:root` and is also removed as redundant.

Purpose: Tabs dark mode cascades automatically via `.dark --color-muted`, `.dark --color-muted-foreground`, `.dark --color-foreground`, and `.dark --color-background`, exactly as established in Phases 70–85.
Output: `tailwind.css` with 8 fewer lines (comment + 7 tabs dark declarations) in the `.dark` block.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key context the executor needs. -->

From packages/core/src/styles/tailwind.css :root block (ground truth — these stay unchanged):
```css
  --ui-tabs-border: var(--color-border, var(--ui-color-border));

  --ui-tabs-list-bg: var(--color-muted, var(--ui-color-muted));
  --ui-tabs-list-padding: 0.25rem;
  --ui-tabs-list-radius: 0.375rem;
  --ui-tabs-list-gap: 0.25rem;

  --ui-tabs-tab-padding: 0.5rem 1rem;
  --ui-tabs-tab-radius: 0.25rem;
  --ui-tabs-tab-font-size: 0.875rem;
  --ui-tabs-tab-font-weight: 500;
  --ui-tabs-tab-text: var(--color-muted-foreground, var(--ui-color-muted-foreground));
  --ui-tabs-tab-bg: transparent;
  --ui-tabs-tab-hover-text: var(--color-foreground, var(--ui-color-foreground));
  --ui-tabs-tab-hover-bg: transparent;

  --ui-tabs-tab-active-text: var(--color-foreground, var(--ui-color-foreground));
  --ui-tabs-tab-active-bg: var(--color-background, white);
  --ui-tabs-tab-active-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);

  --ui-tabs-panel-padding: 1rem 0;
  --ui-tabs-panel-text: var(--color-foreground, var(--ui-color-foreground));

  --ui-tabs-ring: var(--color-ring, var(--ui-color-ring));

  --ui-tabs-transition: 150ms;

  --ui-tabs-indicator-color: var(--color-primary, var(--ui-color-primary));
  --ui-tabs-indicator-height: 2px;
  --ui-tabs-indicator-radius: 9999px;
  --ui-tabs-indicator-transition: 200ms;

  --ui-tabs-scroll-button-size: 2rem;
```

From packages/core/src/styles/tailwind.css .dark block — ONLY THESE 8 LINES ARE TO BE REMOVED:
```css
  /* Tabs dark mode */
  --ui-tabs-list-bg: var(--color-gray-800);
  --ui-tabs-tab-text: var(--color-gray-400);
  --ui-tabs-tab-hover-text: var(--color-gray-50);
  --ui-tabs-tab-active-text: var(--color-gray-50);
  --ui-tabs-tab-active-bg: var(--color-gray-900);
  --ui-tabs-panel-text: var(--color-gray-50);
  --ui-tabs-indicator-color: var(--color-primary, var(--ui-color-primary));
```

Why removal is safe (no exceptions needed):
- `:root` defines `--ui-tabs-list-bg: var(--color-muted, var(--ui-color-muted))` — `.dark` overrides `--color-muted` to a dark gray; semantic cascade sufficient
- `:root` defines `--ui-tabs-tab-text: var(--color-muted-foreground, ...)` — `.dark` overrides `--color-muted-foreground`; semantic cascade sufficient
- `:root` defines `--ui-tabs-tab-hover-text` and `--ui-tabs-tab-active-text` and `--ui-tabs-panel-text` as `var(--color-foreground, ...)` — `.dark` overrides `--color-foreground` to a light value; semantic cascade sufficient
- `:root` defines `--ui-tabs-tab-active-bg: var(--color-background, white)` — `.dark` overrides `--color-background` to a dark value; the `white` fallback is only used if `--color-background` is unset (which it is not in `.dark`); semantic cascade sufficient
- `--ui-tabs-indicator-color` in `.dark` is identical to `:root` — purely redundant
- Tabs has NO raw color literals (unlike checkbox check-color: white or switch thumb-bg: white) that would require .dark exceptions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tabs dark mode declarations from .dark block</name>
  <files>packages/core/src/styles/tailwind.css</files>
  <action>
    In `packages/core/src/styles/tailwind.css`, locate the `.dark` block. Find and remove exactly these 8 lines (comment + 7 tabs declarations):

    ```
      /* Tabs dark mode */
      --ui-tabs-list-bg: var(--color-gray-800);
      --ui-tabs-tab-text: var(--color-gray-400);
      --ui-tabs-tab-hover-text: var(--color-gray-50);
      --ui-tabs-tab-active-text: var(--color-gray-50);
      --ui-tabs-tab-active-bg: var(--color-gray-900);
      --ui-tabs-panel-text: var(--color-gray-50);
      --ui-tabs-indicator-color: var(--color-primary, var(--ui-color-primary));
    ```

    Do NOT touch the `:root` block tabs tokens — they are correct and must stay.
    Do NOT touch any other component's declarations.

    The removal is correct because:
    - `:root` uses `var(--color-muted, var(--ui-color-muted))` for list-bg — `.dark` defines `--color-muted` as a dark gray (semantic cascade)
    - `:root` uses `var(--color-muted-foreground, ...)` for tab-text — `.dark` defines `--color-muted-foreground` (semantic cascade)
    - `:root` uses `var(--color-foreground, ...)` for hover-text, active-text, panel-text — `.dark` defines `--color-foreground` to a light gray (semantic cascade)
    - `:root` uses `var(--color-background, white)` for active-bg — `.dark` defines `--color-background` to a dark value, overriding the `white` fallback (semantic cascade)
    - `--ui-tabs-indicator-color` in `.dark` is identical to `:root` — redundant
    - Tabs has NO dark-mode exceptions (unlike checkbox check-color: white or switch thumb-bg: white)

    After removal, verify the .dark block contains no `--ui-tabs-*` declarations at all.
  </action>
  <verify>
    <automated>grep -n "ui-tabs" /Users/sn0w/Documents/dev/lit-components/packages/core/src/styles/tailwind.css</automated>
    Expect: All --ui-tabs-* occurrences are only in the :root block (lines ~835-875). Zero occurrences in the .dark block.
  </verify>
  <done>The .dark block has no --ui-tabs-* declarations. The :root block retains all 25 tabs token entries unchanged with double-fallback var() form for color tokens.</done>
</task>

</tasks>

<verification>
grep -n "ui-tabs" /Users/sn0w/Documents/dev/lit-components/packages/core/src/styles/tailwind.css
Expected: All matches are in :root block only (lines ~835-875). No tabs tokens in .dark block.

grep -c "ui-tabs" /Users/sn0w/Documents/dev/lit-components/packages/core/src/styles/tailwind.css
Expected: 25 occurrences (all in :root; the 7 .dark declarations removed).
</verification>

<success_criteria>
- `.dark` block contains no `--ui-tabs-*` declarations
- `:root` block retains all tabs tokens unchanged (25 entries)
- Tabs dark mode now cascades via `.dark` semantic color tokens
</success_criteria>

<output>
After completion, create `.planning/phases/86-tabs/86-01-SUMMARY.md`
</output>
