---
phase: 76-radio
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/skills/radio/SKILL.md
autonomous: true
requirements:
  - RAD-03

must_haves:
  truths:
    - "The radio SKILL.md CSS token table lists all 20 tokens from the tailwind.css :root radio block with exact default values"
    - "The Events table correctly names the consumer-facing event as ui-change (not change)"
    - "A Behavior Notes section exists covering key radio behavioral semantics"
  artifacts:
    - path: "skill/skills/radio/SKILL.md"
      provides: "Accurate radio skill file with expanded CSS token table and Behavior Notes"
      contains: "## Behavior Notes"
  key_links:
    - from: "skill/skills/radio/SKILL.md (CSS Custom Properties)"
      to: "packages/core/src/styles/tailwind.css (:root radio block)"
      via: "token defaults must match exactly"
      pattern: "ui-radio-size-sm|ui-radio-dot-size|ui-radio-font-size|ui-radio-border-error"
---

<objective>
Expand the `CSS Custom Properties` table in `skill/skills/radio/SKILL.md` from 10 to 20 entries covering all tokens in the tailwind.css :root radio block, fix the Events table event name from `change` to `ui-change`, and add a Behavior Notes section with key radio behavioral semantics.

Purpose: The skill file is what Claude uses to implement radio components in user projects. Stale or incomplete docs cause Claude to generate incorrect CSS overrides or miss important behavioral constraints. This brings the skill file to the same accuracy standard as button (70-03), dialog (71-03), input (72-03), textarea (73-03), select (74-03), and checkbox (75-03).

Output: `skill/skills/radio/SKILL.md` — CSS token table 10→20 entries, event name fixed, Behavior Notes section added
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-checkbox/75-03-SUMMARY.md

<interfaces>
<!-- Current SKILL.md CSS Custom Properties table (10 entries): -->
```markdown
| Property | Default | Description |
|----------|---------|-------------|
| `--ui-radio-border` | `var(--color-border)` | Border color of the radio circle. |
| `--ui-radio-border-checked` | `var(--color-primary)` | Border color when checked. |
| `--ui-radio-border-width` | `2px` | Border width of the radio circle. |
| `--ui-radio-bg` | `var(--color-background)` | Background color of the radio circle. |
| `--ui-radio-dot-color` | `var(--color-primary)` | Color of the inner dot when checked. |
| `--ui-radio-ring` | `var(--color-ring)` | Focus ring color. |
| `--ui-radio-transition` | `150ms` | Transition duration for state changes. |
| `--ui-radio-label-gap` | `0.5rem` | Gap between radio circle and label. |
| `--ui-radio-group-gap` | `0.5rem` | Gap between radio items in a group. |
| `--ui-radio-text-error` | `var(--color-destructive)` | Error message text color. |
```

<!-- Current Events table (event name is wrong — "change" not "ui-change"): -->
```markdown
| Event | Detail | Description |
|-------|--------|-------------|
| `change` | `{ value: string }` | Fired when the selected radio changes. Emitted on the radio-group. |
```

<!-- Full radio :root block in tailwind.css — source of truth: -->
```css
  --ui-radio-size-sm: 1rem;
  --ui-radio-size-md: 1.25rem;
  --ui-radio-size-lg: 1.5rem;
  --ui-radio-dot-size-sm: 0.5rem;
  --ui-radio-dot-size-md: 0.625rem;
  --ui-radio-dot-size-lg: 0.75rem;
  --ui-radio-label-gap: 0.5rem;
  --ui-radio-transition: 150ms;
  --ui-radio-border-width: 2px;
  --ui-radio-font-size-sm: 0.875rem;
  --ui-radio-font-size-md: 1rem;
  --ui-radio-font-size-lg: 1.125rem;
  --ui-radio-bg: var(--color-background, white);
  --ui-radio-border: var(--color-border, var(--ui-color-border));
  --ui-radio-border-checked: var(--color-primary, var(--ui-color-primary));
  --ui-radio-dot-color: var(--color-primary, var(--ui-color-primary));
  --ui-radio-ring: var(--color-ring, var(--ui-color-ring));
  --ui-radio-border-error: var(--color-destructive, var(--ui-color-destructive));
  --ui-radio-text-error: var(--color-destructive, var(--ui-color-destructive));
  --ui-radio-group-gap: 0.5rem;
```

<!-- Events from radio-group.ts source — consumer-facing events: -->
<!-- dispatchCustomEvent(this, 'ui-change', { value: this.value }) — fired on lui-radio-group -->
<!-- Internal: dispatchCustomEvent(this, 'ui-radio-change', ...) — fired on lui-radio, consumed by group, NOT for consumer use -->

<!-- Behavior from radio.ts and radio-group.ts source code: -->
<!-- - Radio does NOT toggle its own checked state (RadioGroup manages mutual exclusion) -->
<!-- - RadioGroup is form-associated (static formAssociated = true), Radio is NOT -->
<!-- - Arrow keys move focus AND selection simultaneously (roving tabindex, W3C APG radio group pattern) -->
<!-- - Space key selects focused radio (handled by individual lui-radio handleKeyDown) -->
<!-- - formResetCallback restores defaultValue (initial value at connectedCallback) -->
<!-- - formDisabledCallback propagates disabled to child radios -->
<!-- - Required validation: error shows only after touched=true (user has interacted) -->
<!-- - Disabled propagation: group.disabled=true sets all child radios disabled=true -->
<!-- - ui-radio-change is internal event (stopPropagation'd by RadioGroup) — consumer should NOT listen to it -->
<!-- - prefers-reduced-motion: transition-duration 0ms on .radio-dot and .radio-circle -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CSS tokens, fix event name, and add Behavior Notes to SKILL.md</name>
  <files>skill/skills/radio/SKILL.md</files>
  <action>
Make three changes to `skill/skills/radio/SKILL.md`:

**Change 1: Replace the CSS Custom Properties table (10→20 entries)**

Replace the current `## CSS Custom Properties` section with:

```markdown
## CSS Custom Properties

| Property | Default | Description |
|----------|---------|-------------|
| `--ui-radio-size-sm` | `1rem` | Radio circle width and height at sm size. |
| `--ui-radio-size-md` | `1.25rem` | Radio circle width and height at md size (default). |
| `--ui-radio-size-lg` | `1.5rem` | Radio circle width and height at lg size. |
| `--ui-radio-dot-size-sm` | `0.5rem` | Inner dot width and height at sm size. |
| `--ui-radio-dot-size-md` | `0.625rem` | Inner dot width and height at md size (default). |
| `--ui-radio-dot-size-lg` | `0.75rem` | Inner dot width and height at lg size. |
| `--ui-radio-border-width` | `2px` | Border width of the radio circle. |
| `--ui-radio-label-gap` | `0.5rem` | Gap between radio circle and label. |
| `--ui-radio-group-gap` | `0.5rem` | Gap between radio items in a group. |
| `--ui-radio-transition` | `150ms` | Transition duration for state changes. |
| `--ui-radio-font-size-sm` | `0.875rem` | Label font size at sm size. |
| `--ui-radio-font-size-md` | `1rem` | Label font size at md size (default). |
| `--ui-radio-font-size-lg` | `1.125rem` | Label font size at lg size. |
| `--ui-radio-bg` | `var(--color-background, white)` | Background color of the radio circle (unchecked). |
| `--ui-radio-border` | `var(--color-border, var(--ui-color-border))` | Border color of the radio circle (unchecked). |
| `--ui-radio-border-checked` | `var(--color-primary, var(--ui-color-primary))` | Border color when checked. |
| `--ui-radio-dot-color` | `var(--color-primary, var(--ui-color-primary))` | Color of the inner dot when checked. |
| `--ui-radio-ring` | `var(--color-ring, var(--ui-color-ring))` | Focus ring color. |
| `--ui-radio-border-error` | `var(--color-destructive, var(--ui-color-destructive))` | Border color in error/invalid state. |
| `--ui-radio-text-error` | `var(--color-destructive, var(--ui-color-destructive))` | Error message text color. |
```

**Change 2: Fix the Events table**

Replace the current Events section (which incorrectly uses `change`):

```markdown
## Events

| Event | Fired on | Detail | Description |
|-------|----------|--------|-------------|
| `ui-change` | `lui-radio-group` | `{ value: string }` | Fired when the selected radio changes. |
| `ui-radio-change` | `lui-radio` | `{ value: string }` | Internal event consumed by RadioGroup. Do not listen to this — use `ui-change` on the group. |
```

**Change 3: Add Behavior Notes section at the end of the file**

Append after the Events section:

```markdown
## Behavior Notes

- **Mutual exclusion**: `lui-radio` does NOT toggle its own `checked` state. It dispatches `ui-radio-change` internally; `lui-radio-group` handles mutual exclusion by setting `checked` on the matching child radio.
- **Form participation**: `lui-radio-group` is form-associated (uses ElementInternals, client-side only with `isServer` guard). Individual `lui-radio` is NOT form-associated. The group submits its selected `value` under the group's `name` attribute.
- **Roving tabindex**: RadioGroup implements the W3C APG Radio Group pattern. Only the checked (or first enabled) radio receives `tabIndex=0`. All others get `tabIndex=-1`. This creates a single tab stop for the entire group.
- **Keyboard**: Arrow Down/Right moves focus and selection to next radio (wraps). Arrow Up/Left moves to previous (wraps). Space selects the focused radio. Tab/Shift+Tab moves into/out of the group.
- **Required validation**: Error message shows only after `touched=true` (the user has interacted with the group via arrow keys or click). Submitting the form with nothing selected also triggers the error.
- **Disabled propagation**: Setting `disabled` on `lui-radio-group` propagates `disabled=true` to all child `lui-radio` elements. Group-level disabled renders at 50% opacity via `:host([disabled])` on the group.
- **Form reset**: `formResetCallback` restores the group to its `defaultValue` (the `value` attribute at `connectedCallback` time) and clears `touched` and error state.
- **Focus ring**: Applied to `.radio-circle:focus-visible` (the inner div with `role="radio"`, not the host). Uses `outline: 2px solid` with 2px offset.
- **prefers-reduced-motion**: `transition-duration: 0ms` is applied to `.radio-dot` and `.radio-circle` when the user prefers reduced motion.
```
  </action>
  <verify>
    <automated>grep -c "ui-radio-size-sm\|ui-radio-dot-size\|ui-radio-font-size\|ui-radio-border-error\|Behavior Notes" /Users/sn0w/Documents/dev/lit-components/skill/skills/radio/SKILL.md</automated>
  </verify>
  <done>
The SKILL.md file contains:
- 20-entry CSS Custom Properties table (all tokens from tailwind.css :root radio block)
- Events table with `ui-change` on `lui-radio-group` and `ui-radio-change` internal event note
- Behavior Notes section with at least 9 entries covering mutual exclusion, form participation, roving tabindex, keyboard, validation, disabled propagation, form reset, focus ring, and reduced motion
The grep command returns 5 (one for each of the 4 new token names + 1 for Behavior Notes).
  </done>
</task>

</tasks>

<verification>
After task completion:
1. `grep -c "ui-radio" skill/skills/radio/SKILL.md` — returns 20+ (one per token entry)
2. `grep "ui-change" skill/skills/radio/SKILL.md` — shows the corrected event name
3. `grep "Behavior Notes" skill/skills/radio/SKILL.md` — shows the section header
4. `grep "change\b" skill/skills/radio/SKILL.md` — does NOT show standalone `change` event (only `ui-change`)
</verification>

<success_criteria>
- CSS Custom Properties table has 20 entries covering all tokens from tailwind.css :root radio block
- Color token defaults use double-fallback var() form (e.g., `var(--color-border, var(--ui-color-border))`)
- Structural tokens use exact rem/px values from tailwind.css :root
- Events table uses `ui-change` (not `change`) and includes note that `ui-radio-change` is internal
- Behavior Notes section added with at least 9 entries
</success_criteria>

<output>
After completion, create `.planning/phases/76-radio/76-03-SUMMARY.md` following the summary template.
</output>
