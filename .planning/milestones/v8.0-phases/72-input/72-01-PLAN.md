---
phase: 72-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/styles/tailwind.css
autonomous: true
requirements: [INP-01]

must_haves:
  truths:
    - "Input dark mode tokens inherit from semantic .dark overrides — no hardcoded oklch values in the .dark block"
    - "All input state tokens (bg, text, border, placeholder, focus, disabled) use the double-fallback pattern in :root"
    - "Input token defaults in :root match the THEME-SPEC.md values exactly"
  artifacts:
    - path: "packages/core/src/styles/tailwind.css"
      provides: "Input CSS tokens in :root and cleaned-up .dark block"
      contains: "--ui-input-bg: var(--color-background"
  key_links:
    - from: "packages/core/src/styles/tailwind.css (.dark block)"
      to: "Semantic dark tokens (.dark { --color-background, --color-foreground, --color-border, etc. })"
      via: "double-fallback var(--color-*, var(--ui-color-*))"
      pattern: "ui-input.*var\\(--color-"
---

<objective>
Polish the Input component CSS defaults in tailwind.css to match the v8.0 monochrome theme by removing redundant hardcoded dark mode overrides.

Purpose: The .dark block in tailwind.css contains 9 hardcoded oklch values for input tokens (lines 393-401). These bypass the semantic dark mode token cascade. The :root block tokens correctly use the double-fallback pattern — but the .dark block overrides them with raw oklch values, breaking the token contract. The same pattern was applied to Button (Phase 70-01) and Dialog (Phase 71-01).

Output: The .dark block no longer overrides input color tokens. The semantic .dark overrides of --color-background, --color-foreground, --color-border, --color-muted-foreground, --color-ring, and --color-muted are sufficient to drive input dark mode appearance via the :root double-fallback cascade.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-theme-foundation/THEME-SPEC.md

<interfaces>
<!-- Current state of input tokens in tailwind.css. Executor should verify these before editing. -->

From packages/core/src/styles/tailwind.css (:root block, CORRECT — matches THEME-SPEC):
```css
/* Layout */
--ui-input-radius: 0.375rem;
--ui-input-border-width: 1px;
--ui-input-transition: 150ms;

/* Typography */
--ui-input-font-size-sm: 0.875rem;
--ui-input-font-size-md: 1rem;
--ui-input-font-size-lg: 1.125rem;

/* Spacing */
--ui-input-padding-x-sm: 0.75rem;
--ui-input-padding-y-sm: 0.375rem;
--ui-input-padding-x-md: 1rem;
--ui-input-padding-y-md: 0.5rem;
--ui-input-padding-x-lg: 1.25rem;
--ui-input-padding-y-lg: 0.75rem;

/* Default state colors — all use double-fallback (CORRECT) */
--ui-input-bg: var(--color-background, white);
--ui-input-text: var(--color-foreground, var(--ui-color-foreground));
--ui-input-border: var(--color-border, var(--ui-color-border));
--ui-input-placeholder: var(--color-muted-foreground, var(--ui-color-muted-foreground));

/* Focus state */
--ui-input-border-focus: var(--color-ring, var(--ui-color-ring));
--ui-input-ring: var(--color-ring, var(--ui-color-ring));

/* Error state */
--ui-input-border-error: var(--color-destructive, var(--ui-color-destructive));
--ui-input-text-error: var(--color-destructive, var(--ui-color-destructive));

/* Disabled state */
--ui-input-bg-disabled: var(--color-muted, var(--ui-color-muted));
--ui-input-text-disabled: var(--color-muted-foreground, var(--ui-color-muted-foreground));
--ui-input-border-disabled: var(--color-border, var(--ui-color-border));
```

From packages/core/src/styles/tailwind.css (.dark block, PROBLEMATIC — needs removal):
```css
/* Input dark mode */
--ui-input-bg: oklch(0.10 0 0);
--ui-input-text: oklch(0.985 0 0);
--ui-input-border: oklch(0.37 0 0);
--ui-input-placeholder: oklch(0.55 0 0);
--ui-input-border-focus: oklch(0.71 0 0);
--ui-input-ring: oklch(0.55 0 0);
--ui-input-bg-disabled: oklch(0.18 0 0);
--ui-input-text-disabled: oklch(0.55 0 0);
--ui-input-border-disabled: oklch(0.37 0 0);
```

Why these should be removed: The .dark semantic block already defines:
- `--color-background: oklch(0.10 0 0)` → --ui-input-bg inherits via var(--color-background, white)
- `--color-foreground: oklch(0.985 0 0)` → --ui-input-text inherits via var(--color-foreground, ...)
- `--color-border: oklch(0.37 0 0)` → --ui-input-border and --ui-input-border-disabled inherit
- `--color-muted-foreground: oklch(0.71 0 0)` → --ui-input-placeholder inherits
- `--color-ring: oklch(0.55 0 0)` → --ui-input-border-focus and --ui-input-ring inherit
- `--color-muted: oklch(0.27 0 0)` → --ui-input-bg-disabled inherits
The explicit .dark input overrides are redundant and violate the double-fallback contract.

Note: --ui-input-border-error and --ui-input-text-error are NOT in the .dark block (correctly omitted — destructive color changes in .dark are handled by the --color-destructive semantic override).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hardcoded input tokens from .dark block in tailwind.css</name>
  <files>packages/core/src/styles/tailwind.css</files>
  <action>
In packages/core/src/styles/tailwind.css, find the .dark block. Locate the "/* Input dark mode */" comment and the 9 lines of hardcoded --ui-input-* overrides below it (bg, text, border, placeholder, border-focus, ring, bg-disabled, text-disabled, border-disabled).

Remove the entire "/* Input dark mode */" comment and all 9 --ui-input-* property lines from the .dark block.

These overrides are redundant: the .dark block already overrides the semantic --color-* tokens (background, foreground, border, muted-foreground, ring, muted), and the :root input tokens use the double-fallback pattern var(--color-*, var(--ui-color-*)), so they automatically pick up the correct dark values without explicit input token overrides.

Do NOT modify the :root block — it is correct and matches THEME-SPEC.md. Do NOT modify input.ts — the component CSS is correct.

After editing, verify the .dark block no longer contains any --ui-input-* declarations.
  </action>
  <verify>
    <automated>grep -c "ui-input" /Users/sn0w/Documents/dev/lit-components/packages/core/src/styles/tailwind.css | xargs -I{} sh -c 'grep "dark" /Users/sn0w/Documents/dev/lit-components/packages/core/src/styles/tailwind.css | grep "ui-input" | wc -l'</automated>
  </verify>
  <done>The .dark block contains no --ui-input-* declarations. The :root block still has all 20+ input tokens using the double-fallback pattern. Running `grep "ui-input" packages/core/src/styles/tailwind.css` shows only :root block entries (lines 592-630 range), none inside .dark.</done>
</task>

</tasks>

<verification>
1. Run: `grep -n "ui-input" packages/core/src/styles/tailwind.css` — all results should be in the `:root` block only, not in the `.dark` block
2. Run: `grep -c "ui-input-bg: oklch" packages/core/src/styles/tailwind.css` — should output `0`
3. Run: `grep "ui-input-bg" packages/core/src/styles/tailwind.css` — should show only the :root entry with `var(--color-background, white)`
</verification>

<success_criteria>
- The .dark block no longer contains any --ui-input-* properties
- The :root block input tokens remain intact and match THEME-SPEC.md
- Input dark mode appearance is now governed entirely by the semantic .dark overrides
- INP-01 satisfied: Input default styles match the v8.0 monochrome theme via the correct token inheritance chain
</success_criteria>

<output>
After completion, create `.planning/phases/72-input/72-01-SUMMARY.md` with what was changed, why, and the before/after for the .dark block removal.
</output>
