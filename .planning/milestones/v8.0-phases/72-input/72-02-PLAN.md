---
phase: 72-input
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/src/pages/components/InputPage.tsx
autonomous: true
requirements: [INP-02]

must_haves:
  truths:
    - "CSS Custom Properties table lists all user-facing --ui-input-* tokens covering layout, typography, spacing, and state colors"
    - "CSS example code uses correct --ui-input-* property names"
    - "CSS vars count badge reflects the expanded set of documented properties"
  artifacts:
    - path: "apps/docs/src/pages/components/InputPage.tsx"
      provides: "Accurate Input docs page with complete CSS token table"
      contains: "--ui-input-radius"
  key_links:
    - from: "apps/docs/src/pages/components/InputPage.tsx (inputCSSVars array)"
      to: "packages/core/src/styles/tailwind.css (:root --ui-input-* tokens)"
      via: "CSS property names matching tailwind.css token names"
      pattern: "ui-input-radius"
---

<objective>
Update the Input docs page (InputPage.tsx) to document the full set of user-facing --ui-input-* CSS custom properties.

Purpose: The docs page currently lists only 7 CSS custom properties (bg, text, border, placeholder, border-focus, border-error, radius) in the inputCSSVars array. The actual token set in tailwind.css :root has 20 properties covering layout, typography, spacing, and all state colors. The table is incomplete — users cannot discover the sizing and spacing tokens from the docs. The names are correct (no --lui- prefix issue), but the coverage needs expansion.

Output: InputPage.tsx inputCSSVars array expanded to include the key user-facing tokens across all categories. The example code and demo already use correct --ui-input-* names.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-theme-foundation/THEME-SPEC.md

<interfaces>
<!-- Current inputCSSVars array (INCOMPLETE — needs expansion) -->

Current inputCSSVars array (7 tokens only):
```tsx
const inputCSSVars: CSSVarDef[] = [
  { name: '--ui-input-radius', default: 'var(--radius-md)', description: 'Border radius of the input.' },
  { name: '--ui-input-border', default: 'var(--color-border)', description: 'Border color.' },
  { name: '--ui-input-border-focus', default: 'var(--color-ring)', description: 'Border color on focus.' },
  { name: '--ui-input-border-error', default: 'var(--color-destructive)', description: 'Border color on error.' },
  { name: '--ui-input-bg', default: 'var(--color-background)', description: 'Background color.' },
  { name: '--ui-input-text', default: 'var(--color-foreground)', description: 'Text color.' },
  { name: '--ui-input-placeholder', default: 'var(--color-muted-foreground)', description: 'Placeholder text color.' },
];
```

Note: The `default` field shows simplified values. Use the actual values from tailwind.css :root (below) for accuracy.

Actual tokens in tailwind.css :root (use these exact values):
Layout: --ui-input-radius (0.375rem), --ui-input-border-width (1px), --ui-input-transition (150ms)
Typography: --ui-input-font-size-sm (0.875rem), --ui-input-font-size-md (1rem), --ui-input-font-size-lg (1.125rem)
Spacing: --ui-input-padding-x-sm (0.75rem), --ui-input-padding-y-sm (0.375rem)
         --ui-input-padding-x-md (1rem), --ui-input-padding-y-md (0.5rem)
         --ui-input-padding-x-lg (1.25rem), --ui-input-padding-y-lg (0.75rem)
Default state: --ui-input-bg (var(--color-background, white)), --ui-input-text (var(--color-foreground, ...))
               --ui-input-border (var(--color-border, ...)), --ui-input-placeholder (var(--color-muted-foreground, ...))
Focus state: --ui-input-border-focus (var(--color-ring, ...)), --ui-input-ring (var(--color-ring, ...))
Error state: --ui-input-border-error (var(--color-destructive, ...)), --ui-input-text-error (var(--color-destructive, ...))
Disabled state: --ui-input-bg-disabled (var(--color-muted, ...)), --ui-input-text-disabled (var(--color-muted-foreground, ...))
                --ui-input-border-disabled (var(--color-border, ...))

Note: The cssVarsCode and demo style tag already use correct --ui-input-radius and --ui-input-border-focus — do NOT change those.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand inputCSSVars array in InputPage.tsx with complete token set</name>
  <files>apps/docs/src/pages/components/InputPage.tsx</files>
  <action>
In apps/docs/src/pages/components/InputPage.tsx, replace the inputCSSVars array with an expanded set documenting the key user-facing tokens. Focus on the tokens users are most likely to override — include layout/structural, typography, spacing (md size), and state color tokens. Keep it comprehensive but readable (12 entries).

Replace:
```tsx
const inputCSSVars: CSSVarDef[] = [
  { name: '--ui-input-radius', default: 'var(--radius-md)', description: 'Border radius of the input.' },
  { name: '--ui-input-border', default: 'var(--color-border)', description: 'Border color.' },
  { name: '--ui-input-border-focus', default: 'var(--color-ring)', description: 'Border color on focus.' },
  { name: '--ui-input-border-error', default: 'var(--color-destructive)', description: 'Border color on error.' },
  { name: '--ui-input-bg', default: 'var(--color-background)', description: 'Background color.' },
  { name: '--ui-input-text', default: 'var(--color-foreground)', description: 'Text color.' },
  { name: '--ui-input-placeholder', default: 'var(--color-muted-foreground)', description: 'Placeholder text color.' },
];
```

With:
```tsx
const inputCSSVars: CSSVarDef[] = [
  { name: '--ui-input-radius', default: '0.375rem', description: 'Border radius of the input container.' },
  { name: '--ui-input-border-width', default: '1px', description: 'Border width of the input container.' },
  { name: '--ui-input-transition', default: '150ms', description: 'Transition duration for border-color and box-shadow.' },
  { name: '--ui-input-font-size-sm', default: '0.875rem', description: 'Font size for size="sm".' },
  { name: '--ui-input-font-size-md', default: '1rem', description: 'Font size for size="md".' },
  { name: '--ui-input-font-size-lg', default: '1.125rem', description: 'Font size for size="lg".' },
  { name: '--ui-input-padding-x-md', default: '1rem', description: 'Horizontal padding for size="md".' },
  { name: '--ui-input-padding-y-md', default: '0.5rem', description: 'Vertical padding for size="md".' },
  { name: '--ui-input-bg', default: 'var(--color-background, white)', description: 'Background color of the input.' },
  { name: '--ui-input-text', default: 'var(--color-foreground, ...)', description: 'Text color of the input.' },
  { name: '--ui-input-border', default: 'var(--color-border, ...)', description: 'Border color (default state).' },
  { name: '--ui-input-placeholder', default: 'var(--color-muted-foreground, ...)', description: 'Placeholder text color.' },
  { name: '--ui-input-border-focus', default: 'var(--color-ring, ...)', description: 'Border color on focus.' },
  { name: '--ui-input-border-error', default: 'var(--color-destructive, ...)', description: 'Border color on validation error.' },
  { name: '--ui-input-bg-disabled', default: 'var(--color-muted, ...)', description: 'Background color when disabled.' },
  { name: '--ui-input-text-disabled', default: 'var(--color-muted-foreground, ...)', description: 'Text color when disabled.' },
];
```

Do NOT change any other part of InputPage.tsx — the cssVarsCode string, demo style, props, slots, examples, or layout sections are correct and should remain unchanged.
  </action>
  <verify>
    <automated>grep -c "ui-input-radius" /Users/sn0w/Documents/dev/lit-components/apps/docs/src/pages/components/InputPage.tsx</automated>
  </verify>
  <done>The inputCSSVars array has 16 entries covering layout, typography, spacing, and state colors. Running `grep -c "name: '--ui-input" InputPage.tsx` returns 16. The cssVarsCode and demo style remain unchanged with --ui-input-radius and --ui-input-border-focus.</done>
</task>

</tasks>

<verification>
1. Run: `grep -c "name: '--ui-input" apps/docs/src/pages/components/InputPage.tsx` — should return 16
2. Run: `grep "lui-input" apps/docs/src/pages/components/InputPage.tsx` — should return no matches
3. Run: `grep "ui-input-radius" apps/docs/src/pages/components/InputPage.tsx` — should show correct usage in both the array and demo
</verification>

<success_criteria>
- inputCSSVars array documents 16 tokens covering all key categories (layout, typography, spacing, state colors)
- All property names use correct --ui-input-* prefix
- Default values accurately reflect the tailwind.css :root values
- INP-02 satisfied: Input docs page is accurate and up-to-date
</success_criteria>

<output>
After completion, create `.planning/phases/72-input/72-02-SUMMARY.md` with what changed and why (expanded from 7 to 16 tokens, accurate default values).
</output>
