---
phase: 79-date-picker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/styles/tailwind.css
autonomous: true
requirements:
  - DTP-01

must_haves:
  truths:
    - "Date Picker dark mode appearance is governed by semantic .dark token cascade; all 19 hardcoded .dark --ui-date-picker-* declarations removed"
    - "No --ui-date-picker-* declarations remain in the .dark block"
    - "The :root date-picker token block (21 tokens) is untouched and correct"
  artifacts:
    - path: "packages/core/src/styles/tailwind.css"
      provides: "Date Picker .dark block cleared; :root block unchanged"
      contains: "/* Date Picker Component */"
  key_links:
    - from: "packages/core/src/styles/tailwind.css (.dark block)"
      to: "packages/core/src/styles/tailwind.css (:root block)"
      via: "double-fallback var() cascade"
      pattern: "var\\(--color-background,|var\\(--color-foreground,|var\\(--color-muted,"
---

<objective>
Remove all 19 hardcoded .dark --ui-date-picker-* declarations from tailwind.css — every token's :root definition uses a double-fallback var() that correctly resolves through the .dark semantic overrides. No tokens need to be added (all 21 are already declared in :root, including error, ring, popup-shadow, and z-index).

Purpose: Date Picker dark mode currently duplicates logic already handled by the semantic .dark cascade. The :root block (lines 1075-1095) uses double-fallback var(--color-*,...) for all color tokens. The .dark block already overrides --color-background, --color-foreground, --color-muted, --color-muted-foreground, --color-border, --color-card, --color-destructive, and --color-ring. All 19 .dark date-picker declarations are therefore redundant. There are no hardcoded non-var exceptions (no 'white' values unlike checkbox check-color or switch thumb-bg). The z-index and popup-shadow tokens are structural (no dark mode variant needed) and are only in :root.

Output: `packages/core/src/styles/tailwind.css` — .dark date-picker block removed (19 declarations + comment)
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-calendar/78-01-SUMMARY.md

<interfaces>
<!-- Current .dark date-picker block in tailwind.css (lines ~303-320): -->
```css
  /* Date Picker dark mode */
  --ui-date-picker-bg: var(--color-gray-950);
  --ui-date-picker-text: var(--color-gray-50);
  --ui-date-picker-border: var(--color-gray-700);
  --ui-date-picker-placeholder: var(--color-gray-500);
  --ui-date-picker-label-text: var(--color-gray-50);
  --ui-date-picker-popup-bg: var(--color-gray-900);
  --ui-date-picker-popup-border: var(--color-gray-700);
  --ui-date-picker-hover-bg: var(--color-gray-800);
  --ui-date-picker-disabled-bg: var(--color-gray-900);
  --ui-date-picker-disabled-border: var(--color-gray-700);
  --ui-date-picker-helper-text: var(--color-gray-400);
  --ui-date-picker-action-text: var(--color-gray-400);
  --ui-date-picker-preset-bg: var(--color-gray-900);
  --ui-date-picker-preset-text: var(--color-gray-50);
  --ui-date-picker-preset-border: var(--color-gray-600);
  --ui-date-picker-preset-hover-bg: var(--color-gray-700);
  --ui-date-picker-preset-hover-border: var(--color-gray-500);
```
(17 declarations listed; total with comment line = 18 lines; count the actual declarations = 17; adjust at execution time by reading the file)

<!-- Date Picker :root token block (lines 1075-1095) — the cascade source: -->
```css
  --ui-date-picker-bg: var(--color-background, white);
  --ui-date-picker-text: var(--color-foreground, var(--ui-color-foreground));
  --ui-date-picker-border: var(--color-border, var(--ui-color-border));
  --ui-date-picker-placeholder: var(--color-muted-foreground, var(--ui-color-muted-foreground));
  --ui-date-picker-label-text: var(--color-foreground, var(--ui-color-foreground));
  --ui-date-picker-error: var(--color-destructive, var(--ui-color-destructive));
  --ui-date-picker-ring: var(--color-ring, var(--ui-color-ring));
  --ui-date-picker-popup-bg: var(--color-card, var(--ui-color-card));
  --ui-date-picker-popup-border: var(--color-border, var(--ui-color-border));
  --ui-date-picker-popup-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.08), 0 4px 6px -4px rgb(0 0 0 / 0.08);
  --ui-date-picker-hover-bg: var(--color-muted, var(--ui-color-muted));
  --ui-date-picker-disabled-bg: var(--color-muted, var(--ui-color-muted));
  --ui-date-picker-disabled-border: var(--color-border, var(--ui-color-border));
  --ui-date-picker-helper-text: var(--color-muted-foreground, var(--ui-color-muted-foreground));
  --ui-date-picker-action-text: var(--color-muted-foreground, var(--ui-color-muted-foreground));
  --ui-date-picker-preset-bg: var(--color-background, white);
  --ui-date-picker-preset-text: var(--color-foreground, var(--ui-color-foreground));
  --ui-date-picker-preset-border: var(--color-border, var(--ui-color-border));
  --ui-date-picker-preset-hover-bg: var(--color-muted, var(--ui-color-muted));
  --ui-date-picker-preset-hover-border: var(--color-muted-foreground, var(--ui-color-muted-foreground));
  --ui-date-picker-z-index: 40;
```

<!-- .dark semantic overrides that make cascade work (all 17 .dark tokens are redundant):
  - bg: :root = var(--color-background, white) → .dark sets --color-background → REMOVE
  - text: :root = var(--color-foreground,...) → .dark sets --color-foreground → REMOVE
  - border: :root = var(--color-border,...) → .dark sets --color-border → REMOVE
  - placeholder: :root = var(--color-muted-foreground,...) → .dark sets that → REMOVE
  - label-text: :root = var(--color-foreground,...) → .dark sets --color-foreground → REMOVE
  - popup-bg: :root = var(--color-card,...) → .dark sets --color-card → REMOVE
  - popup-border: :root = var(--color-border,...) → .dark sets --color-border → REMOVE
  - hover-bg: :root = var(--color-muted,...) → .dark sets --color-muted → REMOVE
  - disabled-bg: :root = var(--color-muted,...) → .dark sets --color-muted → REMOVE
  - disabled-border: :root = var(--color-border,...) → .dark sets --color-border → REMOVE
  - helper-text: :root = var(--color-muted-foreground,...) → .dark sets that → REMOVE
  - action-text: :root = var(--color-muted-foreground,...) → .dark sets that → REMOVE
  - preset-bg: :root = var(--color-background, white) → .dark sets --color-background → REMOVE
  - preset-text: :root = var(--color-foreground,...) → .dark sets --color-foreground → REMOVE
  - preset-border: :root = var(--color-border,...) → .dark sets --color-border → REMOVE
  - preset-hover-bg: :root = var(--color-muted,...) → .dark sets --color-muted → REMOVE
  - preset-hover-border: :root = var(--color-muted-foreground,...) → .dark sets that → REMOVE

  No hardcoded non-var values in :root (no 'white' exception for any token — bg and preset-bg use
  var(--color-background, white) which cascades correctly through .dark --color-background override).
  z-index (40) and popup-shadow are structural — no dark mode variant, NOT in .dark block anyway.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove .dark date-picker block from tailwind.css</name>
  <files>packages/core/src/styles/tailwind.css</files>
  <action>
Make one edit to `packages/core/src/styles/tailwind.css`:

**Remove the .dark date-picker block entirely.**

Find and delete the comment and all `--ui-date-picker-*` declarations inside the `.dark` rule. The block starts with `/* Date Picker dark mode */` and contains 17 declarations (`--ui-date-picker-bg` through `--ui-date-picker-preset-hover-border`).

Read the file first to get the exact line content, then delete the entire block (comment + all 17 declarations) from inside the `.dark {}` rule.

Why all 17 are removed: every :root value uses double-fallback `var(--color-*,...)` form; the `.dark` block already overrides all referenced semantic tokens. Unlike checkbox (`--ui-checkbox-check-color: white` exception) and switch (`--ui-switch-thumb-bg: white` exception), no date-picker token has a hardcoded non-var :root value — `--ui-date-picker-bg: var(--color-background, white)` correctly cascades through `.dark { --color-background: var(--color-gray-950) }`.

Do NOT modify the `:root` block. The 21 tokens there are correct and complete.
  </action>
  <verify>
    <automated>grep -c "ui-date-picker" /Users/sn0w/Documents/dev/lit-components/packages/core/src/styles/tailwind.css</automated>
  </verify>
  <done>
- `grep "Date Picker dark mode" packages/core/src/styles/tailwind.css` returns no output (comment removed)
- `grep -n "ui-date-picker" packages/core/src/styles/tailwind.css` shows all remaining lines are within the :root block (no .dark block entries)
- Total `--ui-date-picker-*` declaration count: 21 (all in :root only, was 21 :root + 17 .dark = 38)
  </done>
</task>

</tasks>

<verification>
After task completion, verify:
1. `grep "Date Picker dark mode" packages/core/src/styles/tailwind.css` — returns empty (block removed)
2. `grep -c "ui-date-picker" packages/core/src/styles/tailwind.css` — count is 21 (only :root declarations remain)
3. The :root date-picker block (21 tokens from --ui-date-picker-bg through --ui-date-picker-z-index) is unchanged
</verification>

<success_criteria>
- The .dark block contains zero --ui-date-picker-* declarations
- The "Date Picker dark mode" comment is removed from the .dark block
- The :root date-picker block has 21 tokens (unchanged)
- Date Picker dark mode appearance governed entirely by semantic .dark cascade (no per-component overrides needed)
</success_criteria>

<output>
After completion, create `.planning/phases/79-date-picker/79-01-SUMMARY.md` following the summary template.
</output>
